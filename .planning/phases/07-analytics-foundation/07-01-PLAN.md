---
phase: 07-analytics-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/analytics/events.ts
  - src/lib/analytics/events.test.ts
autonomous: true
requirements:
  - FOUND-03
  - FOUND-05
must_haves:
  truths:
    - "posthog-js and posthog-node are installed in node_modules and present in package.json dependencies"
    - "package.json declares engines.node 20.x satisfying posthog-node@5 peer requirement"
    - "src/lib/analytics/events.ts exports an EVENT const object with all event names following category:object_action naming"
    - "All event name string values pass the category:object_action format test"
    - "events.test.ts passes: npm test covers FOUND-03 format validation"
  artifacts:
    - path: "src/lib/analytics/events.ts"
      provides: "Typed event catalog — EVENT const object and EventName type"
      exports: ["EVENT", "EventName"]
    - path: "src/lib/analytics/events.test.ts"
      provides: "Vitest unit tests validating event name format"
    - path: "package.json"
      provides: "posthog-js, posthog-node dependencies + engines.node field"
      contains: "20.x"
  key_links:
    - from: "src/lib/analytics/events.ts"
      to: "Phase 8 instrumentation call sites"
      via: "import { EVENT } from '@/lib/analytics/events' — typed constant replaces inline strings"
      pattern: "EVENT\\."
---

<objective>
Install PostHog SDKs, declare the Node.js engine constraint, and build the typed event catalog with tests.

Purpose: FOUND-03 (typed catalog) and FOUND-05 (Node 20.x runtime) must be in place before any other PostHog wiring. The catalog is the foundation every Phase 8 event call site imports. The engine field is required before `posthog-node@5` runs on Vercel.

Output: `posthog-js` and `posthog-node` installed, `package.json` engines field set to `20.x`, `src/lib/analytics/events.ts` with a fully-typed EVENT const object, and `src/lib/analytics/events.test.ts` validating the naming convention.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-analytics-foundation/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install PostHog SDKs and declare Node.js engine constraint (FOUND-05)</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>
    Run `npm install posthog-js posthog-node` to add both packages to dependencies. After the install completes, open package.json and add an `"engines"` field at the root level (alongside `"name"`, `"version"`, etc.) with value `{ "node": "20.x" }`. This satisfies the `posthog-node@5` peer requirement (`^20.20.0 || >=22.22.0`) because Vercel's latest Node 20.x patch is above 20.20.0. Do NOT use `vercel.json` functions.runtime — that is the legacy approach; `package.json` engines is the current Vercel mechanism per FOUND-05.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); console.assert(p.engines && p.engines.node === '20.x', 'missing engines'); console.assert(p.dependencies['posthog-js'], 'missing posthog-js'); console.assert(p.dependencies['posthog-node'], 'missing posthog-node'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"engines": { "node": "20.x" }` at root level
    - `package.json` dependencies include `posthog-js` and `posthog-node`
    - `node_modules/posthog-js` and `node_modules/posthog-node` directories exist
    - `npm view posthog-node engines` output shows `{ node: '^20.20.0 || >=22.22.0' }` confirming the installed version's peer requirement
  </acceptance_criteria>
  <done>package.json has engines.node "20.x" and both posthog packages are present in dependencies and node_modules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create typed event catalog with format tests (FOUND-03)</name>
  <files>src/lib/analytics/events.ts, src/lib/analytics/events.test.ts</files>
  <read_first>src/lib/analytics/events.ts (will not exist yet), src/lib/analytics/events.test.ts (will not exist yet), src/lib/types.ts (for naming convention reference), src/lib/onboarding-progress.ts (for module export pattern reference)</read_first>
  <behavior>
    - Test 1: every value in EVENT satisfies the regex /^[a-z_]+:[a-z_]+$/ (category:object_action, lowercase, underscores allowed)
    - Test 2: EVENT has no duplicate values (all string values are unique)
    - Test 3: EventName type resolves to a union of all EVENT values (compile-time — confirmed by no TS error in test file)
    - Test 4: EVENT.ONBOARDING_STEP_COMPLETED equals the string "onboarding:step_completed"
    - Test 5: EVENT.CHECKIN_SUBMITTED equals the string "checkin:submitted"
  </behavior>
  <action>
    First create `src/lib/analytics/events.test.ts` with tests that:
    - Import EVENT and EventName from `@/lib/analytics/events`
    - Assert each value matches `/^[a-z_]+:[a-z_]+$/` via `Object.values(EVENT).forEach(...)`
    - Assert all values are unique: `new Set(Object.values(EVENT)).size === Object.values(EVENT).length`
    - Assert specific spot-check values for ONBOARDING_STEP_COMPLETED and CHECKIN_SUBMITTED
    Run `npm test -- src/lib/analytics/events.test.ts` and confirm RED (module not found).

    Then create `src/lib/analytics/events.ts` exporting:
    - `EVENT` as an `as const` object with ALL of the following keys and their string values (naming convention: category:object_action, lowercase only, underscores between words):
      - ONBOARDING_STEP_COMPLETED → "onboarding:step_completed"
      - ONBOARDING_WALKTHROUGH_COMPLETED → "onboarding:walkthrough_completed"
      - CHECKIN_SUBMITTED → "checkin:submitted"
      - CHECKIN_VERIFIED → "checkin:verified"
      - CHECKIN_GEO_FAILED → "checkin:geo_failed"
      - PACT_CREATED → "pact:created"
      - PACT_INVITE_ACCEPTED → "pact:invite_accepted"
      - PACT_INVITE_DECLINED → "pact:invite_declined"
      - PACT_MEMBER_LEFT → "pact:member_left"
      - FINANCIAL_PENALTY_ISSUED → "financial:penalty_issued"
      - FINANCIAL_SETTLEMENT_RECORDED → "financial:settlement_recorded"
      - FEATURE_TAB_VISITED → "feature:tab_visited"
      - FEATURE_NOTIFICATION_CLICKED → "feature:notification_clicked"
      - FEATURE_SHORTCUT_SETUP_VIEWED → "feature:shortcut_setup_viewed"
    - `EventName` type derived as `(typeof EVENT)[keyof typeof EVENT]`

    Use named exports. No default export. Follow kebab-case filename (already correct: events.ts).
    The directory `src/lib/analytics/` does not exist yet — create it.

    After implementation, run `npm test -- src/lib/analytics/events.test.ts` and confirm GREEN.
  </action>
  <verify>
    <automated>npm test -- src/lib/analytics/events.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/analytics/events.ts` exists with named exports EVENT and EventName
    - `src/lib/analytics/events.test.ts` exists with format-validation tests
    - `npm test -- src/lib/analytics/events.test.ts` exits 0 with all tests passing
    - `Object.values(EVENT)` in the test file produces exactly 14 entries
    - No event value string contains an uppercase letter or a space
    - TypeScript strict mode: `npx tsc --noEmit` exits 0 (no type errors from the new files)
  </acceptance_criteria>
  <done>events.ts exports EVENT const with 14 typed event-name constants; events.test.ts passes all format and value assertions in Vitest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | posthog-js and posthog-node are downloaded from npm; typosquatting or supply-chain attack possible |
| events.ts → call sites | Event names are TypeScript constants; no runtime input; no trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | npm install posthog-js, posthog-node | mitigate | Package Legitimacy Audit in RESEARCH.md confirms both packages are from github.com/PostHog org with 6M+ weekly downloads; SUS verdict resolved — approved for install |
| T-07-02 | Information Disclosure | events.ts event name strings | accept | Event names are non-sensitive product action labels; no PII; `as const` export is type-only and does not expose secrets |
| T-07-SC | Tampering | npm install | mitigate | slopcheck passed (posthog-js, posthog-node are VERIFIED in Package Legitimacy Audit); no postinstall scripts on either package |
</threat_model>

<verification>
After both tasks complete:
1. `node -e "require('./package.json').engines"` prints `{ node: '20.x' }`
2. `node -e "const p=require('./package.json'); console.log(p.dependencies['posthog-js'], p.dependencies['posthog-node'])"` prints two version strings
3. `npm test -- src/lib/analytics/events.test.ts` exits 0
4. `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- posthog-js and posthog-node installed and in package.json dependencies
- package.json engines.node set to "20.x"
- src/lib/analytics/events.ts exports EVENT (14 constants) and EventName type
- All event name values match /^[a-z_]+:[a-z_]+$/ regex
- events.test.ts passes (npm test)
</success_criteria>

## Artifacts this phase produces

| Symbol | File | Type | Description |
|--------|------|------|-------------|
| `EVENT` | `src/lib/analytics/events.ts` | `const` object | 14 typed event name constants (category:object_action) |
| `EventName` | `src/lib/analytics/events.ts` | TypeScript type | Union of all EVENT values |
| `events.test.ts` | `src/lib/analytics/events.test.ts` | Vitest test file | Format + uniqueness + spot-check tests for FOUND-03 |

<output>
Create `.planning/phases/07-analytics-foundation/07-01-SUMMARY.md` when done.
</output>
