---
phase: 04-coachmark-engine-single-route
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/app/layout.tsx
  - src/app/(tabs)/dashboard/page.tsx
autonomous: false
requirements: [TOUR-01, TOUR-02]
must_haves:
  truths:
    - "react-joyride v3.1 is installed and resolvable as an import without TypeScript errors"
    - "A #tour-root portal div exists in the DOM at the same level as other root portals, isolated from the Radix portal layer"
    - "At least one unconditionally-mounted dashboard element carries a data-tour anchor that the coachmark engine can spotlight"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "#tour-root portal target div in the root layout body"
      contains: "tour-root"
    - path: "src/app/(tabs)/dashboard/page.tsx"
      provides: "data-tour anchor on the always-mounted weekly-goal section"
      contains: "data-tour"
    - path: "package.json"
      provides: "react-joyride dependency declaration"
      contains: "react-joyride"
  key_links:
    - from: "src/app/layout.tsx"
      to: "react-joyride portalElement (Plan 03)"
      via: "#tour-root div is the documented portal target Plan 03 passes to joyride portalElement"
      pattern: "id=\"tour-root\""
---

<objective>
Lay the foundation for the coachmark engine: install the vetted rendering library (react-joyride v3.1, CONTEXT D-01), create the dedicated `#tour-root` portal element that isolates joyride from the Radix portal hierarchy (TOUR-02 z-index/portal isolation), and add at least one `data-tour` anchor to an unconditionally-mounted dashboard element so the engine can be proven end-to-end on a single route (TOUR-01).

Purpose: Plans 02 and 03 cannot run without the library present, a portal target to render into, and a real anchor to spotlight. This plan delivers all three with zero rendering behavior of its own.
Output: react-joyride in package.json/lockfile; `<div id="tour-root">` in the root layout; a `data-tour="schedule"` attribute on the dashboard "This week" weekly-goal section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-coachmark-engine-single-route/04-CONTEXT.md
@.planning/phases/04-coachmark-engine-single-route/04-UI-SPEC.md
@.planning/research/SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (04-01) produces:
- npm dependency: `react-joyride` (^3.1.0) added to `package.json` + `package-lock.json`
- DOM element: `<div id="tour-root">` in `src/app/layout.tsx` (portal target consumed by Plan 03)
- HTML attribute: `data-tour="schedule"` on the dashboard "This week" section element in `src/app/(tabs)/dashboard/page.tsx`
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify react-joyride package legitimacy before install</name>
  <action>Block the install in Task 2 until a human confirms react-joyride is the legitimate package on npmjs.com (correct name, MIT license, 3.1.x line, healthy download/maintenance signals, no typosquat). This is a [ASSUMED]-package legitimacy gate (no audit table in RESEARCH); it is never auto-approvable.</action>
  <what-built>
    Nothing yet — this is a pre-install legitimacy gate. react-joyride was vetted in
    RESEARCH (`.planning/research/SUMMARY.md` Recommended Stack: MIT, published Apr 2026,
    React 16.8–19, verified 2026-06-14) and approved in `04-UI-SPEC.md` §Registry Safety
    as a standard npm dependency under CONTEXT D-01. No `## Package Legitimacy Audit` table
    exists in RESEARCH, so per planner fallback policy react-joyride is treated as [ASSUMED]
    and requires a human legitimacy confirmation before the install runs.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/react-joyride in a browser.
    2. Confirm: license is MIT, latest version is in the 3.1.x line, weekly downloads are
       substantial (hundreds of thousands), repository links to github.com/gilbarbara/react-joyride,
       and the last publish is recent (not abandoned).
    3. Confirm the package name is exactly `react-joyride` (no typosquat lookalike such as
       `react-joyrides`, `react-joy-ride`, or `reactjoyride`).
  </how-to-verify>
  <resume-signal>Type "approved" to allow the install, or describe the concern to block it.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install react-joyride v3.1</name>
  <read_first>
    - package.json (confirm react-easy-crop is the existing ssr:false-style client-only dep precedent; confirm React 18.3.1 + Next 14.2.x versions so the install matches the React 18 peer range)
    - .planning/phases/04-coachmark-engine-single-route/04-CONTEXT.md (D-01 library decision)
    - .planning/phases/04-coachmark-engine-single-route/04-UI-SPEC.md (§Registry Safety — joyride added via package.json, NOT via shadcn add)
  </read_first>
  <action>
    Install react-joyride at the v3.1 line as a runtime dependency: run `npm install react-joyride@^3.1.0`
    (per D-01). This is a standard npm install, NOT a shadcn registry add (UI-SPEC §Registry Safety).
    Do not pin to an exact 3.1.0 if a higher 3.1.x patch is available — the caret range is intended.
    After install, confirm the package appears under `dependencies` in package.json and that
    package-lock.json updated. Do not add any other package. Do not modify React or Next versions.
  </action>
  <verify>
    <automated>node -e "require.resolve('react-joyride'); console.log('resolved')" && grep -q '"react-joyride"' package.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "require.resolve('react-joyride')"` resolves without throwing.
    - `package.json` `dependencies` contains a `react-joyride` entry with a `^3.1` range.
    - `package-lock.json` is modified (the new dependency tree is locked).
    - No package other than react-joyride (and its transitive deps) was added to `dependencies`.
  </acceptance_criteria>
  <done>react-joyride v3.1.x is installed, declared in package.json dependencies, locked, and importable.</done>
</task>

<task type="auto">
  <name>Task 3: Add #tour-root portal target and the dashboard data-tour anchor</name>
  <read_first>
    - src/app/layout.tsx (root layout — the `<body className="min-h-screen">` wraps `<InstallGate>{children}</InstallGate>`; the new portal div goes inside `<body>` as a sibling to InstallGate so it is never nested in the InstallGate/Radix subtree)
    - src/app/(tabs)/dashboard/page.tsx (the first `<section className="animate-fade-up-item shrink-0 rounded-[2rem] glass-card px-4 py-3">` — the "This week" weekly-goal card containing CheckinStrip — is rendered unconditionally in the returned tree, not behind a conditional or Suspense boundary; this is the stable anchor target)
    - .planning/phases/04-coachmark-engine-single-route/04-UI-SPEC.md (§Interaction Contract "POC anchor" row and §Color/Spacing — confirm the anchor must be on an element mounted without conditional/streamed gating, satisfying TOUR-01)
  </read_first>
  <action>
    Two edits, both additive (no behavior change, no styling change):

    (a) In src/app/layout.tsx, add a portal target div with id `tour-root` inside `<body>` as a
    direct sibling of `<InstallGate>` (place it AFTER `<InstallGate>{children}</InstallGate>` so it
    sits at the same DOM level as other root-level portals, outside the InstallGate subtree and
    outside any Radix portal). The div must be an empty, non-visual container — give it no layout
    styling that affects flow. It exists only as joyride's `portalElement` target (consumed in
    Plan 03 via `document.getElementById("tour-root")`).

    (b) In src/app/(tabs)/dashboard/page.tsx, add the attribute `data-tour="schedule"` to the
    opening tag of the first `<section>` in the returned JSX — the "This week" card
    (`className="animate-fade-up-item shrink-0 rounded-[2rem] glass-card px-4 py-3"`). Use the value
    `schedule` because that section IS the weekly-goal surface and `schedule` is the corresponding
    STEPS registry id (src/lib/onboarding/steps.ts line 55). Add ONLY the attribute — do not change
    the className, layout, or any child markup. This element renders unconditionally (it is not
    wrapped in a conditional or a Suspense boundary), satisfying the TOUR-01 "mounted, never empty
    space" requirement.

    Do not add joyride imports, overlay markup, or any rendering logic in this plan — that is Plan 03.
  </action>
  <verify>
    <automated>grep -q 'id="tour-root"' src/app/layout.tsx && grep -q 'data-tour="schedule"' "src/app/(tabs)/dashboard/page.tsx" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/layout.tsx` contains exactly one element with `id="tour-root"`, placed as a sibling of `<InstallGate>` inside `<body>` (not nested inside InstallGate).
    - `src/app/(tabs)/dashboard/page.tsx` contains `data-tour="schedule"` on the first returned `<section>` (the `glass-card px-4 py-3` "This week" card), with its className unchanged.
    - `npx tsc --noEmit` reports no new type errors.
    - No joyride import, overlay, or rendering code was added in this plan.
  </acceptance_criteria>
  <done>The #tour-root portal target exists in the root layout and a data-tour="schedule" anchor is present on the always-mounted dashboard weekly-goal section, with no rendering behavior added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | A third-party package (react-joyride) is added to the dependency tree and will execute client-side. |
| DOM portal layer | `#tour-root` is a portal target that Plan 03's joyride overlay renders into, above all app chrome. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | npm install of react-joyride | mitigate | Task 1 blocking-human legitimacy checkpoint verifies name/license/popularity on npmjs.com before install ([ASSUMED] package, no audit table in RESEARCH); caret-ranged to the vetted 3.1.x line. |
| T-04-01 | Tampering | `data-tour` anchor attribute | accept | A static `data-tour` attribute carries no user input and no privilege; it is an inert DOM hook. No PII, no auth surface. |
| T-04-02 | Information Disclosure | `#tour-root` portal div | accept | Empty static container with no data; client-only overlay target. No server data crosses it. |
</threat_model>

<verification>
- `node -e "require.resolve('react-joyride')"` resolves.
- `react-joyride` present in package.json dependencies; package-lock.json updated.
- `id="tour-root"` present in src/app/layout.tsx as an InstallGate sibling.
- `data-tour="schedule"` present on the first dashboard `<section>`.
- `npx tsc --noEmit` passes with no new errors.
- The dashboard renders identically to before (no visual/layout change from the additive attribute and the empty portal div).
</verification>

<success_criteria>
react-joyride v3.1.x is installed and importable, a `#tour-root` portal target exists at the root-layout body level isolated from the Radix/InstallGate subtree, and the dashboard's unconditionally-mounted weekly-goal section carries a `data-tour="schedule"` anchor — with no coachmark rendering behavior introduced.
</success_criteria>

<output>
Create `.planning/phases/04-coachmark-engine-single-route/04-01-SUMMARY.md` when done.
</output>
