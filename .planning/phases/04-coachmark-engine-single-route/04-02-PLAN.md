---
phase: 04-coachmark-engine-single-route
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/onboarding/coachmark-progress.ts
  - src/lib/onboarding/coachmark-progress.test.ts
  - src/components/tour/coachmark-card.tsx
autonomous: true
requirements: [TOUR-04]
must_haves:
  truths:
    - "The dot-indicator state for each of the 5 STEPS is derived purely from currentStepId and is unit-tested"
    - "CoachmarkCard renders the locked card shell: title, body, 5-dot indicator, white Next button, muted Skip link"
    - "CoachmarkCard exposes onAdvance/onDismiss callbacks and is keyboard-operable per the UI-SPEC interaction contract"
  artifacts:
    - path: "src/lib/onboarding/coachmark-progress.ts"
      provides: "Pure deriveDotStates(currentStepId) -> per-step dot state for the progress indicator"
      exports: ["deriveDotStates"]
    - path: "src/lib/onboarding/coachmark-progress.test.ts"
      provides: "Vitest coverage of dot-state derivation across current/past/future/null cases"
      contains: "deriveDotStates"
    - path: "src/components/tour/coachmark-card.tsx"
      provides: "Custom tooltipComponent card shell (title/body/dots/Next/Skip) per UI-SPEC"
      min_lines: 40
  key_links:
    - from: "src/components/tour/coachmark-card.tsx"
      to: "src/lib/onboarding/coachmark-progress.ts"
      via: "card imports deriveDotStates to render the dot indicator row"
      pattern: "deriveDotStates"
    - from: "src/components/tour/coachmark-card.tsx"
      to: "src/lib/onboarding/steps.ts"
      via: "card reads STEPS.length for the dot count"
      pattern: "STEPS"
---

<objective>
Build the coachmark card's visual shell and its testable logic, decoupled from react-joyride. The dot-indicator derivation (which of the 5 STEPS dots is current/past/future) is extracted into a pure `.ts` module with co-located Vitest (matching the Phase 3 `deriveCurrentStep` precedent), so the project's `.test.ts`-only Vitest config can cover it. `CoachmarkCard` then renders the locked card shell from `04-UI-SPEC.md` — title, body, dot row, white "Next →" button, muted "Skip tour" link — taking `onAdvance`/`onDismiss` callbacks. This is the component Plan 03 passes to joyride's `tooltipComponent` prop (CONTEXT D-02), and the slot Phase 5 will extend with action surfaces (D-05).

Purpose: The card is pure presentation with no library coupling, so it can be built and committed in parallel with the install/scaffold plan, and its dot logic is unit-proven before any rendering wiring exists.
Output: `coachmark-progress.ts` (+ test) and `coachmark-card.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-coachmark-engine-single-route/04-CONTEXT.md
@.planning/phases/04-coachmark-engine-single-route/04-UI-SPEC.md
@src/lib/onboarding/steps.ts
@src/lib/onboarding/current-step.ts
</context>

<artifacts_this_phase_produces>
This plan (04-02) produces:
- function `deriveDotStates(currentStepId: string | null)` in `src/lib/onboarding/coachmark-progress.ts` — returns an ordered array (one entry per STEPS step) of `{ id, state }` where `state` is `"current" | "past" | "future"`
- type `DotState = "current" | "past" | "future"` (exported from the same module)
- React component `CoachmarkCard` in `src/components/tour/coachmark-card.tsx` — props `{ stepId, title, body, onAdvance, onDismiss }`; renders title/body/dots/Next/Skip
- new directory `src/components/tour/`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure deriveDotStates logic with Vitest (RED then GREEN)</name>
  <read_first>
    - src/lib/onboarding/steps.ts (STEPS is a frozen 5-entry ordered array: schedule, gym, challenge, money, shortcut_viewed; deriveDotStates maps over STEPS in order so the dot order matches the registry order — D-07)
    - src/lib/onboarding/current-step.ts (the Phase-3 pure-helper precedent: a pure .ts function with co-located .test.ts; mirror its module/export/test style)
    - src/lib/onboarding/steps.test.ts (the established Vitest style in this directory — describe/it, importing from "@/lib/onboarding/...")
    - vitest.config.ts (include is "src/**/*.test.ts" ONLY — Node env, no jsdom/testing-library; this is why the dot logic is a pure .ts function, not a component test)
  </read_first>
  <behavior>
    - deriveDotStates returns one entry per STEPS step, in STEPS order, length === STEPS.length (5).
    - When currentStepId is "challenge" (index 2): schedule and gym are "past", challenge is "current", money and shortcut_viewed are "future".
    - When currentStepId is the first step "schedule": that dot is "current", all others "future", none "past".
    - When currentStepId is the last step "shortcut_viewed": all prior dots "past", last "current", none "future".
    - When currentStepId is null: every dot is "future" (no current, no past) — the inactive/no-step case.
    - When currentStepId is an id NOT in STEPS (unknown key): every dot is "future" (defensive — no throw, treat as no current step).
    - Each entry carries the step `id` so the renderer can key on it.
  </behavior>
  <action>
    Write src/lib/onboarding/coachmark-progress.test.ts FIRST (RED): cover each case in <behavior>
    above — current/past/future split for a mid step, first step, last step, the null case, and the
    unknown-id case, plus the length === STEPS.length assertion. Import `deriveDotStates` and the
    `DotState` type from "@/lib/onboarding/coachmark-progress" and `STEPS` from
    "@/lib/onboarding/steps". Run the test, confirm it fails because the module does not exist yet.

    Then write src/lib/onboarding/coachmark-progress.ts (GREEN): export `type DotState = "current" |
    "past" | "future"` and `export function deriveDotStates(currentStepId: string | null): { id:
    string; state: DotState }[]`. Implementation: find the index of currentStepId in STEPS; if not
    found (null or unknown) treat the current index as -1; map STEPS to `{ id, state }` where state is
    "current" when the index equals the current index, "past" when the index is less than the current
    index, and "future" otherwise. Keep it pure — no React, no DOM, no side effects. Re-run the test,
    confirm GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/lib/onboarding/coachmark-progress.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/onboarding/coachmark-progress.test.ts` passes with all <behavior> cases covered.
    - `deriveDotStates(null)` returns 5 entries all with state "future".
    - `deriveDotStates("challenge")` returns states [past, past, current, future, future] in STEPS order.
    - `deriveDotStates("not_a_step")` returns 5 entries all "future" without throwing.
    - The module imports nothing from React or the DOM (pure logic).
    - Two commits exist: a `test(...)` RED commit then a `feat(...)` GREEN commit.
  </acceptance_criteria>
  <done>deriveDotStates is implemented, pure, and fully unit-tested under the existing .test.ts Vitest config.</done>
</task>

<task type="auto">
  <name>Task 2: CoachmarkCard visual shell</name>
  <read_first>
    - .planning/phases/04-coachmark-engine-single-route/04-UI-SPEC.md (§Typography, §Color, §Spacing, §Copywriting Contract, §Interaction Contract "Card structure (D-05)" and "Dot indicator (D-07)" rows — these fix every visual token, copy string, and structural order; obey them exactly)
    - src/lib/onboarding/coachmark-progress.ts (deriveDotStates — the card consumes this for the dot row; created in Task 1)
    - src/lib/onboarding/steps.ts (STEPS.length is the dot count)
    - src/components/ui/button.tsx (the Button primitive default variant is bg-white text-black; reuse it for "Next →" — do NOT re-add a button via shadcn, UI-SPEC §Registry Safety)
    - src/app/globals.css (the `.glass-card` utility is the card surface per UI-SPEC §Design System; the reduced-motion media block at lines ~57 and ~324 disables `.animate-fade-up*` etc. — the card's enter animation must use one of those animate utilities so it is auto-disabled under prefers-reduced-motion)
    - src/app/(tabs)/u/[username]/avatar-cropper.tsx (existing "use client" component style + react-easy-crop usage — match the "use client" directive placement and import conventions)
  </read_first>
  <action>
    Create src/components/tour/coachmark-card.tsx as a "use client" component named `CoachmarkCard`.
    Props: `{ stepId: string | null; title: string; body: string; onAdvance: () => void; onDismiss:
    () => void }`. Render the locked structure from UI-SPEC §"Card structure (D-05)" top-to-bottom:

    1. Step TITLE — `text-base font-semibold` white (`text-foreground`/`text-white`), 16px semibold.
    2. BODY — `text-sm font-normal` white, 1-2 sentence instructional text (the `body` prop).
    3. DOT INDICATOR row — call `deriveDotStates(stepId)`, render one small round dot per entry
       (5 total) horizontally centered with `gap-1`: state "current" = solid white (`bg-white`),
       "past" = `bg-white/40`, "future" = `bg-white/15`. No "2 of 5" text (D-07). Key each dot on
       its `id`.
    4. Primary "Next →" BUTTON — reuse the shadcn `Button` default variant (renders `bg-white
       text-black`), label "Next →", min height 44px (`h-11`), `onClick` calls `onAdvance`.
    5. "Skip tour" LINK — a muted secondary text link below the button (`text-[13px]` or `text-sm`,
       `text-white/60` with `hover:text-white/85`), label "Skip tour", `onClick` calls `onDismiss`,
       padded to a 44px tap target. Render it as a real `<button>` for keyboard operability, styled
       as a link (no header X — D-06).

    Surface styling: the card root uses the `.glass-card` utility on a `w-[300px]` desktop width,
    `max-w-[calc(100vw-32px)]` and `mx-4` on mobile, `p-4` (md) inner padding, `space-y-2` title→body
    gap, `mt-3` Next→Skip gap. Apply a card enter animation via an existing animate utility
    (e.g. `animate-fade-up` / `animate-state-in`) so the reduced-motion CSS block auto-disables it.
    Use ONLY existing tokens: do NOT introduce `bg-zinc-900`/`bg-gray-900` (UI-SPEC overrides the
    CONTEXT zinc candidate to the `--card`/`.glass-card` token) and do NOT use the cyan `--c-action`
    as a coachmark color.

    Keyboard (TOUR-04, the parts the card owns): the card's two controls are real focusable buttons
    so Enter/Space on the focused "Next →" button advances natively; the global Enter/Space=advance
    and Escape=dismiss key handling and focus-on-appear are wired in Plan 03 (the renderer). The card
    must NOT trap focus (no focus-trap wrapper) — Tab moves through its two buttons and then out
    (UI-SPEC: focus is NOT trapped). Add `aria-label` text on the dot row describing position is
    optional; the step `aria-live` announcement is the renderer's job in Plan 03.

    Do NOT import react-joyride here and do NOT call useTour() — the card is a pure presentational
    component driven entirely by props. joyride/useTour wiring is Plan 03.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -v '^[[:space:]]*//' src/components/tour/coachmark-card.tsx | grep -c 'bg-zinc-\|bg-gray-9\|c-action' | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/tour/coachmark-card.tsx` starts with `"use client"` and exports `CoachmarkCard`.
    - The card renders, in order: title (`text-base font-semibold`), body (`text-sm`), a 5-dot row from `deriveDotStates`, a `Button` default-variant "Next →", and a muted "Skip tour" button-styled link.
    - `onAdvance` is wired to the "Next →" click; `onDismiss` is wired to the "Skip tour" click.
    - The card surface uses `.glass-card` (not a new gray) and the cyan `--c-action` is not used (grep for `bg-zinc-`/`bg-gray-9`/`c-action` in non-comment lines returns 0).
    - The Next button has `h-11` (44px) and the Skip link has a 44px tap area.
    - No `react-joyride` import and no `useTour` call appear in the file.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>CoachmarkCard renders the locked UI-SPEC card shell from props, consumes deriveDotStates for the dot row, reuses the Button primitive, and has no library coupling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| props → render | CoachmarkCard renders `title`/`body` strings supplied by the renderer (Plan 03), which sources placeholder copy from the step registry — no user-controlled input in Phase 4. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | CoachmarkCard `body`/`title` rendering | accept | Copy is static placeholder text from the registry (Phase 4), rendered as plain React text nodes (auto-escaped) — no `dangerouslySetInnerHTML`, no user input. Phase 5 introduces real copy, still static. |
| T-04-04 | Denial of Service | deriveDotStates with unknown stepId | mitigate | deriveDotStates treats null/unknown ids as "no current step" (all future) without throwing — a phantom stepId cannot crash the card render. |
</threat_model>

<verification>
- `npx vitest run src/lib/onboarding/coachmark-progress.test.ts` passes.
- `npx tsc --noEmit` passes.
- coachmark-card.tsx has no `react-joyride` import, no `useTour` call, no `bg-zinc-*`/`bg-gray-9*`/`c-action` usage.
- The card structure matches the UI-SPEC §"Card structure (D-05)" order and §"Dot indicator (D-07)" colors.
</verification>

<success_criteria>
The dot-indicator logic is pure and unit-tested under the existing Vitest config, and CoachmarkCard renders the exact locked card shell (title, body, 5-dot indicator, white "Next →" button, muted "Skip tour" link) from props using only existing design tokens, with zero react-joyride coupling.
</success_criteria>

<output>
Create `.planning/phases/04-coachmark-engine-single-route/04-02-SUMMARY.md` when done.
</output>
