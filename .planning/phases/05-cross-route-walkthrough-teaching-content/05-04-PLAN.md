---
phase: 05-cross-route-walkthrough-teaching-content
plan: 04
type: execute
wave: 2
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - src/components/tour/coachmark-renderer.tsx
autonomous: false
requirements: [TOUR-05, ONB-03, TEACH-01, TEACH-02, TEACH-03, TEACH-04, TEACH-05, SETUP-02, UX-04]
must_haves:
  truths:
    - "On advance, the renderer navigates to the next step's route when it differs from the current pathname, then reveals once the anchor mounts (navigate-then-reveal, dual-gated, TOUR-05)"
    - "Each surface-bearing step (schedule/gym/shortcut_viewed) embeds its real surface inline in the card; the surface onComplete advances the tour (D-01/D-03/SETUP-02/TEACH-01)"
    - "The challenge step routes to /notifications instead of /groups when pendingCount > 0, with invited-variant copy (ONB-03/D-09/D-10)"
    - "The shortcut step shows a clearly-labeled practice check-in that makes ZERO API calls and never touches /api/checkin (TEACH-05/D-05)"
    - "Per-step brand-voiced teaching copy renders for all five steps (UX-04)"
  artifacts:
    - path: "src/components/tour/coachmark-renderer.tsx"
      provides: "cross-route navigation, surface embedding, practice check-in, invited-path, teaching copy"
      contains: "usePathname"
  key_links:
    - from: "src/components/tour/coachmark-renderer.tsx"
      to: "src/lib/onboarding/steps.ts"
      via: "reads next step's route field (Plan 01) to drive router.push"
      pattern: "\\.route"
    - from: "src/components/tour/coachmark-renderer.tsx"
      to: "src/components/onboarding/gym-surface.tsx"
      via: "embeds GymSurface/ScheduleSurface/ShortcutSurface in the card surface slot with onComplete=advance"
      pattern: "GymSurface|ScheduleSurface|ShortcutSurface"
    - from: "src/components/tour/coachmark-renderer.tsx"
      to: "src/app/(tabs)/groups/page.tsx"
      via: "reads data-pending-count from the DOM for invited-path detection (D-09)"
      pattern: "data-pending-count"
---

<objective>
Extend the Phase 4 `CoachmarkRenderer` into the full cross-route teaching engine. This is the load-bearing heavy plan: it owns navigate-then-reveal sequencing (D-06/TOUR-05), inline embedding of the real setup surfaces (D-01/D-03), the invited-path route swap (D-09/D-10/ONB-03), the pure-UI practice check-in (D-05/TEACH-05), and per-step brand-voiced teaching copy (UX-04).

**This is the single highest-risk file in the phase.** The practice check-in (TEACH-05) is financial-safety-critical: it must make ZERO API calls and never touch `/api/checkin`. Verify this with an adversarial grep gate and a blocking human checkpoint.

Depends on Plan 01 (`route` field on STEPS, `surface` slot on CoachmarkCard), Plan 02 (route anchors + `data-pending-count`), and Plan 03 (`data-tour="gym"` on dashboard) all existing at runtime.

Purpose: Wire the entire walkthrough together so the four teaching points complete via real in-context actions across tabs, for both entry paths.
Output: An extended `coachmark-renderer.tsx` that navigates, embeds surfaces, simulates a safe practice check-in, branches the invited path, and renders real copy — verified by a human.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cross-route-walkthrough-teaching-content/05-CONTEXT.md
@.planning/phases/05-cross-route-walkthrough-teaching-content/05-PATTERNS.md
@.planning/phases/05-cross-route-walkthrough-teaching-content/05-UI-SPEC.md
@src/components/tour/coachmark-renderer.tsx
@src/components/tour/coachmark-card.tsx
@src/lib/onboarding/steps.ts
@src/components/onboarding/gym-surface.tsx
@src/components/onboarding/schedule-surface.tsx
@src/components/onboarding/shortcut-surface.tsx
@src/components/tour-provider.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add navigate-then-reveal cross-route sequencing + invited-path route swap (TOUR-05/ONB-03/D-06/D-09/D-10)</name>
  <files>src/components/tour/coachmark-renderer.tsx</files>
  <read_first>
    - src/components/tour/coachmark-renderer.tsx (full file — import block line 3-13; useTour destructure line 57; anchor-gate effect lines 85-95; dialog-pause effect lines 99-114; handleAdvance lines 116-118; anyDialogOpen DOM-read pattern lines 26-29; showing predicate line 127)
    - src/lib/onboarding/steps.ts (STEPS with the new route field from Plan 01; deriveCurrentStep is consumed via useTour, not imported here)
    - src/components/tour-provider.tsx (advance() recomputes currentStepId via deriveCurrentStep — lines 54-88; the renderer does NOT extend TourValue, D-08)
    - .planning/phases/05-cross-route-walkthrough-teaching-content/05-CONTEXT.md (D-06 line 45, D-07 lines 47-53, D-08 line 54, D-09 line 58, D-10 line 60, invited copy specifics line 140)
    - .planning/phases/05-cross-route-walkthrough-teaching-content/05-PATTERNS.md (lines 56-79 — hooks pattern, advance callback to extend, anchor-gate to REUSE unchanged, invited-path detection via data-pending-count)
  </read_first>
  <action>
    Add `import { useRouter, usePathname } from "next/navigation";` to the import block (the same hooks are already used in notifications/client.tsx:5). In the component, call `const router = useRouter();` and `const pathname = usePathname();`. Add a pure helper that resolves a step's EFFECTIVE route: `function effectiveRoute(stepId: string | null): string | null` — look up the step in STEPS, return `step.route ?? null`, BUT for the `challenge` step apply the invited-path swap (D-10): if a DOM read of `data-pending-count` is > 0, return `/notifications` instead of `/groups`. Read pending count with the same `document.querySelector` style as `anyDialogOpen()` (lines 26-29): `const el = document.querySelector("[data-pending-count]"); const n = el ? Number(el.getAttribute("data-pending-count")) : 0;` — guard for SSR (`typeof document === "undefined"` returns 0) and `NaN` (treat as 0). Extend `handleAdvance` (lines 116-118): after calling `advance(currentStepId)`, the provider recomputes `currentStepId` to the NEXT step on its next render. Because `advance` is async and currentStepId updates via state, drive the navigation off a `useEffect` keyed on `currentStepId`: when `currentStepId` changes and `isActive`, compute `effectiveRoute(currentStepId)`; if it is non-null and differs from `pathname`, call `router.push(thatRoute)`. The existing anchor-gate `MutationObserver` (lines 85-95) then naturally fires when the new page mounts the anchor — do NOT add a second observer or any polling (PATTERNS line 74). Guard against redundant pushes (only push when route differs from `pathname`) so re-renders on the same route do not loop. Do NOT extend `TourValue` (D-08) — all navigation lives in this client component. Do NOT bump TOUR_VERSION.
  </action>
  <verify>
    <automated>cd "D:/Code Projects/Gym Tracker" && npx tsc --noEmit -p tsconfig.json && grep -c 'usePathname\|useRouter\|data-pending-count' src/components/tour/coachmark-renderer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'usePathname\|useRouter' src/components/tour/coachmark-renderer.tsx` shows both hooks imported and called
    - `grep -n 'router.push' src/components/tour/coachmark-renderer.tsx` shows navigation guarded by a route-differs-from-pathname check
    - `grep -n 'data-pending-count' src/components/tour/coachmark-renderer.tsx` shows the DOM read for invited-path detection
    - Behavior assertion: `effectiveRoute("challenge")` returns `/notifications` when a `[data-pending-count]` element reports > 0, else `/groups`
    - Source assertion: only ONE `new MutationObserver` for anchor-readiness exists (the Phase-4 one at lines 85-95 is reused — no second navigation observer added); `grep -c 'new MutationObserver' src/components/tour/coachmark-renderer.tsx` is unchanged at 2 (anchor gate + dialog pause)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>The renderer navigates to the next step's effective route on advance (pushing only when it differs from the current pathname), swaps the challenge step to /notifications when pendingCount>0, reveals via the reused anchor-gate observer, and does not extend TourValue or add a second observer; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Embed real surfaces inline + render per-step brand-voiced teaching copy (D-01/D-03/SETUP-02/TEACH-01..04/UX-04)</name>
  <files>src/components/tour/coachmark-renderer.tsx</files>
  <read_first>
    - src/components/tour/coachmark-renderer.tsx (TooltipAdapter lines 185-207 — where CoachmarkCard is rendered with PLACEHOLDER_BODY; stepTitle useMemo lines 152-155; PLACEHOLDER_BODY const lines 19-20; handleAdvance/handleDismiss)
    - src/components/tour/coachmark-card.tsx (the surface prop added in Plan 01; CoachmarkCard signature)
    - src/components/onboarding/gym-surface.tsx (GymSurface({ initialGymCount, onComplete }) — line 14-20)
    - src/components/onboarding/schedule-surface.tsx (ScheduleSurface({ initialGoal, initialRestDays, onComplete }) — line 17-25)
    - src/components/onboarding/shortcut-surface.tsx (ShortcutSurface({ onComplete }) — line 6; ALREADY writes complete_step:"shortcut_viewed" at line 19 — do NOT double-write)
    - .planning/phases/05-cross-route-walkthrough-teaching-content/05-UI-SPEC.md (Copywriting per-step table lines 103-110 — exact title + body per step incl. self-starter vs invited challenge; standard contract elements lines 114-124)
    - .planning/phases/05-cross-route-walkthrough-teaching-content/05-CONTEXT.md (D-01 surfaces inline line 31, D-03 onComplete=advance line 35, challenge/money teaching-only line 16; teaching copy discretion lines 67)
    - .planning/phases/05-cross-route-walkthrough-teaching-content/05-PATTERNS.md (lines 184-195 — surface onComplete→advance wiring; do-not-double-write note for shortcut)
  </read_first>
  <action>
    Replace the single `PLACEHOLDER_BODY` usage with real per-step teaching copy. Define a `STEP_COPY` map (a `Record<string, { title: string; body: string }>` or a small lookup) carrying the exact UI-SPEC copy (lines 103-110): `schedule` → title "Set your weekly goal", body "How many days a week are you showing up? This is the bar you and your partner are held to."; `gym` → "Pick your gym" / "We verify check-ins by location. Set the gym you actually train at — no gym, no proof."; `challenge` (self-starter) → "Start a pact" / "Challenge your gym partner with real money on the line. Skip a day you owe — show up and you don't."; `challenge` (invited, pendingCount>0) → "Your partner challenged you" / "This is where you respond. Accept the pact to put real money on the line — or decline."; `money` → "This is the scoreboard that matters" / "Not streaks — money. What you've earned, what you owe, and how it settles every week."; `shortcut_viewed` → "Check in from your phone" / "iOS users: one tap via the Shortcut. Everyone else: manual check-in works the same. Try a practice run below — it won't count." Resolve the challenge variant at render time using the same `data-pending-count` DOM read from Task 1 (pendingCount>0 → invited copy). For the title, prefer this copy over the registry `stepTitle` (the registry titles are terse internal labels; UI-SPEC copy is the brand-voiced surface). In `TooltipAdapter`, build a `surfaceNode` for the current step: for `schedule` render `<ScheduleSurface initialGoal={...} initialRestDays={[]} onComplete={handleAdvance} />`; for `gym` render `<GymSurface initialGymCount={0} onComplete={handleAdvance} />`; for `shortcut_viewed` render the practice-check-in UI built in Task 3 (NOT ShortcutSurface's network call as the only path — see Task 3 for the safe pattern). Pass Phase-3-style neutral initial values (initialGymCount=0, initialRestDays=[], initialGoal a sensible default like 4) since auto-skip-from-real-state is deferred to Phase 6 (CONTEXT deferred line 150). For `challenge` and `money` (teaching-only, no surface), pass NO surface — the card keeps its "Next →" button (D-03). Pass `surface={surfaceNode}` to `CoachmarkCard` only for surface-bearing steps; when surface is present the card hides "Next →" (Plan 01). Wire each surface's `onComplete` to `handleAdvance` (which advances and triggers navigation from Task 1) per D-03/PATTERNS line 187. IMPORTANT: ShortcutSurface ALREADY writes `complete_step:"shortcut_viewed"` (shortcut-surface.tsx:19) — for the shortcut step the practice check-in (Task 3) is the embedded surface, and it advances WITHOUT a duplicate write; do not also mount ShortcutSurface for that step's advance unless its single write is intended. Keep the safe-area wrapper (lines 188-196) and aria-live announcement (lines 280-294), updating the announcement to use the real per-step title+body.
  </action>
  <verify>
    <automated>cd "D:/Code Projects/Gym Tracker" && npx tsc --noEmit -p tsconfig.json && grep -c 'GymSurface\|ScheduleSurface\|This is the scoreboard that matters' src/components/tour/coachmark-renderer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'GymSurface' src/components/tour/coachmark-renderer.tsx` and `grep -n 'ScheduleSurface' src/components/tour/coachmark-renderer.tsx` show both embedded with `onComplete={handleAdvance}`
    - Source assertion: the five UI-SPEC step bodies are present (e.g. `grep -c 'no gym, no proof'` and `grep -c 'how it settles every week'` each return 1)
    - Behavior assertion: the challenge step renders invited copy ("Your partner challenged you") when pendingCount>0 and self-starter copy ("Start a pact") otherwise
    - Behavior assertion: challenge and money steps pass NO surface to CoachmarkCard (they keep "Next →"); schedule/gym/shortcut_viewed pass a surface
    - Source assertion: PLACEHOLDER_BODY is no longer the rendered body (`grep -c 'Real lessons land in the next phase' src/components/tour/coachmark-renderer.tsx` returns 0)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Each surface-bearing step embeds its real surface inline with onComplete=handleAdvance; challenge/money stay teaching-only with Next; all five steps render the exact brand-voiced UI-SPEC copy with the invited-variant swap; the placeholder body is gone; tsc passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build the practice check-in UI — pure simulation, ZERO API calls, never touches the check-in endpoint (TEACH-05/D-05)</name>
  <files>src/components/tour/coachmark-renderer.tsx</files>
  <read_first>
    - src/components/tour/coachmark-renderer.tsx (reducedMotion state lines 67-80; handleAdvance; the shortcut_viewed surface slot from Task 2)
    - src/components/onboarding/shortcut-surface.tsx (the network-writing pattern this practice UI deliberately AVOIDS — it calls /api/onboarding-progress at line 16)
    - .planning/phases/05-cross-route-walkthrough-teaching-content/05-CONTEXT.md (D-05 lines 40-41 — pure UI simulation, zero contact with /api/checkin; practice animation specifics line 139)
    - .planning/phases/05-cross-route-walkthrough-teaching-content/05-UI-SPEC.md (Copywriting practice button line 117 "Practice check-in" + sub-label "Practice only — never counts toward stakes."; Motion practice pulse ≤400ms reduced-motion-safe line 164; Color success pulse line 88)
  </read_first>
  <action>
    Build the practice check-in as a small inline client UI rendered as the `shortcut_viewed` step's embedded surface (the `surfaceNode` for that step, per Task 2). It is a clearly-labeled button reading "Practice check-in" with a sub-label "Practice only — never counts toward stakes." On click: set a local `simulating` state true, run a brief success animation/pulse (use tailwindcss-animate utilities, e.g. a success-colored checkmark pulse; cap at ~400ms via a `setTimeout`), then call `handleAdvance()` to advance the tour. Under reduced motion (the existing `reducedMotion` state, lines 67-80), skip the delay/animation and advance immediately (or near-immediately). **CRITICAL SAFETY (D-05/TEACH-05):** this UI makes ZERO network calls — NO `fetch`, NO call to the check-in endpoint, NO call to the onboarding-progress endpoint, NO submission_id, NO geo read. It is cosmetic only. The only side effect is `handleAdvance()` (which advances the tour and, via TourProvider, writes `complete_step:"shortcut_viewed"` through the existing best-effort onboarding PATCH — that is the tour-completion write, NOT a check-in). Do NOT import or reference any check-in endpoint, the checkin-strip, or TodayActionCard logic. Implement the practice UI either as a small inline component inside coachmark-renderer.tsx or a tiny local function component in the same file — keep it co-located so the no-network-call guarantee is auditable in one file. Add a copy sub-label and a "Skip tour" path remains available via the card's always-present Skip tour link (Plan 01). Add an HTML comment near the button (referring to the check-in path by concept, not by literal endpoint string) documenting that this control is cosmetic and never contacts the check-in API (so the safety intent is legible at the source).
  </action>
  <verify>
    <automated>cd "D:/Code Projects/Gym Tracker" && npx tsc --noEmit -p tsconfig.json && test $(grep -v '^//' src/components/tour/coachmark-renderer.tsx | grep -c 'api/checkin') -eq 0 && echo "SAFE: no /api/checkin call"</automated>
  </verify>
  <acceptance_criteria>
    - HARD SAFETY GATE: `grep -v '^//' src/components/tour/coachmark-renderer.tsx | grep -c 'api/checkin'` returns 0 — the renderer makes no reference to the check-in endpoint in executable code
    - Behavior assertion: clicking "Practice check-in" advances the tour (handleAdvance fires) after a ≤400ms pulse with NO network request issued (verify in the human checkpoint via browser devtools Network tab — zero requests to /api/checkin on click)
    - Source assertion: `grep -n 'Practice check-in' src/components/tour/coachmark-renderer.tsx` shows the labeled button; the sub-label "Practice only — never counts toward stakes." is present
    - Source assertion: the practice UI contains no `fetch(` of its own (the only writes in this file remain those the tour already makes via useTour/handleAdvance, which route through TourProvider's onboarding-progress PATCH — not a check-in)
    - Reduced-motion: when `reducedMotion` is true the pulse is skipped / instant
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>The shortcut step embeds a clearly-labeled "Practice check-in" UI that runs a ≤400ms reduced-motion-safe success pulse then advances the tour, makes ZERO API calls (no /api/checkin reference anywhere in executable code), and is co-located in the renderer for one-file auditability; tsc passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify the full cross-route walkthrough and the practice check-in safety guarantee</name>
  <action>Pause execution for a human to run the app and verify the end-to-end walkthrough across both entry paths AND confirm the financial-safety guarantee (TEACH-05) that automated greps cannot fully prove: that clicking the practice check-in issues zero network requests to the check-in endpoint and creates no real check-in.</action>
  <what-built>
    The full cross-route walkthrough: navigate-then-reveal across tabs (TOUR-05), inline-embedded gym/schedule/shortcut surfaces (D-01/SETUP-02/TEACH-01), invited-path challenge routing to /notifications (ONB-03), the pure-UI practice check-in (TEACH-05), and brand-voiced per-step copy (UX-04). Plus the dashboard checklist + empty-state CTA (Plan 03) and route anchors (Plan 02).
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`. Sign in as a fresh user (username-only start) so the tour is active from the schedule step.
    2. SAFETY (TEACH-05) — most important: open browser DevTools → Network tab, filter to "checkin". Advance the tour to the shortcut step, click "Practice check-in". CONFIRM: zero requests to `/api/checkin` are issued; the button shows a brief pulse then the tour advances. There must be NO real check-in created (verify your dashboard check-in state is unchanged).
    3. CROSS-ROUTE (TOUR-05): advance through schedule → gym (both on /dashboard) → challenge (navigates to /groups) → money (/groups) → shortcut (/shortcut). CONFIRM the coachmark only appears AFTER each route's anchor mounts — it never spotlights empty space mid-navigation.
    4. SURFACES (D-01/SETUP-02): on the schedule step the ScheduleSurface (goal picker + rest days) renders inside the card and scrolls internally if tall; setting a goal + Continue advances. On the gym step the GymSurface (search box) renders inside the card; the card never overflows the viewport (respects safe-area). The card has NO standalone "Next →" on surface steps; "Skip tour" is always present.
    5. INVITED PATH (ONB-03): with a user who HAS a pending challenge invite (pendingCount > 0 on /groups), CONFIRM the challenge step routes to /notifications and shows invited copy "Your partner challenged you", anchored on the first pending invite card. A self-starter (no pending invites) stays on /groups with "Start a pact" copy.
    6. COPY (UX-04): each step shows the brand-voiced copy from the UI-SPEC (consequence-first, "stakes not stats") — no placeholder text.
    7. CHECKLIST + CTA (UX-01/UX-02): the dashboard "Getting started" checklist shows checkmarks as teaching keys complete and disappears when all four are done; a user with no challenges sees the "Start your first pact" empty-state CTA.
    8. ACCESSIBILITY (carried TOUR-04): keyboard advance/skip still works; reduced-motion (OS setting) makes the practice pulse instant.
  </how-to-verify>
  <resume-signal>Type "approved" once the practice check-in is confirmed to make zero /api/checkin requests and the cross-route flow + both entry paths work, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| practice check-in (client) → check-in API | THE critical boundary: the practice UI must NEVER cross it. Zero contact with /api/checkin or any financial-critical table (D-05/TEACH-05) |
| renderer (client) → onboarding-progress PATCH | the tour's advance() writes `complete_step` via TourProvider's best-effort PATCH — owner-scoped, server-authoritative dedupe (Phase 1 D-04); the renderer does not write directly |
| /groups DOM (data-pending-count) → renderer | the renderer reads a non-sensitive integer the page already renders; used only to choose a route |
| embedded surfaces → existing endpoints | GymSurface/ScheduleSurface hit pre-existing /api/gyms, /api/profile, /api/places/* (Phase 2 vetted, SETUP-01) — no new endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-CHECKIN | Tampering / EoP | practice check-in UI | mitigate (HIGH severity) | HARD GATE: the practice UI makes ZERO API calls; automated grep asserts 0 `api/checkin` references in executable code; blocking human checkpoint confirms zero network requests on click. A practice run can never forge a verified check-in, alter stakes, penalties, or stats (D-05/TEACH-05) |
| T-05-04-01 | Tampering | invited-path route swap via data-pending-count | mitigate | the DOM read only CHOOSES a route (/groups vs /notifications); it grants no authority. The accept/decline write still goes through the existing RLS-scoped `respond()` → /api/challenges/respond; a spoofed data-pending-count merely points the coachmark at a page the user already has access to |
| T-05-04-02 | EoP | tour advance() write path | mitigate | the renderer does not write progress directly — it calls useTour().advance() which PATCHes only `complete_step`+`last_step_id` (never a full completed_steps array, Phase 1 D-04); the server dedupe-appends authoritatively |
| T-05-04-03 | DoS | navigation loop on re-render | mitigate | `router.push` is guarded by a route-differs-from-pathname check so same-route re-renders do not loop; the reused anchor-gate observer disconnects on cleanup |
| T-05-04-04 | Tampering | embedded surfaces | mitigate | surfaces are the Phase-2 vetted components hitting only pre-existing endpoints (SETUP-01); the renderer adds no new fetch except none — it only passes onComplete=handleAdvance |
| T-05-04-SC | — | package installs | accept | N/A — no package installs (react-joyride gated in Phase 4 Plan 04-01; tailwindcss-animate already present) |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` — renderer typechecks with hooks, surfaces, copy, practice UI
- HARD SAFETY GATE: `grep -v '^//' src/components/tour/coachmark-renderer.tsx | grep -c 'api/checkin'` returns 0
- `grep -n 'usePathname\|useRouter\|data-pending-count' src/components/tour/coachmark-renderer.tsx` — navigation + invited-path wiring present
- `grep -n 'GymSurface\|ScheduleSurface' src/components/tour/coachmark-renderer.tsx` — surfaces embedded with onComplete=handleAdvance
- `grep -c 'new MutationObserver' src/components/tour/coachmark-renderer.tsx` returns 2 (no second observer added)
- Blocking human checkpoint confirms zero /api/checkin requests on practice click + cross-route + both entry paths
</verification>

<success_criteria>
- Navigate-then-reveal sequences across tabs, dual-gated on pathname + anchor (TOUR-05)
- Real gym/schedule surfaces embed inline and advance on their own Continue (D-01/D-03/SETUP-02/TEACH-01)
- Challenge step swaps to /notifications with invited copy when pendingCount>0 (ONB-03/D-09/D-10)
- Practice check-in makes ZERO API calls, never touches /api/checkin (TEACH-05/D-05) — verified by grep gate + human checkpoint
- All five steps render brand-voiced UI-SPEC copy (UX-04); TourValue not extended (D-08); TOUR_VERSION not bumped
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)

- `effectiveRoute(stepId)` — pure helper in coachmark-renderer.tsx resolving a step's route with the invited-path swap (challenge → /notifications when pendingCount>0)
- `STEP_COPY` — per-step brand-voiced title/body lookup in coachmark-renderer.tsx (5 steps + invited variant)
- Practice check-in inline UI — cosmetic-only "Practice check-in" control in coachmark-renderer.tsx (zero API calls)
- `usePathname`/`useRouter` wiring + navigation effect keyed on currentStepId in coachmark-renderer.tsx
- Surface embedding: GymSurface/ScheduleSurface mounted in the card surface slot with onComplete=handleAdvance
</artifacts_produced>

<output>
Create `.planning/phases/05-cross-route-walkthrough-teaching-content/05-04-SUMMARY.md` when done
</output>