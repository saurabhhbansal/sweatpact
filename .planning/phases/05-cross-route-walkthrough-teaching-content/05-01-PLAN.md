---
phase: 05-cross-route-walkthrough-teaching-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/onboarding/steps.ts
  - src/lib/onboarding/steps.test.ts
  - src/components/tour/coachmark-card.tsx
autonomous: true
requirements: [TOUR-05, TEACH-01, SETUP-02, UX-04]
must_haves:
  truths:
    - "Each STEPS entry carries a route field pointing at the tab route where its anchor lives"
    - "Adding the route field does NOT change TOUR_VERSION (no add/remove/reorder/rename)"
    - "CoachmarkCard renders an optional embedded surface in a bounded-scroll slot between body and buttons"
    - "When a surface is embedded, the standalone Next button is hidden; Skip tour always remains"
  artifacts:
    - path: "src/lib/onboarding/steps.ts"
      provides: "route field on OnboardingStep + populated STEPS registry"
      contains: "route?: string"
    - path: "src/lib/onboarding/steps.test.ts"
      provides: "route-field coverage + TOUR_VERSION unchanged assertion"
      contains: "route"
    - path: "src/components/tour/coachmark-card.tsx"
      provides: "surface slot + conditional Next button"
      contains: "surface"
  key_links:
    - from: "src/lib/onboarding/steps.ts"
      to: "src/lib/onboarding-progress.ts"
      via: "STEP_KEY_REGEX import — every id stays a legal complete_step key"
      pattern: "STEP_KEY_REGEX"
---

<objective>
Land the two foundation primitives the cross-route renderer (Plan 04) depends on: (1) add an optional `route?: string` field to the `OnboardingStep` type and populate it for all five steps per D-06/D-07, and (2) extend the locked Phase 4 `CoachmarkCard` with an optional inline `surface` slot (D-01/D-02) that hides the standalone "Next →" button when present (D-03).

Both are pure/foundation changes with zero file overlap with Plans 02/03. They unblock the renderer's navigate-then-reveal logic and inline-surface embedding.

Purpose: Phase 5 navigation reads each step's `route` from the registry; the card must be able to host an embedded setup surface. These two contracts must exist before the renderer can wire them.
Output: Extended `OnboardingStep` type + populated `STEPS`, updated co-located Vitest, and an extended `CoachmarkCard` with a `surface` prop.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cross-route-walkthrough-teaching-content/05-CONTEXT.md
@.planning/phases/05-cross-route-walkthrough-teaching-content/05-PATTERNS.md
@.planning/phases/05-cross-route-walkthrough-teaching-content/05-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add route field to OnboardingStep type and populate STEPS (D-06/D-07)</name>
  <files>src/lib/onboarding/steps.ts, src/lib/onboarding/steps.test.ts</files>
  <read_first>
    - src/lib/onboarding/steps.ts (current type at lines 37-42, registry at lines 54-60, TOUR_VERSION at line 13, TEACHING_KEYS at line 68 — read all of it)
    - src/lib/onboarding/steps.test.ts (existing assertions — the new test extends these; the "five step ids in exact order" test at lines 17-25 and the frozen-registry test at lines 55-60 MUST still pass unchanged)
    - .planning/phases/05-cross-route-walkthrough-teaching-content/05-CONTEXT.md (D-06 line 45, D-07 lines 47-53)
    - .planning/phases/05-cross-route-walkthrough-teaching-content/05-PATTERNS.md (lines 26-52 — type extension + registry population + TOUR_VERSION constraint)
  </read_first>
  <behavior>
    - Test: every step in STEPS has a route that is a non-empty string starting with "/"
    - Test: route mapping equals schedule→/dashboard, gym→/dashboard, challenge→/groups, money→/groups, shortcut_viewed→/shortcut (per D-07)
    - Test: TOUR_VERSION is still exactly 1 (adding a field is not an add/remove/reorder/rename — no bump)
    - Test: the existing "five step ids in exact order" assertion still passes
    - Test: STEPS is still frozen and a push still throws
  </behavior>
  <action>
    In src/lib/onboarding/steps.ts, add one optional field `route?: string` to the `OnboardingStep` type (after `probe?: ProbeId;`), documented with the same JSDoc-comment style already used for `surface`/`probe`, noting it is the tab route where this step's `data-tour` anchor lives (D-06). Populate the `STEPS` array per the D-07 route mapping: `schedule` → `/dashboard`, `gym` → `/dashboard`, `challenge` → `/groups`, `money` → `/groups`, `shortcut_viewed` → `/shortcut`. Keep `Object.freeze([...] as const)`, keep every existing `id`/`title`/`surface`/`probe` value byte-identical, keep the array order identical. Do NOT change `TOUR_VERSION` (still 1) — adding an optional field does not change the ordered set or identity of step ids, so the bump rule in the TOUR_VERSION JSDoc does not fire. Do NOT touch `TEACHING_KEYS`. Add Vitest cases to steps.test.ts: one asserting the full route mapping object, one asserting every route is a non-empty string beginning with "/", and one re-asserting `TOUR_VERSION === 1`. The challenge step's invited-variant route swap to `/notifications` is resolved at RUNTIME in the renderer (D-09/D-10) — the registry route for `challenge` stays `/groups`; do not add a second route field.
  </action>
  <verify>
    <automated>cd "D:/Code Projects/Gym Tracker" && npx vitest run src/lib/onboarding/steps.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/onboarding/steps.test.ts` passes with the new route-mapping, route-format, and TOUR_VERSION cases green
    - Source assertion: `grep -n 'route' src/lib/onboarding/steps.ts` shows `route?: string` in the type and a `route:` on each of the five STEPS entries
    - Source assertion: `grep -c 'TOUR_VERSION = 1' src/lib/onboarding/steps.ts` returns 1 (version unchanged)
    - The pre-existing "lists the five step ids in exact order" and "cannot be mutated by callers" tests still pass (run shows no regressions)
  </acceptance_criteria>
  <done>OnboardingStep has an optional route field; all five STEPS carry the D-07 route; TOUR_VERSION is still 1; co-located tests cover route mapping/format and version stability and pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add optional surface slot to CoachmarkCard, hide Next when present (D-01/D-02/D-03)</name>
  <files>src/components/tour/coachmark-card.tsx</files>
  <read_first>
    - src/components/tour/coachmark-card.tsx (full file — props type lines 18-24, body p at line 62, dot row at lines 65-80, primary Next Button at lines 83-85, Skip tour button at lines 88-94, card root width at line 55)
    - .planning/phases/05-cross-route-walkthrough-teaching-content/05-CONTEXT.md (D-01 line 31, D-02 line 33, D-03 line 35, code_context line 130)
    - .planning/phases/05-cross-route-walkthrough-teaching-content/05-PATTERNS.md (lines 82-107 — props extension, surface slot render, conditional Next, style tokens)
    - .planning/phases/05-cross-route-walkthrough-teaching-content/05-UI-SPEC.md (Spacing card sizing line 54; Component Inventory CoachmarkCard row line 148; Copywriting "no standalone Next on surface steps" line 116)
  </read_first>
  <action>
    Extend `CoachmarkCardProps` with an optional `surface?: React.ReactNode` field (document it: embedded setup surface per D-01; when present the standalone Next is hidden per D-03). Render the surface in a bounded-scroll container inserted BETWEEN the body `<p>` (line 62) and the dot-indicator row (line 65): wrap it in a `div` with classes `mt-3 max-h-[calc(80vh-8rem)] overflow-y-auto`, rendered only when `surface` is truthy (`{surface ? (...) : null}`), per D-02. Make the existing primary "Next →" `Button` (lines 83-85) conditional: render it ONLY when `surface` is NOT provided (teaching-only steps keep "Next →"; surface steps rely on the surface's own Continue/Skip-for-now CTA per D-03). The "Skip tour" button (lines 88-94) ALWAYS renders unchanged (ONB-04). When a surface is present, widen the card root from `w-[300px]` to a bounded wider width that still respects the viewport — use `w-[360px] max-w-[calc(100vw-32px)]` when `surface` is set and keep `w-[300px] max-w-[calc(100vw-32px)]` otherwise (gym/schedule surfaces have search inputs and result lists that need the extra width). Use `cn()` from `@/lib/utils` (already imported) for the conditional width. Keep `glass-card animate-fade-up rounded-2xl p-4 text-white` and the `mx-4` margin. Do NOT add any tour-library or tour-context import — the card stays pure-presentation, prop-driven (it imports only `Button`, `deriveDotStates`, `cn`). Do NOT change the dot-indicator logic.
  </action>
  <verify>
    <automated>cd "D:/Code Projects/Gym Tracker" && npx tsc --noEmit -p tsconfig.json && npx vitest run src/lib/onboarding/coachmark-progress.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes (the new `surface?: React.ReactNode` prop typechecks; no untyped JSX)
    - Source assertion: `grep -n 'surface' src/components/tour/coachmark-card.tsx` shows `surface?: React.ReactNode` in the props type and a conditional render of `surface`
    - Source assertion: `grep -n 'max-h-\[calc(80vh-8rem)\]' src/components/tour/coachmark-card.tsx` returns the surface-slot scroll container
    - Behavior assertion: the "Next →" Button is wrapped in a `surface ? null :`-style condition (rendered only when surface is absent); "Skip tour" has no such condition and always renders
    - Source assertion: `grep -c 'react-joyride\|useTour\|tour-provider' src/components/tour/coachmark-card.tsx | grep -v '^#'` returns 0 (card imports no tour library/context — purity preserved)
  </acceptance_criteria>
  <done>CoachmarkCard accepts an optional surface node rendered in an 80vh-bounded scroll slot between body and dots; the standalone Next button is hidden when a surface is present and shown otherwise; Skip tour always renders; the card remains a pure prop-driven component; tsc passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → onboarding-progress PATCH | step ids feed `complete_step`; an out-of-registry `route` could mislead navigation but never reaches the server (route is client-only metadata) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | `STEPS` registry (steps.ts) | mitigate | `Object.freeze([...] as const)` retained; existing frozen-registry test (steps.test.ts:55-60) re-runs green so the route field cannot be mutated by callers |
| T-05-01-02 | EoP (logic) | `OnboardingStep.id` ↔ `STEP_KEY_REGEX` | mitigate | adding `route` does not alter ids; the existing per-id `STEP_KEY_REGEX` assertion (steps.test.ts:27-31) still passes so no step id can become an illegal `complete_step` key |
| T-05-01-03 | Tampering | `CoachmarkCard` purity | mitigate | card takes no tour-library/context import; acceptance grep asserts 0 joyride/useTour imports so the presentation layer cannot be a write path |
| T-05-01-SC | — | package installs | accept | N/A — no package installs in this plan (react-joyride was gated in Phase 4 Plan 04-01) |
</threat_model>

<verification>
- `npx vitest run src/lib/onboarding/steps.test.ts` — route mapping/format + TOUR_VERSION stability green
- `npx tsc --noEmit -p tsconfig.json` — surface prop typechecks, no regressions
- `grep -n 'route' src/lib/onboarding/steps.ts` — route field present in type + all five entries
- `grep -n 'surface' src/components/tour/coachmark-card.tsx` — surface prop + conditional render present
</verification>

<success_criteria>
- OnboardingStep has `route?: string`; all five STEPS carry their D-07 route; TOUR_VERSION is still 1
- CoachmarkCard hosts an optional bounded-scroll surface slot and hides Next when a surface is present
- CoachmarkCard remains pure presentation (no tour import); tsc and steps tests pass
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

- `OnboardingStep.route?: string` — new optional field on the step type (src/lib/onboarding/steps.ts)
- `STEPS[].route` — populated route per step: schedule/gym → `/dashboard`, challenge/money → `/groups`, shortcut_viewed → `/shortcut`
- `CoachmarkCardProps.surface?: React.ReactNode` — new optional prop on the card (src/components/tour/coachmark-card.tsx)
- Surface-slot render container class `max-h-[calc(80vh-8rem)] overflow-y-auto` inside CoachmarkCard
- Conditional card width token `w-[360px]` (surface present) vs `w-[300px]` (no surface)
</artifacts_produced>

<output>
Create `.planning/phases/05-cross-route-walkthrough-teaching-content/05-01-SUMMARY.md` when done
</output>
