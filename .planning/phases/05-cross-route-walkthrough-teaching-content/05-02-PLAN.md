---
phase: 05-cross-route-walkthrough-teaching-content
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(tabs)/groups/page.tsx
  - src/app/(tabs)/notifications/client.tsx
  - src/app/(tabs)/shortcut/page.tsx
autonomous: true
requirements: [TOUR-05, ONB-03, TEACH-02, TEACH-03, TEACH-04]
must_haves:
  truths:
    - "The groups page exposes data-tour=challenge and data-tour=money on unconditionally-mounted elements"
    - "The groups page exposes the live pending-invite count as a data-pending-count attribute the renderer can read without a fetch"
    - "The notifications first pending invite card carries data-tour=challenge (invited variant)"
    - "The shortcut page exposes data-tour=shortcut_viewed on an always-mounted element"
  artifacts:
    - path: "src/app/(tabs)/groups/page.tsx"
      provides: "challenge + money anchors and data-pending-count"
      contains: "data-pending-count"
    - path: "src/app/(tabs)/notifications/client.tsx"
      provides: "invited-variant challenge anchor on first pending invite"
      contains: "data-tour"
    - path: "src/app/(tabs)/shortcut/page.tsx"
      provides: "shortcut_viewed anchor"
      contains: "data-tour=\"shortcut_viewed\""
  key_links:
    - from: "src/app/(tabs)/groups/page.tsx"
      to: "src/components/tour/coachmark-renderer.tsx"
      via: "data-pending-count attribute read by the renderer for D-09 invited-path detection"
      pattern: "data-pending-count"
---

<objective>
Add the cross-route `data-tour` anchors the walkthrough navigates to, and expose the live pending-invite count for invited-path detection. This is the DOM-attribute half of the cross-route walkthrough (TOUR-05): the renderer (Plan 04) navigates to each step's route and reveals the coachmark once these anchors mount.

Anchors added: `data-tour="challenge"` + `data-tour="money"` + `data-pending-count={pendingCount}` on `/groups`; `data-tour="challenge"` on the first pending invite card on `/notifications` (invited variant, ONB-03); `data-tour="shortcut_viewed"` on `/shortcut`. The `data-tour="gym"` anchor on `/dashboard` is owned by Plan 03 (it shares the dashboard file with the checklist/CTA).

Purpose: The renderer's dual-gate (pathname match + anchor present) and invited-path route swap cannot function until these anchors and the pending-count signal exist in the DOM.
Output: Three route files carrying unconditionally-mounted anchors and the pending-count attribute.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cross-route-walkthrough-teaching-content/05-CONTEXT.md
@.planning/phases/05-cross-route-walkthrough-teaching-content/05-PATTERNS.md
@.planning/phases/05-cross-route-walkthrough-teaching-content/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add challenge + money anchors and data-pending-count to /groups</name>
  <files>src/app/(tabs)/groups/page.tsx</files>
  <read_first>
    - src/app/(tabs)/groups/page.tsx (full file — pendingCount derived at line 128; main element at line 140; new-challenge search section at lines 216-224; conditional empty-state card at lines 162-171; ChallengeVersusCard at lines 201-209)
    - .planning/phases/05-cross-route-walkthrough-teaching-content/05-CONTEXT.md (D-07 line 50-51 route mapping, D-09 line 58 data-pending-count, Claude's Discretion line 64-65, money teaching specifics line 141)
    - .planning/phases/05-cross-route-walkthrough-teaching-content/05-PATTERNS.md (lines 125-128 — exact anchor placement guidance for groups)
  </read_first>
  <action>
    On the always-mounted `<main>` element (line 140), add `data-pending-count={pendingCount}` (pendingCount is already derived at line 128) so the renderer can read invited-path state without a fetch (D-09). Add `data-tour="challenge"` to the new-challenge search `<section>` (lines 216-224) — it is unconditionally mounted, unlike the empty-state card at 162-171 (which is conditional on `activeMemberships.length === 0`); do NOT anchor the conditional card. Add `data-tour="money"` to an unconditionally-mounted element that represents the financial standing: per-challenge standing in `ChallengeVersusCard` may be absent when there are no challenges, so anchor `data-tour="money"` to the same always-mounted region the copy can teach against — attach it to the new-challenge `<section>`'s parent or, if challenge and money must point at distinct elements, add `data-tour="money"` to the `<main>` wrapper alongside `data-pending-count` (the money step's coachmark then anchors to the page region and the teaching copy carries the lesson per specifics line 141). Choose distinct elements where both are unconditionally mounted: `data-tour="challenge"` on the search `<section>` and `data-tour="money"` on the `<main>`. Do NOT alter any existing data fetch, the pendingCount derivation, activeMemberships filtering, or standingByGroup logic. Anchors are additive attributes only.
  </action>
  <verify>
    <automated>cd "D:/Code Projects/Gym Tracker" && npx tsc --noEmit -p tsconfig.json && grep -c 'data-tour="challenge"\|data-tour="money"\|data-pending-count' "src/app/(tabs)/groups/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'data-tour="challenge"' src/app/(tabs)/groups/page.tsx` returns the search section anchor
    - `grep -n 'data-tour="money"' src/app/(tabs)/groups/page.tsx` returns an anchor on an unconditionally-mounted element (the main wrapper)
    - `grep -n 'data-pending-count={pendingCount}' src/app/(tabs)/groups/page.tsx` returns the attribute on the main element
    - `npx tsc --noEmit` passes (attributes are valid on the host elements)
    - Source assertion: the empty-state card block (lines around `No challenges yet`) has NO data-tour attribute (anchor is on the always-mounted section, not the conditional card)
  </acceptance_criteria>
  <done>/groups exposes data-tour=challenge (search section), data-tour=money (always-mounted main), and data-pending-count={pendingCount}; no existing data logic changed; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add invited-variant challenge anchor to the first pending invite card on /notifications</name>
  <files>src/app/(tabs)/notifications/client.tsx</files>
  <read_first>
    - src/app/(tabs)/notifications/client.tsx (full file — NotificationsList items.map at line 211 with index param; isInvite computed at line 227; the invite `<li>` at lines 236-300; the isInvite branch JSX at lines 254-300; respond() flow at lines 161-188 — DO NOT modify it)
    - .planning/phases/05-cross-route-walkthrough-teaching-content/05-CONTEXT.md (D-10 invited route line 60, invited-path copy specifics line 140, Claude's Discretion line 64)
    - .planning/phases/05-cross-route-walkthrough-teaching-content/05-PATTERNS.md (lines 130-131 — anchor the FIRST pending invite, index===0 && isInvite)
  </read_first>
  <action>
    In `NotificationsList`, the `items.map((item, index) => ...)` already exposes `index` (line 211) and computes `isInvite` (line 227). Add `data-tour="challenge"` to the rendered invite `<li>` (line 236) ONLY when this is the first pending invite — gate it on `index === 0 && isInvite`. Express it as a conditional attribute spread or a conditional value on the `<li>`: when `index === 0 && isInvite`, set `data-tour="challenge"`, otherwise omit the attribute (a `{...(index === 0 && isInvite ? { "data-tour": "challenge" } : {})}` spread on the `<li>`, or a ternary computing the attribute value to `undefined`). Place it on the `<li>` element so the coachmark anchors the whole first invite card. Do NOT touch the `respond()` accept/decline flow (lines 161-188), the dismiss/clearAll handlers, or any other notification-type branch. This is the invited-path variant (D-10): when the renderer detects pendingCount > 0 it routes the challenge step to /notifications and reveals on this anchor.
  </action>
  <verify>
    <automated>cd "D:/Code Projects/Gym Tracker" && npx tsc --noEmit -p tsconfig.json && grep -c 'data-tour' "src/app/(tabs)/notifications/client.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'data-tour' src/app/(tabs)/notifications/client.tsx` shows the attribute gated on `index === 0 && isInvite`
    - Behavior assertion: the attribute is conditional — it resolves to "challenge" only for the first invite card and is absent (undefined / not spread) for index > 0 and non-invite cards
    - `npx tsc --noEmit` passes
    - Source assertion: `grep -c 'respond' src/app/(tabs)/notifications/client.tsx` is unchanged from before the edit (respond flow untouched) — the function body at lines 161-188 is byte-identical
  </acceptance_criteria>
  <done>The first pending challenge-invite card on /notifications carries data-tour=challenge; subsequent cards and non-invite cards do not; respond() and all other handlers are untouched; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: Add shortcut_viewed anchor to /shortcut</name>
  <files>src/app/(tabs)/shortcut/page.tsx</files>
  <read_first>
    - src/app/(tabs)/shortcut/page.tsx (full file — header div at lines 24-30, ShortcutSetup wrapper div at lines 31-37, both unconditionally mounted)
    - .planning/phases/05-cross-route-walkthrough-teaching-content/05-CONTEXT.md (D-07 line 52 route mapping, D-04 ShortcutSurface embed line 37)
    - .planning/phases/05-cross-route-walkthrough-teaching-content/05-PATTERNS.md (lines 133-134 — anchor the always-mounted ShortcutSetup wrapper or header; the page anchor is separate from the surface embedded in the card)
  </read_first>
  <action>
    Add `data-tour="shortcut_viewed"` to the always-mounted `ShortcutSetup` wrapper `<div>` (lines 31-37). This element renders unconditionally (it is not inside a loading/streaming slot). The id MUST be exactly `shortcut_viewed` to match `STEPS[].id` (steps.ts line 59). Note: this page anchor only GATES where the coachmark points; the actual ShortcutSurface that does the `complete_step: "shortcut_viewed"` write is embedded inside the coachmark card by the renderer (Plan 04) — do NOT add ShortcutSurface to this page. Do NOT alter the webhook secret fetch, the ShortcutSetup props, or the page header copy.
  </action>
  <verify>
    <automated>cd "D:/Code Projects/Gym Tracker" && npx tsc --noEmit -p tsconfig.json && grep -c 'data-tour="shortcut_viewed"' "src/app/(tabs)/shortcut/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'data-tour="shortcut_viewed"' src/app/(tabs)/shortcut/page.tsx` returns exactly the ShortcutSetup wrapper anchor (count 1)
    - The anchor id is literally `shortcut_viewed` (matches the STEPS id, not `shortcut`)
    - `npx tsc --noEmit` passes
    - Source assertion: the webhook secret select and ShortcutSetup props are unchanged
  </acceptance_criteria>
  <done>/shortcut exposes data-tour=shortcut_viewed on the always-mounted ShortcutSetup wrapper; no ShortcutSurface added to the page; secret fetch untouched; tsc passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /groups RSC → coachmark renderer (client) | `data-pending-count` is read from the DOM by the client renderer; it is a non-sensitive integer count (number of pending invites) already rendered on the page |
| /notifications client → tour anchor | a `data-tour` attribute on an invite card is presentation-only metadata; the accept/decline write path (`respond()`) is unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Information Disclosure | `data-pending-count` on /groups | accept | the value is the count of pending invitations the page already renders (groups/page.tsx:147-157); exposing the integer as an attribute discloses nothing not already visible to the owner; RLS still scopes which invites the owner can see |
| T-05-02-02 | Tampering | invite accept/decline flow | mitigate | the edit adds only a `data-tour` attribute; acceptance criterion asserts `respond()` (notifications/client.tsx:161-188) is byte-identical so the financial-relevant accept path gains no new entry point |
| T-05-02-03 | Spoofing | anchor id vs STEPS id | mitigate | `data-tour="shortcut_viewed"`/`"challenge"`/`"money"` ids are asserted to equal `STEPS[].id`; a renderer pointed at a mismatched anchor silently no-ops (the Phase-4 anchor gate degrades to render-nothing) — no crash, no spoofed step |
| T-05-02-SC | — | package installs | accept | N/A — no package installs in this plan |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` — all three route files typecheck with the new attributes
- `grep -n 'data-tour="challenge"\|data-tour="money"\|data-pending-count' src/app/(tabs)/groups/page.tsx`
- `grep -n 'data-tour' src/app/(tabs)/notifications/client.tsx` — gated on index===0 && isInvite
- `grep -n 'data-tour="shortcut_viewed"' src/app/(tabs)/shortcut/page.tsx`
</verification>

<success_criteria>
- /groups carries challenge + money anchors and a live data-pending-count
- /notifications carries the invited-variant challenge anchor on only the first pending invite
- /shortcut carries the shortcut_viewed anchor
- No existing data fetch, respond() flow, or page copy is altered; tsc passes
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

- `data-tour="challenge"` anchor on /groups new-challenge search section
- `data-tour="money"` anchor on /groups main wrapper
- `data-pending-count` attribute on /groups main wrapper (carries `pendingCount`)
- `data-tour="challenge"` conditional anchor on /notifications first pending invite `<li>` (invited variant)
- `data-tour="shortcut_viewed"` anchor on /shortcut ShortcutSetup wrapper
</artifacts_produced>

<output>
Create `.planning/phases/05-cross-route-walkthrough-teaching-content/05-02-SUMMARY.md` when done
</output>
