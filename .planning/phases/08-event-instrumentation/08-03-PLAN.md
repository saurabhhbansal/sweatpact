---
phase: 08-event-instrumentation
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/app/api/groups/create/route.ts
  - src/app/api/groups/join/route.ts
  - src/app/api/challenges/respond/route.ts
  - src/app/api/groups/leave/route.ts
autonomous: true
requirements:
  - INSTR-03
must_haves:
  truths:
    - "PACT_CREATED fires from groups/create POST after both the groups insert and the group_members (owner) insert succeed"
    - "PACT_INVITE_ACCEPTED fires from groups/join POST (invite-code path) after the group_members insert succeeds"
    - "PACT_INVITE_ACCEPTED fires from challenges/respond POST (accept branch) after the recipient's group_members insert succeeds"
    - "PACT_INVITE_DECLINED fires from challenges/respond POST (decline branch) after the invitation status is updated to declined"
    - "PACT_MEMBER_LEFT fires from groups/leave POST only on the non-owner leave path (when deleted_group is false) — not on the owner-deletes-group path"
    - "All five events fire only after their respective DB operations succeed — never on error return paths"
  artifacts:
    - path: "src/app/api/groups/create/route.ts"
      provides: "PACT_CREATED instrumentation"
      contains: "captureServerEvent"
    - path: "src/app/api/groups/join/route.ts"
      provides: "PACT_INVITE_ACCEPTED (invite-code) instrumentation"
      contains: "captureServerEvent"
    - path: "src/app/api/challenges/respond/route.ts"
      provides: "PACT_INVITE_ACCEPTED (invitation) and PACT_INVITE_DECLINED instrumentation"
      contains: "captureServerEvent"
    - path: "src/app/api/groups/leave/route.ts"
      provides: "PACT_MEMBER_LEFT instrumentation"
      contains: "captureServerEvent"
  key_links:
    - from: "src/app/api/challenges/respond/route.ts"
      to: "src/lib/analytics/server.ts"
      via: "import captureServerEvent — used for both accept and decline branches"
      pattern: "captureServerEvent"
---

<objective>
Instrument the four pact lifecycle API routes to emit typed events at each key state transition.

Purpose: Delivers INSTR-03 — challenge created, invite accepted (both paths), invite declined, and member left events — giving the Phase 9 dashboard pact funnel and retention data.
Output: Four modified route handlers, each with captureServerEvent calls at the correct post-DB-success points.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-event-instrumentation/08-CONTEXT.md
@.planning/phases/08-event-instrumentation/08-RESEARCH.md
@.planning/phases/08-event-instrumentation/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instrument groups/create and groups/join (PACT_CREATED, PACT_INVITE_ACCEPTED via code)</name>
  <files>src/app/api/groups/create/route.ts, src/app/api/groups/join/route.ts</files>
  <read_first>
    - src/app/api/groups/create/route.ts — read the complete POST handler; identify where the group_members insert (owner row) is checked for memberError; the event fires after the memberError null-check
    - src/app/api/groups/join/route.ts — read the complete POST handler; identify where the group_members insert (member row) is checked for memberError; the event fires after the memberError null-check
    - src/lib/analytics/events.ts — confirm EVENT.PACT_CREATED and EVENT.PACT_INVITE_ACCEPTED constant names
    - .planning/phases/08-event-instrumentation/08-RESEARCH.md — read the Integration Point Analysis for INSTR-03 (event/route/trigger table)
  </read_first>
  <action>
GROUPS/CREATE route (src/app/api/groups/create/route.ts):

Add imports: captureServerEvent from @/lib/analytics/server, EVENT from @/lib/analytics/events.

Find the block that checks memberError: "if (memberError) { return NextResponse.json(...) }". After this block (when memberError is null, meaning both inserts succeeded), insert await captureServerEvent(auth.user.id, EVENT.PACT_CREATED, { group_id: group.id }). This line goes immediately before the final "return NextResponse.json({ ok: true, group })" statement.

GROUPS/JOIN route (src/app/api/groups/join/route.ts):

Add imports: captureServerEvent from @/lib/analytics/server, EVENT from @/lib/analytics/events.

Find the block that checks memberError: "if (memberError) { return NextResponse.json(...) }". After this block (when memberError is null), insert await captureServerEvent(auth.user.id, EVENT.PACT_INVITE_ACCEPTED, { group_id: group.id, method: "invite_code" }). The method property distinguishes this invite-code acceptance path from the invitation-based accept in the respond route. This line goes immediately before the final "return NextResponse.json({ ok: true, group })" statement.

Neither call is wrapped in additional try/catch.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>groups/create/route.ts emits PACT_CREATED after both inserts succeed. groups/join/route.ts emits PACT_INVITE_ACCEPTED with method "invite_code" after the member insert succeeds. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Instrument challenges/respond and groups/leave (PACT_INVITE_ACCEPTED, PACT_INVITE_DECLINED, PACT_MEMBER_LEFT)</name>
  <files>src/app/api/challenges/respond/route.ts, src/app/api/groups/leave/route.ts</files>
  <read_first>
    - src/app/api/challenges/respond/route.ts — read the complete POST handler; identify (a) the decline branch ending in "return NextResponse.json({ ok: true, action: 'declined' })" and (b) the accept branch where the recipient's group_members insert (memberError check) is confirmed, before the backfill loop
    - src/app/api/groups/leave/route.ts — read the complete POST handler; identify (a) the owner path that calls groups.delete and returns { deleted_group: true }, and (b) the non-owner path that deletes the single member row and returns { deleted_group: false }
    - src/lib/analytics/events.ts — confirm EVENT.PACT_INVITE_ACCEPTED, EVENT.PACT_INVITE_DECLINED, EVENT.PACT_MEMBER_LEFT constant names
    - .planning/phases/08-event-instrumentation/08-RESEARCH.md — read Integration Point Analysis for INSTR-03; note that PACT_MEMBER_LEFT fires only on the non-owner path
  </read_first>
  <action>
CHALLENGES/RESPOND route (src/app/api/challenges/respond/route.ts):

Add imports: captureServerEvent from @/lib/analytics/server, EVENT from @/lib/analytics/events.

DECLINE BRANCH: Find the decline branch that ends with "return NextResponse.json({ ok: true, action: 'declined' })". Insert await captureServerEvent(auth.user.id, EVENT.PACT_INVITE_DECLINED, { invitation_id, group_id: invitation.group_id ?? null }) immediately before that return statement. At this point the invitation's status has already been updated to "declined" in the DB.

ACCEPT BRANCH: Find the block that checks memberError for the recipient's group_members insert (the line reading "if (memberError) { ... return releaseAndFail(memberError.message); }"). After the if block (when memberError is null, meaning the recipient successfully joined), insert await captureServerEvent(auth.user.id, EVENT.PACT_INVITE_ACCEPTED, { group_id: groupId, method: "invitation" }) immediately before the backfill loop (the "for (const userId of backfillUsers)" loop). The method property is "invitation" to distinguish from the invite-code join path.

GROUPS/LEAVE route (src/app/api/groups/leave/route.ts):

Add imports: captureServerEvent from @/lib/analytics/server, EVENT from @/lib/analytics/events.

OWNER PATH: Do NOT add any analytics call here. The owner-deletes-group path (which deletes all members and the group itself) returns { deleted_group: true } — this is a group dissolution, not a member-left event.

NON-OWNER PATH: Find the block that checks the error from "group_members.delete().eq(group_id).eq(user_id)": "if (error) { return NextResponse.json({ error: error.message }, { status: 500 }) }". After this error check (when error is null, meaning the delete succeeded), insert await captureServerEvent(auth.user.id, EVENT.PACT_MEMBER_LEFT, { group_id: groupId, role: membership.role }). This line goes immediately before the final "return NextResponse.json({ ok: true, deleted_group: false })" statement.

No calls are wrapped in additional try/catch.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20</automated>
  </verify>
  <done>challenges/respond/route.ts emits PACT_INVITE_DECLINED in the decline branch and PACT_INVITE_ACCEPTED in the accept branch. groups/leave/route.ts emits PACT_MEMBER_LEFT only on the non-owner delete path. npm test exits 0. npx tsc --noEmit exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| respond route → captureServerEvent | invitation_id (UUID) and group_id (UUID or null) cross into analytics |
| leave route → captureServerEvent | group_id (UUID) and membership.role (enum string) cross into analytics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Tampering | PACT_CREATED firing before both inserts confirm | mitigate | Event fires after the group_members insert (owner row) error check. Both the groups insert and group_members insert must have succeeded before the event fires. |
| T-08-03-02 | Tampering | PACT_INVITE_ACCEPTED firing on rollback paths in challenges/respond | mitigate | The event is placed after memberError is confirmed null and before the backfill loop — which is after the critical race-condition claim is already confirmed stable. releaseAndFail paths all return before reaching the event. |
| T-08-03-03 | Information Disclosure | group_id and invitation_id in properties | accept | These are Supabase UUIDs — opaque identifiers with no PII content. Standard analytics property. |
</threat_model>

<verification>
- groups/create/route.ts: captureServerEvent(auth.user.id, EVENT.PACT_CREATED, { group_id }) after memberError check
- groups/join/route.ts: captureServerEvent(auth.user.id, EVENT.PACT_INVITE_ACCEPTED, { group_id, method: "invite_code" }) after memberError check
- challenges/respond/route.ts decline branch: captureServerEvent before the "declined" return
- challenges/respond/route.ts accept branch: captureServerEvent before the backfill loop, method: "invitation"
- groups/leave/route.ts: captureServerEvent only in the non-owner path (deleted_group: false path), not in the owner-deletes-group path
- npx tsc --noEmit exits 0
- npm test exits 0
</verification>

<success_criteria>
All five pact lifecycle events fire from the correct routes at the correct success points. No event fires on error return paths. TypeScript strict mode satisfied.
</success_criteria>

## Artifacts this phase produces

New call sites added to existing files:
- `src/app/api/groups/create/route.ts` — captureServerEvent for PACT_CREATED
- `src/app/api/groups/join/route.ts` — captureServerEvent for PACT_INVITE_ACCEPTED (method: "invite_code")
- `src/app/api/challenges/respond/route.ts` — captureServerEvent for PACT_INVITE_DECLINED and PACT_INVITE_ACCEPTED (method: "invitation")
- `src/app/api/groups/leave/route.ts` — captureServerEvent for PACT_MEMBER_LEFT (non-owner path only)

<output>
Create .planning/phases/08-event-instrumentation/08-03-SUMMARY.md when done
</output>
