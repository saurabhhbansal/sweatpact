---
phase: 08-event-instrumentation
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/app/api/onboarding-progress/route.ts
  - src/app/api/checkin/route.ts
autonomous: true
requirements:
  - INSTR-01
  - INSTR-02
must_haves:
  truths:
    - "Each successful onboarding-progress PATCH where complete_step is set emits ONBOARDING_STEP_COMPLETED with step_id and tour_version properties"
    - "ONBOARDING_WALKTHROUGH_COMPLETED emits only on the first completion — gated on the transition from null to a non-null completed_at value (existing?.completed_at was null, data.completed_at is now set)"
    - "A geo-failed check-in emits CHECKIN_GEO_FAILED before the 422 return, carrying method and distance_m properties"
    - "A successful check-in (verified or unverified) emits CHECKIN_SUBMITTED after reconciliation with outcome, method, and distance_m properties"
    - "Analytics events fire only after the primary DB operation succeeds and its error is confirmed nil — no phantom events on DB failures"
  artifacts:
    - path: "src/app/api/onboarding-progress/route.ts"
      provides: "PATCH route instrumented for INSTR-01"
      contains: "captureServerEvent"
    - path: "src/app/api/checkin/route.ts"
      provides: "POST route instrumented for INSTR-02"
      contains: "captureServerEvent"
  key_links:
    - from: "src/app/api/onboarding-progress/route.ts"
      to: "src/lib/analytics/server.ts"
      via: "import captureServerEvent from @/lib/analytics/server"
      pattern: "captureServerEvent"
    - from: "src/app/api/checkin/route.ts"
      to: "src/lib/analytics/server.ts"
      via: "import captureServerEvent from @/lib/analytics/server"
      pattern: "captureServerEvent"
---

<objective>
Instrument the onboarding-progress and checkin API routes to emit typed server-side events after their primary DB operations succeed.

Purpose: Delivers INSTR-01 (onboarding funnel visibility) and INSTR-02 (check-in outcome tracking) so the Phase 9 dashboard has real per-step and per-outcome data.
Output: Two modified route handlers with analytics calls inserted at the correct success points.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-event-instrumentation/08-CONTEXT.md
@.planning/phases/08-event-instrumentation/08-RESEARCH.md
@.planning/phases/08-event-instrumentation/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instrument onboarding-progress PATCH (INSTR-01)</name>
  <files>src/app/api/onboarding-progress/route.ts</files>
  <read_first>
    - src/app/api/onboarding-progress/route.ts — read the complete PATCH handler; note where `existing` is read (SELECT before upsert), where the upsert result `data` is returned, and where revalidateTag is called
    - src/lib/analytics/events.ts — confirm EVENT.ONBOARDING_STEP_COMPLETED and EVENT.ONBOARDING_WALKTHROUGH_COMPLETED constant names
    - .planning/phases/08-event-instrumentation/08-RESEARCH.md — read Pattern 2 (INSTR-01 onboarding step events) and Pitfall 4 (walkthrough replay guard)
  </read_first>
  <action>
Add two new imports at the top of src/app/api/onboarding-progress/route.ts, after the existing imports: captureServerEvent from @/lib/analytics/server, and EVENT from @/lib/analytics/events.

In the PATCH handler, locate the block that checks the upsert error: "if (error) { return NextResponse.json(...) }". The analytics calls go immediately after this block (after confirming error is null), before the revalidateTag call.

Add the following two analytics calls in sequence:

First call (step event): If parsed.data.complete_step is truthy, call await captureServerEvent with auth.user.id as distinctId, EVENT.ONBOARDING_STEP_COMPLETED as event, and an object containing step_id set to parsed.data.complete_step and tour_version set to data.tour_version as properties.

Second call (walkthrough completion event): Emit only on the null-to-set transition of completed_at. The condition is: data.completed_at is truthy AND existing?.completed_at is falsy (null or undefined). When this condition is true, call await captureServerEvent with auth.user.id as distinctId, EVENT.ONBOARDING_WALKTHROUGH_COMPLETED as event, and an object containing tour_version set to data.tour_version as properties. This guard prevents re-firing when the user replays the tour — existing?.completed_at being already set means the walkthrough was previously completed.

Both calls must appear between the upsert error check and the revalidateTag line. Both use await. Neither call is wrapped in any additional try/catch — the helper already swallows errors internally.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>onboarding-progress/route.ts contains captureServerEvent calls for ONBOARDING_STEP_COMPLETED and ONBOARDING_WALKTHROUGH_COMPLETED. Calls are after the upsert error check and before revalidateTag. TypeScript compiles without errors. npm test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Instrument checkin POST (INSTR-02)</name>
  <files>src/app/api/checkin/route.ts</files>
  <read_first>
    - src/app/api/checkin/route.ts — read the complete POST handler; identify: (a) the geo-fail early return that checks `!verified && (!body.allow_unverified || body.source === "shortcut")`, (b) the final success return at the very end of the function
    - src/lib/analytics/events.ts — confirm EVENT.CHECKIN_GEO_FAILED and EVENT.CHECKIN_SUBMITTED constant names
    - .planning/phases/08-event-instrumentation/08-RESEARCH.md — read Integration Point Analysis for INSTR-02 (three-branch outcome mapping table)
  </read_first>
  <action>
Add two new imports at the top of src/app/api/checkin/route.ts: captureServerEvent from @/lib/analytics/server, and EVENT from @/lib/analytics/events.

PLACEMENT 1 — geo-fail event: Find the conditional block that reads "if (!verified && (!body.allow_unverified || body.source === 'shortcut'))". This block returns a 422 JSON response with error "location_outside_radius". Insert await captureServerEvent(profile.id, EVENT.CHECKIN_GEO_FAILED, { method: body.source, distance_m: distance }) immediately before that 422 return statement, inside the same if block. At this point in the code profile.id is guaranteed non-null (profile was fetched and null-checked earlier). distance may be null if no gym coordinates were provided — pass it as-is; the properties type accepts null values.

PLACEMENT 2 — success event: Find the final return statement at the end of the POST handler that returns NextResponse.json({ ok: true, action, checkin: ..., submission_id, verified, distance_m }). Insert await captureServerEvent(profile.id, EVENT.CHECKIN_SUBMITTED, { outcome: verified ? "verified" : "unverified", method: body.source, distance_m: distance }) immediately before that final return statement, after the notifyGroupCheckin block. This fires for both verified and unverified outcomes — the outcome property carries the distinction.

Neither call is wrapped in additional try/catch. Both use await. The geo-fail event fires inside the 422-return block; the success event fires at the very end of the happy path.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20</automated>
  </verify>
  <done>checkin/route.ts contains CHECKIN_GEO_FAILED emit before the 422 return and CHECKIN_SUBMITTED emit before the final 200 return. TypeScript compiles clean. Existing test suite passes (npm test exits 0).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route handler → captureServerEvent | Validated, Zod-parsed data crosses into the analytics helper |
| checkin route → PostHog | outcome/method/distance_m properties are sent to PostHog EU |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Tampering | Event fires before DB confirms | mitigate | Both captureServerEvent calls are placed after their respective DB operations succeed and error is confirmed null. A failed upsert or insert returns early before analytics is reached. |
| T-08-02-02 | Information Disclosure | distance_m in CHECKIN_GEO_FAILED properties | accept | distance_m is a numeric distance in meters — not a precise location coordinate. It reveals proximity to a gym but not the user's actual location. Acceptable for analytics. |
| T-08-02-03 | Information Disclosure | ONBOARDING_WALKTHROUGH_COMPLETED replay guard | accept | The completed_at transition gate prevents duplicate events on tour replay. No PII is included in event properties. |
</threat_model>

<verification>
- onboarding-progress/route.ts imports captureServerEvent and EVENT
- EVENT.ONBOARDING_STEP_COMPLETED call is after the upsert error check and guarded by parsed.data.complete_step being truthy
- EVENT.ONBOARDING_WALKTHROUGH_COMPLETED call is guarded by data.completed_at && !existing?.completed_at
- checkin/route.ts imports captureServerEvent and EVENT
- EVENT.CHECKIN_GEO_FAILED fires inside the 422 geo-reject conditional
- EVENT.CHECKIN_SUBMITTED fires before the final success return with outcome/method/distance_m properties
- npm test exits 0 (existing suite unaffected)
- npx tsc --noEmit exits 0
</verification>

<success_criteria>
Onboarding funnel events and check-in outcome events are emitted server-side at the correct points in each route's success/failure paths. No existing business logic is affected. TypeScript strict mode satisfied.
</success_criteria>

## Artifacts this phase produces

New symbols added to existing files:
- `src/app/api/onboarding-progress/route.ts` — two captureServerEvent call sites (ONBOARDING_STEP_COMPLETED, ONBOARDING_WALKTHROUGH_COMPLETED)
- `src/app/api/checkin/route.ts` — two captureServerEvent call sites (CHECKIN_GEO_FAILED, CHECKIN_SUBMITTED)

<output>
Create .planning/phases/08-event-instrumentation/08-02-SUMMARY.md when done
</output>
