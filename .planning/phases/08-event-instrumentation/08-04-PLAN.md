---
phase: 08-event-instrumentation
plan: 04
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/lib/enforcement.ts
  - src/app/api/cron/enforce/route.ts
  - src/app/api/settlements/route.ts
autonomous: true
requirements:
  - INSTR-04
must_haves:
  truths:
    - "FINANCIAL_PENALTY_ISSUED fires once per penalized user inside the cron run — one event per user_id that transitions from non-missed to missed"
    - "posthog.shutdown() runs in a finally block in the cron handler so events are flushed before Vercel tears down the function — even if runEnforcement throws"
    - "FINANCIAL_SETTLEMENT_RECORDED fires from settlements/route.ts POST after both the settlements insert and the obligations status update succeed"
    - "The cron uses a single shared PostHog client for the entire run (not the captureServerEvent helper) — one client, N capture calls, one shutdown"
    - "enforcement.ts returns penalized_user_ids in addition to existing scalar counters — callers that spread the result into JSON responses include the field (acceptable for the admin-only cron endpoint)"
  artifacts:
    - path: "src/lib/enforcement.ts"
      provides: "EnforcementResult with penalized_user_ids field"
      contains: "penalized_user_ids"
    - path: "src/app/api/cron/enforce/route.ts"
      provides: "PostHog client lifecycle + FINANCIAL_PENALTY_ISSUED events"
      contains: "posthog.shutdown"
    - path: "src/app/api/settlements/route.ts"
      provides: "FINANCIAL_SETTLEMENT_RECORDED event"
      contains: "captureServerEvent"
  key_links:
    - from: "src/app/api/cron/enforce/route.ts"
      to: "src/lib/enforcement.ts"
      via: "reads result.penalized_user_ids to emit per-user penalty events"
      pattern: "penalized_user_ids"
    - from: "src/app/api/cron/enforce/route.ts"
      to: "posthog-node"
      via: "direct new PostHog() instantiation — not through captureServerEvent helper"
      pattern: "posthog\\.shutdown"
    - from: "src/app/api/settlements/route.ts"
      to: "src/lib/analytics/server.ts"
      via: "import captureServerEvent from @/lib/analytics/server"
      pattern: "captureServerEvent"
---

<objective>
Instrument the financial event paths — enforcement penalties and manual settlements — ensuring cron events survive Vercel function teardown.

Purpose: Delivers INSTR-04. The cron manages its own PostHog client lifecycle (single client + explicit shutdown in finally) so no penalty events are lost. Settlements use the standard captureServerEvent helper.
Output: enforcement.ts extended with penalized_user_ids; cron route with PostHog lifecycle; settlements route with settlement event.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-event-instrumentation/08-CONTEXT.md
@.planning/phases/08-event-instrumentation/08-RESEARCH.md
@.planning/phases/08-event-instrumentation/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend enforcement.ts and instrument cron/enforce route</name>
  <files>src/lib/enforcement.ts, src/app/api/cron/enforce/route.ts</files>
  <read_first>
    - src/lib/enforcement.ts — read the complete file; note the EnforcementResult type, the result initializer, the per-profile loop, and the block "if (reconciled.status === 'missed' && existing?.status !== 'missed') { result.penalized += 1 }"
    - src/app/api/cron/enforce/route.ts — read the complete handle() function; note the try/catch structure, the runEnforcement call, the spread into NextResponse.json, and where sendPeriodReminders is called
    - src/lib/analytics/events.ts — confirm EVENT.FINANCIAL_PENALTY_ISSUED constant name
    - .planning/phases/08-event-instrumentation/08-RESEARCH.md — read Pattern 3 (INSTR-04 financial events with shutdown), specifically Option B (shared client for cron) and the cron/enforce shutdown pattern code example
  </read_first>
  <action>
ENFORCEMENT.TS changes:

In the EnforcementResult type definition, add a new field: penalized_user_ids as string array.

In the result initializer object (where scanned, penalized, skipped, weeklyChecked, errors are initialized to their default values), add penalized_user_ids initialized to an empty array.

In the per-profile loop, find the block: "if (reconciled.status === 'missed' && existing?.status !== 'missed') { result.penalized += 1 }". After the result.penalized increment, add result.penalized_user_ids.push(profile.id) on a new line within the same if block.

No other changes to enforcement.ts.

CRON/ENFORCE/ROUTE.TS changes:

Add new imports at the top: PostHog from posthog-node, EVENT from @/lib/analytics/events.

In the handle() function, after creating the admin client (const admin = createAdminClient()) and after computing now (const now = new Date()), create a shared PostHog client: const posthog = new PostHog(process.env.NEXT_PUBLIC_POSTHOG_KEY ?? "", { host: "https://eu.i.posthog.com", flushAt: 1, flushInterval: 0 }).

Restructure the existing try/catch into a try/catch/finally. The finally block contains a single statement: await posthog.shutdown(). This guarantees flush runs regardless of whether runEnforcement succeeds or throws.

Inside the try block, after the runEnforcement call returns result, add a loop that iterates result.penalized_user_ids. For each userId in the array, call posthog.capture({ distinctId: userId, event: EVENT.FINANCIAL_PENALTY_ISSUED }) — no additional properties. The capture calls happen after runEnforcement and before the NextResponse.json return.

The existing catch block (which logs the error and returns a 500 response) stays in place. The finally block runs after both the try and catch paths.

The sendPeriodReminders call stays inside the try block in its own nested try/catch, unchanged.

Do NOT import captureServerEvent in the cron route — it manages its own PostHog lifecycle directly.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>enforcement.ts EnforcementResult type includes penalized_user_ids: string[]. The penalized block pushes profile.id. cron/enforce/route.ts creates a PostHog instance, emits per-user FINANCIAL_PENALTY_ISSUED events, and calls await posthog.shutdown() in a finally block. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Instrument settlements/route.ts (FINANCIAL_SETTLEMENT_RECORDED)</name>
  <files>src/app/api/settlements/route.ts</files>
  <read_first>
    - src/app/api/settlements/route.ts — read the complete POST handler; identify (a) the settlements insert and the settleErr check, (b) the obligations status update and the updateErr check, (c) the best-effort rollback on updateErr, and (d) the final "return NextResponse.json({ ok: true })"
    - src/lib/analytics/events.ts — confirm EVENT.FINANCIAL_SETTLEMENT_RECORDED constant name
  </read_first>
  <action>
Add imports at the top of src/app/api/settlements/route.ts: captureServerEvent from @/lib/analytics/server, EVENT from @/lib/analytics/events.

Find the block that checks updateErr (the obligations status update). This block has a rollback (delete the orphaned settlement row) and returns a 500 on error. After this if block (when updateErr is null, meaning both the settlements insert AND the obligations status update have succeeded), insert await captureServerEvent(auth.user.id, EVENT.FINANCIAL_SETTLEMENT_RECORDED, { obligation_id, group_id: oblig.group_id }). This line goes immediately before the final "return NextResponse.json({ ok: true })" statement.

The event fires only when both DB writes succeed — if settleErr fires first (earlier in the function), the function already returned a 500 before reaching this point. If updateErr fires, the rollback happens and a 500 is returned — again the event line is not reached. Only the successful path reaches the event.

No additional try/catch wrapper needed — captureServerEvent swallows errors internally.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20</automated>
  </verify>
  <done>settlements/route.ts emits FINANCIAL_SETTLEMENT_RECORDED with obligation_id and group_id after both DB writes succeed. npm test exits 0. npx tsc --noEmit exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron/enforce → PostHog EU | Per-user penalty events (user UUIDs only) sent during the enforcement loop |
| settlements → captureServerEvent | obligation_id and group_id UUIDs cross into analytics |
| enforcement.ts → cron route | penalized_user_ids array (Supabase UUIDs) flows from financial module to analytics layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Availability | Vercel function teardown drops cron events | mitigate | await posthog.shutdown() runs in a finally block — executes regardless of whether runEnforcement succeeds or throws. Events are flushed synchronously before the function exits. |
| T-08-04-02 | Information Disclosure | Financial amounts in penalty/settlement events | mitigate | FINANCIAL_PENALTY_ISSUED carries only the user UUID (distinctId) with no amount_cents property. FINANCIAL_SETTLEMENT_RECORDED carries obligation_id and group_id (UUIDs). No dollar amounts in PostHog properties. |
| T-08-04-03 | Tampering | Penalty events fired for non-penalized users | mitigate | penalized_user_ids is populated only inside "if (reconciled.status === 'missed' && existing?.status !== 'missed')" — the same guard that increments result.penalized. Events are scoped to the same condition as the existing counter. |
</threat_model>

<verification>
- src/lib/enforcement.ts: EnforcementResult type has penalized_user_ids: string[]
- src/lib/enforcement.ts: result initializer includes penalized_user_ids: []
- src/lib/enforcement.ts: profile.id is pushed to penalized_user_ids in the missed-transition block
- cron/enforce/route.ts: PostHog instance created with eu.i.posthog.com host
- cron/enforce/route.ts: posthog.capture called per userId in penalized_user_ids after runEnforcement
- cron/enforce/route.ts: await posthog.shutdown() is in a finally block
- settlements/route.ts: captureServerEvent(auth.user.id, EVENT.FINANCIAL_SETTLEMENT_RECORDED, ...) before final return
- npm test exits 0
- npx tsc --noEmit exits 0
</verification>

<success_criteria>
Financial penalty events survive Vercel function teardown. Settlement events fire at the correct post-DB-success point. No financial amounts are sent to PostHog as properties.
</success_criteria>

## Artifacts this phase produces

New fields and call sites:
- `src/lib/enforcement.ts` — `EnforcementResult.penalized_user_ids: string[]` field; push at penalization detection
- `src/app/api/cron/enforce/route.ts` — PostHog client, per-user FINANCIAL_PENALTY_ISSUED capture loop, posthog.shutdown() in finally
- `src/app/api/settlements/route.ts` — captureServerEvent for FINANCIAL_SETTLEMENT_RECORDED

<output>
Create .planning/phases/08-event-instrumentation/08-04-SUMMARY.md when done
</output>
