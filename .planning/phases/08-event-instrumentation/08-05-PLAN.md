---
phase: 08-event-instrumentation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/nav.tsx
  - src/components/notifications-overlay.tsx
  - src/app/(tabs)/shortcut/client.tsx
autonomous: true
requirements:
  - INSTR-05
must_haves:
  truths:
    - "Clicking any tab link in MobileNav fires FEATURE_TAB_VISITED with a tab property set to the link label lowercased (e.g., 'dashboard', 'challenges', 'profile')"
    - "Clicking within the notifications section in NotificationsOverlay fires FEATURE_NOTIFICATION_CLICKED via event delegation on the section wrapping NotificationsList"
    - "ShortcutSetup component fires FEATURE_SHORTCUT_SETUP_VIEWED once on mount via a useEffect with an empty dependency array"
    - "All client-side capture calls use posthog?.capture (optional chaining) — safe when usePostHog() returns undefined outside PostHogProvider or during SSR hydration"
    - "All event names use EVENT.* constants — no inline event-name strings"
  artifacts:
    - path: "src/components/nav.tsx"
      provides: "FEATURE_TAB_VISITED event on tab link click"
      contains: "FEATURE_TAB_VISITED"
    - path: "src/components/notifications-overlay.tsx"
      provides: "FEATURE_NOTIFICATION_CLICKED event on notification section click"
      contains: "FEATURE_NOTIFICATION_CLICKED"
    - path: "src/app/(tabs)/shortcut/client.tsx"
      provides: "FEATURE_SHORTCUT_SETUP_VIEWED event on ShortcutSetup mount"
      contains: "FEATURE_SHORTCUT_SETUP_VIEWED"
  key_links:
    - from: "src/components/nav.tsx"
      to: "posthog-js/react"
      via: "usePostHog() hook — instance sourced from PostHogProvider in root layout"
      pattern: "usePostHog"
    - from: "src/app/(tabs)/shortcut/client.tsx"
      to: "posthog-js/react"
      via: "usePostHog() hook + useEffect on mount"
      pattern: "usePostHog"
---

<objective>
Wire three client-side feature-usage events using the usePostHog() hook from posthog-js/react.

Purpose: Delivers INSTR-05. No server helper needed — all three components are "use client" descendants of the PostHogProvider in the root layout. Events track tab navigation, notification engagement, and shortcut page discovery.
Output: Three modified client components with posthog?.capture calls at the specified interaction points.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-event-instrumentation/08-CONTEXT.md
@.planning/phases/08-event-instrumentation/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire tab visit events in nav.tsx (FEATURE_TAB_VISITED)</name>
  <files>src/components/nav.tsx</files>
  <read_first>
    - src/components/nav.tsx — read the MobileNav component; note the links.map() render loop that produces Link elements with href, label, and Icon; identify where to add the onClick prop
    - src/lib/analytics/events.ts — confirm EVENT.FEATURE_TAB_VISITED constant name
    - .planning/phases/08-event-instrumentation/08-RESEARCH.md — read Pattern 4 (client-side usePostHog), Pitfall 3 (usePostHog outside provider — optional chaining), and Pitfall 5 (tab events on initial load)
  </read_first>
  <action>
Add two new imports at the top of src/components/nav.tsx (after the existing imports): usePostHog from posthog-js/react, and EVENT from @/lib/analytics/events.

Inside the MobileNav component function body, add const posthog = usePostHog() after the existing state and ref declarations (after the prevIndexRef declaration).

In the links.map() render block, find the Link element that is returned for each nav link. Add an onClick prop to this Link element. The onClick handler calls posthog?.capture(EVENT.FEATURE_TAB_VISITED, { tab: link.label.toLowerCase() }). Use optional chaining on posthog — this is required because usePostHog() can return undefined if called outside PostHogProvider or before the provider initializes. The tab property value is the link's label string converted to lowercase, which produces "dashboard", "challenges", or "profile" for the three main tabs.

The onClick handler fires only when the user actively clicks the Link element — not on programmatic navigation from router.push() elsewhere in the app (Pitfall 5: attach to the Link element, not a parent wrapper).

Do not modify the Cycle tab Link (the female-only circle button below the main pill) — it is a separate Link outside the links.map() loop. The three main tab links are the scope.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>nav.tsx imports usePostHog and EVENT. MobileNav has const posthog = usePostHog(). Each tab Link has an onClick that calls posthog?.capture(EVENT.FEATURE_TAB_VISITED, { tab: link.label.toLowerCase() }). TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire notification CTR in notifications-overlay.tsx and mount event in shortcut/client.tsx</name>
  <files>src/components/notifications-overlay.tsx, src/app/(tabs)/shortcut/client.tsx</files>
  <read_first>
    - src/components/notifications-overlay.tsx — read the complete NotificationsOverlay component; find the section element that wraps the NotificationsList component (inside the mounted portal); this is the target for the onClick handler
    - src/app/(tabs)/shortcut/client.tsx — read the ShortcutSetup function (search for "export function ShortcutSetup" near the bottom of the file); note the existing imports at the top (useState, useTransition, useRouter); useEffect must be added to the React imports
    - src/lib/analytics/events.ts — confirm EVENT.FEATURE_NOTIFICATION_CLICKED and EVENT.FEATURE_SHORTCUT_SETUP_VIEWED constant names
    - .planning/phases/08-event-instrumentation/08-RESEARCH.md — read Pattern 4 shortcut useEffect example and the optional chaining requirement
  </read_first>
  <action>
NOTIFICATIONS-OVERLAY.TSX:

Add two new imports: usePostHog from posthog-js/react, and EVENT from @/lib/analytics/events.

Inside the NotificationsOverlay function body, add const posthog = usePostHog() after the existing state declarations.

Find the section element inside the mounted portal that wraps the NotificationsList. Its className contains "rounded-[2rem] glass-card p-5". Add an onClick prop to this section element: onClick={() => posthog?.capture(EVENT.FEATURE_NOTIFICATION_CLICKED)}. This uses event delegation — any click within the notification list section (on individual notifications, accept/decline buttons, etc.) bubbles up to the section and fires the event once. Use optional chaining on posthog.

SHORTCUT/CLIENT.TSX:

In the React import at the top of the file (the line that imports useState and useTransition), add useEffect to the same destructured import.

Add two new imports: usePostHog from posthog-js/react, and EVENT from @/lib/analytics/events.

Inside the ShortcutSetup function body (the exported function near the bottom of the file), add const posthog = usePostHog() after the existing hook declarations (after the useRouter and useState calls).

Add a useEffect call immediately after the posthog declaration. The effect body calls posthog?.capture(EVENT.FEATURE_SHORTCUT_SETUP_VIEWED). The dependency array is empty ([]) so the effect fires exactly once on mount. This fires when the user lands on the shortcut setup page, regardless of whether they interact with it further.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20</automated>
  </verify>
  <done>notifications-overlay.tsx has usePostHog, EVENT import, posthog instance, and onClick on the notification section. shortcut/client.tsx has useEffect added to React imports, usePostHog and EVENT imported, posthog instance in ShortcutSetup, and a mount-only useEffect that captures FEATURE_SHORTCUT_SETUP_VIEWED. npm test exits 0. npx tsc --noEmit exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → PostHog (via /ingest proxy) | Client-side feature-usage events sent through the existing browser SDK |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Tampering | Client-side events are user-controlled | accept | FEATURE_TAB_VISITED, FEATURE_NOTIFICATION_CLICKED, and FEATURE_SHORTCUT_SETUP_VIEWED are feature-usage signals only — not used for financial calculations or authorization decisions. Server-side events (Plans 08-02 through 08-04) are the authoritative source for business-critical data. |
| T-08-05-02 | Information Disclosure | Tab label in FEATURE_TAB_VISITED properties | accept | The tab property value is one of "dashboard", "challenges", or "profile" — enum-like strings with no PII content. |
| T-08-05-03 | Information Disclosure | FEATURE_SHORTCUT_SETUP_VIEWED fires on mount | accept | The event carries no properties beyond the user identity already managed by PostHogIdentity from Phase 7. No PII is captured. |
</threat_model>

<verification>
- nav.tsx: usePostHog imported and called in MobileNav; tab Link elements have onClick with posthog?.capture(EVENT.FEATURE_TAB_VISITED, { tab: link.label.toLowerCase() })
- notifications-overlay.tsx: usePostHog imported and called; section wrapping NotificationsList has onClick with posthog?.capture(EVENT.FEATURE_NOTIFICATION_CLICKED)
- shortcut/client.tsx: useEffect added to React import; usePostHog and EVENT imported; ShortcutSetup has posthog = usePostHog() and useEffect with empty dep array
- All posthog calls use optional chaining (posthog?.capture)
- No inline event-name strings — all calls reference EVENT.*
- npm test exits 0
- npx tsc --noEmit exits 0
</verification>

<success_criteria>
All three client-side feature-usage events fire at the correct user interaction points. Optional chaining prevents crashes outside PostHogProvider. Typed EVENT constants used throughout.
</success_criteria>

## Artifacts this phase produces

New call sites in existing files:
- `src/components/nav.tsx` — usePostHog() call; onClick on tab Links emitting FEATURE_TAB_VISITED
- `src/components/notifications-overlay.tsx` — usePostHog() call; onClick on notification section emitting FEATURE_NOTIFICATION_CLICKED
- `src/app/(tabs)/shortcut/client.tsx` — useEffect added to React imports; usePostHog() call; mount useEffect emitting FEATURE_SHORTCUT_SETUP_VIEWED in ShortcutSetup

<output>
Create .planning/phases/08-event-instrumentation/08-05-SUMMARY.md when done
</output>
