---
phase: 03-minimal-start-tourprovider-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/supabase/rsc.ts
  - src/lib/onboarding/current-step.ts
  - src/lib/onboarding/current-step.test.ts
autonomous: true
requirements: [ONB-04]
must_haves:
  truths:
    - "A request-cached server reader returns the viewer's own onboarding_progress row (or null) scoped strictly to their user id"
    - "A pure deriveCurrentStep() returns null when dismissed, the first pending non-skippable step otherwise, and null when the tour is complete"
    - "deriveCurrentStep is unit-covered for dismiss, fresh-start, resume, and auto-skip cases (ONB-04 resume/dismiss seam)"
  artifacts:
    - path: "src/lib/supabase/rsc.ts"
      provides: "getOnboardingProgress() request-cached owner-scoped reader"
      contains: "export const getOnboardingProgress"
    - path: "src/lib/onboarding/current-step.ts"
      provides: "deriveCurrentStep() pure step-resolution helper"
      exports: ["deriveCurrentStep"]
      min_lines: 12
    - path: "src/lib/onboarding/current-step.test.ts"
      provides: "Vitest coverage of deriveCurrentStep (ONB-04)"
      contains: "deriveCurrentStep"
  key_links:
    - from: "src/lib/onboarding/current-step.ts"
      to: "src/lib/onboarding/steps.ts"
      via: "imports the frozen STEPS registry and walks it in order"
      pattern: "import.*STEPS.*from.*@/lib/onboarding/steps"
    - from: "src/lib/onboarding/current-step.ts"
      to: "src/lib/onboarding/completion.ts"
      via: "imports isGymDone/isScheduleDone/isShortcutDone probes for auto-skip"
      pattern: "import.*isGymDone.*from.*@/lib/onboarding/completion"
    - from: "src/lib/supabase/rsc.ts"
      to: "supabase onboarding_progress table"
      via: "admin client read filtered .eq(\"user_id\", user.id)"
      pattern: "onboarding_progress"
---

<objective>
Build the two pure, server-side building blocks the rest of Phase 3 consumes: a request-cached server reader `getOnboardingProgress()` (for no-flash hydration) and a pure `deriveCurrentStep()` helper (the single source of truth for "which step is next", and the unit-testable ONB-04 resume/dismiss seam).

Purpose: This phase is pure wiring against infrastructure that already exists (Phase 1 persistence, Phase 2 step logic). These two artifacts are the only net-new *logic* — everything else (provider, layout, page cleanup) is plumbing on top. Extracting `deriveCurrentStep` as a pure `.ts` (not inlining it in the provider) is what makes ONB-04 unit-covered rather than UAT-only, because Vitest collects `src/**/*.test.ts` only (not `.tsx`).

Output: `getOnboardingProgress()` added to `rsc.ts`, new `src/lib/onboarding/current-step.ts` + co-located `current-step.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-minimal-start-tourprovider-wiring/03-RESEARCH.md
@.planning/phases/03-minimal-start-tourprovider-wiring/03-PATTERNS.md
@.planning/phases/03-minimal-start-tourprovider-wiring/03-CONTEXT.md
</context>

## Artifacts this phase produces

This plan creates these net-new symbols (the convergence source-grounding pass MUST treat these as newly-created, not drift):

- `getOnboardingProgress` — request-cached server reader exported from `src/lib/supabase/rsc.ts`.
- `deriveCurrentStep` — pure step-resolution function exported from `src/lib/onboarding/current-step.ts` (NEW file).
- `src/lib/onboarding/current-step.ts`, `src/lib/onboarding/current-step.test.ts` — NEW file paths.

(Later plans in this phase add: `TourProvider`, `useTour`, `TourValue`, `src/components/tour-provider.tsx`.)

<tasks>

<task type="auto">
  <name>Task 1: Add getOnboardingProgress() request-cached reader to rsc.ts</name>
  <files>src/lib/supabase/rsc.ts</files>
  <read_first>
    - src/lib/supabase/rsc.ts — the file being modified; mirror `getViewerProfile` (lines 21-36) exactly: `cache()` wrapper, `getAuthUser()` for the request-cached user, `createAdminClient()`, return null when no user.
    - src/app/api/onboarding-progress/route.ts — `SELECT_COLS` (line 14) is the exact column list to read; the GET handler (lines 23-27) uses `.eq("user_id", auth.user.id).maybeSingle()` — replicate that filter and `.maybeSingle()`.
    - src/lib/onboarding-progress.ts — `ProgressRow` type (lines 38-45) is the return shape; do NOT redefine it, import it.
    - .planning/phases/03-minimal-start-tourprovider-wiring/03-RESEARCH.md — Security Domain section: the `.eq("user_id", user.id)` filter is the single security-critical line; never widen it.
  </read_first>
  <action>
    Add a new exported `getOnboardingProgress` to `src/lib/supabase/rsc.ts`, wrapped in React `cache()`, following the existing `getViewerProfile` shape in the same file. It MUST: (1) call the existing request-cached `getAuthUser()` and return `null` if there is no user; (2) use `createAdminClient()` (already imported); (3) read `from("onboarding_progress")` selecting exactly these columns: `mandatory_done, tour_version, last_step_id, completed_steps, dismissed, completed_at`; (4) filter strictly with `.eq("user_id", user.id)` — this is SECURITY-CRITICAL (admin client bypasses RLS; the filter is the only thing preventing cross-user reads — see threat T-03-IDOR); never widen it to no filter or another user's id; (5) use `.maybeSingle()` (not `.single()`) so a missing row yields `null` not a throw; (6) `return data ?? null` so any failure / missing row degrades to `null` for D-06 blank-slate handling downstream. Type the return as `ProgressRow | null` by importing `ProgressRow` from `@/lib/onboarding-progress` — do not hand-roll the shape. Add a brief comment marking the `.eq("user_id", user.id)` line as the security-critical owner scope, matching the existing comment style on `getViewerProfile`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/supabase/rsc.ts` contains `export const getOnboardingProgress = cache(`
    - The reader queries `.from("onboarding_progress")` and filters with `.eq("user_id", user.id)` (owner-scoped) and uses `.maybeSingle()`
    - The select column list is exactly `mandatory_done, tour_version, last_step_id, completed_steps, dismissed, completed_at` (matches route.ts SELECT_COLS)
    - The function returns `null` when `getAuthUser()` yields no user and returns `data ?? null` otherwise
    - `ProgressRow` is imported from `@/lib/onboarding-progress` (not redefined locally)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>getOnboardingProgress() exists in rsc.ts, mirrors getViewerProfile, is owner-scoped, returns ProgressRow | null, and type-checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create pure deriveCurrentStep() helper + co-located Vitest</name>
  <files>src/lib/onboarding/current-step.ts, src/lib/onboarding/current-step.test.ts</files>
  <read_first>
    - src/lib/onboarding/steps.ts — the frozen ordered `STEPS` registry (lines 54-60) and each step's optional `probe` field; walk this in order, never hardcode the step list.
    - src/lib/onboarding/completion.ts — `isGymDone(gymCount)`, `isScheduleDone(restDays)`, `isShortcutDone(completedSteps)` (lines 17-39); reuse these for auto-skip, do not re-derive probe logic.
    - src/lib/onboarding/completion.test.ts — test file shape to mirror (lines 1-30): `import { describe, it, expect } from "vitest"`, relative `./current-step` import, one `describe` per behavior.
    - .planning/phases/03-minimal-start-tourprovider-wiring/03-PATTERNS.md — `current-step.ts` pattern assignment (lines 166-198) gives the exact function signature and walk logic.
    - .planning/phases/03-minimal-start-tourprovider-wiring/03-RESEARCH.md — Assumptions A2 / Open Question 1: Phase 3 may pass neutral probe state; keep the function pure and probe-aware so Phase 6 needs no refactor.
  </read_first>
  <behavior>
    - Test 1: `deriveCurrentStep(["gym","challenge","money","shortcut_viewed"], true, neutralProbe)` returns `null` (dismissed short-circuits — D-10 / ONB-04).
    - Test 2: `deriveCurrentStep([], false, neutralProbe)` returns `"schedule"` (first registry step on a fresh, non-dismissed slate).
    - Test 3: resume — `deriveCurrentStep(["schedule","gym"], false, neutralProbe)` returns `"challenge"` (first step not in completed_steps, walking STEPS order).
    - Test 4: complete — `deriveCurrentStep(["schedule","gym","challenge","money","shortcut_viewed"], false, neutralProbe)` returns `null`.
    - Test 5: auto-skip — with `completedSteps: []`, `dismissed: false`, but `probe.gymCount > 0`, the `"gym"` step is skipped (D-09 auto-skip via isGymDone); the first non-skippable pending step is returned instead.
    - (neutralProbe = `{ gymCount: 0, restDays: [], completedSteps: [] }`.)
  </behavior>
  <action>
    Create `src/lib/onboarding/current-step.ts` exporting a pure `deriveCurrentStep(completedSteps: string[], dismissed: boolean, probe: { gymCount: number; restDays: number[]; completedSteps: string[] }): string | null`. Logic: if `dismissed` return `null` immediately (D-10 / ONB-04). Otherwise iterate `STEPS` (imported from `@/lib/onboarding/steps`) in order; for each step: `continue` if its `id` is already in `completedSteps`; `continue` if `step.probe === "gym" && isGymDone(probe.gymCount)`; `continue` if `step.probe === "schedule" && isScheduleDone(probe.restDays)`; `continue` if `step.probe === "shortcut" && isShortcutDone(probe.completedSteps)`; otherwise `return step.id` (first pending, non-skippable). If the loop completes, `return null` (tour complete). Import the three probes from `@/lib/onboarding/completion`; do not mutate any input. Keep it a pure module: no Supabase import, no React, no side effects — mirror `completion.ts`. Then create co-located `src/lib/onboarding/current-step.test.ts` covering the five behaviors above plus an assertion that inputs are not mutated. Use the neutral probe `{ gymCount: 0, restDays: [], completedSteps: [] }` where auto-skip is not under test.
  </action>
  <verify>
    <automated>npx vitest run src/lib/onboarding/current-step.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/onboarding/current-step.ts` contains `export function deriveCurrentStep(`
    - The helper imports `STEPS` from `@/lib/onboarding/steps` and `isGymDone`, `isScheduleDone`, `isShortcutDone` from `@/lib/onboarding/completion`
    - `src/lib/onboarding/current-step.ts` does NOT import from `@/lib/supabase` or from `react`
    - `npx vitest run src/lib/onboarding/current-step.test.ts` exits 0
    - The test file asserts: dismissed → null; fresh slate → "schedule"; `["schedule","gym"]` → "challenge"; all four teaching keys + schedule → null; gym auto-skipped when `probe.gymCount > 0`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>deriveCurrentStep is a pure, probe-aware helper in a .ts file with a passing co-located Vitest suite covering dismiss, fresh-start, resume, complete, and auto-skip (ONB-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user → admin (service-role) DB read | `getOnboardingProgress` uses the service-role client, which bypasses RLS; the query filter is the sole access-control boundary for this read. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-IDOR | Information Disclosure | `getOnboardingProgress` admin-client read in `rsc.ts` | mitigate | Filter strictly `.eq("user_id", user.id)` against the request-cached `getAuthUser()` user; never widen. Mirrors the justified `getViewerProfile` admin-read scope. This is the single security-critical line in the plan. |
| T-03-ELEV | Elevation of Privilege | unauthenticated caller hitting the reader | mitigate | `getAuthUser()` returns null when unauthenticated → reader returns `null` (blank slate), never another user's row. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH: zero new packages); supply-chain surface unchanged from shipped baseline. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (strict mode).
- `npx vitest run src/lib/onboarding/current-step.test.ts` exits 0.
- `npm test` (full pure-logic suite) stays green — steps/completion/merge are already covered and must not regress.
- Manual source check: `getOnboardingProgress` filter reads `.eq("user_id", user.id)` and uses `.maybeSingle()`.
</verification>

<success_criteria>
- getOnboardingProgress() exists, is request-cached, owner-scoped, returns `ProgressRow | null`.
- deriveCurrentStep() is pure, probe-aware, and unit-covered for ONB-04 (dismiss + resume).
- No new packages added; tsc strict passes; full vitest suite green.
</success_criteria>

<output>
Create `.planning/phases/03-minimal-start-tourprovider-wiring/03-01-SUMMARY.md` when done
</output>
