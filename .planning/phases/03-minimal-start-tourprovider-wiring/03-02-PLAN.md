---
phase: 03-minimal-start-tourprovider-wiring
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/tour-provider.tsx
  - src/app/(tabs)/layout.tsx
autonomous: true
requirements: [ONB-01, ONB-04]
must_haves:
  truths:
    - "A user who sets only a username lands directly in the (tabs) app — no onboarding_complete bounce to /onboarding/schedule"
    - "The (tabs) layout redirects to /onboarding/username only when the username is missing or auto-generated, before any nav slot renders"
    - "TourProvider mounts in the layout, hydrates from a server-side read on first paint (no useEffect fetch, no flash), and renders {children} with no wrapper element"
    - "useTour() exposes exactly { currentStepId, isActive, advance, dismiss }; dismiss() persists dismissed:true and immediately yields currentStepId null / isActive false"
    - "Reloading after dismiss or mid-walkthrough resumes correctly because currentStepId re-derives from persisted state via deriveCurrentStep"
  artifacts:
    - path: "src/components/tour-provider.tsx"
      provides: "TourProvider context provider + useTour() hook"
      exports: ["TourProvider", "useTour"]
      contains: "createContext"
      min_lines: 40
    - path: "src/app/(tabs)/layout.tsx"
      provides: "async username-only gate + server hydration read + TourProvider mount"
      contains: "getOnboardingProgress"
  key_links:
    - from: "src/app/(tabs)/layout.tsx"
      to: "src/lib/supabase/rsc.ts"
      via: "awaits getViewerProfile() for the gate and getOnboardingProgress() for hydration"
      pattern: "getOnboardingProgress"
    - from: "src/app/(tabs)/layout.tsx"
      to: "src/components/tour-provider.tsx"
      via: "renders <TourProvider initialProgress={...}>{children}</TourProvider>"
      pattern: "TourProvider initialProgress"
    - from: "src/components/tour-provider.tsx"
      to: "src/lib/onboarding/current-step.ts"
      via: "derives currentStepId via deriveCurrentStep"
      pattern: "deriveCurrentStep"
    - from: "src/components/tour-provider.tsx"
      to: "src/app/api/onboarding-progress/route.ts"
      via: "advance()/dismiss() PATCH the endpoint"
      pattern: "fetch.*api/onboarding-progress"
---

<objective>
Introduce the codebase's first React context provider — `TourProvider` (with `useTour()`) — and wire the `(tabs)/layout.tsx` to (1) gate on username-only, (2) hydrate progress server-side with no flash, and (3) mount the provider around `{children}`. After this plan, a username-set user lands directly in the app, tour state is tracked/persisted, and dismiss/resume work end-to-end.

Purpose: Connects Phase-1 persistence and Phase-2 logic through a thin client provider and a narrowed gate. This is where ONB-01 (land directly), ONB-04 (dismiss + resume), and success criteria 1-4 are realized. The provider deliberately renders NO UI — coachmarks are Phase 4; it only tracks state and exposes the frozen 4-member contract Phase 4+ will consume.

Output: NEW `src/components/tour-provider.tsx`; MODIFIED `src/app/(tabs)/layout.tsx` (becomes async).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-minimal-start-tourprovider-wiring/03-RESEARCH.md
@.planning/phases/03-minimal-start-tourprovider-wiring/03-PATTERNS.md
@.planning/phases/03-minimal-start-tourprovider-wiring/03-UI-SPEC.md
@.planning/phases/03-minimal-start-tourprovider-wiring/03-CONTEXT.md
@src/lib/supabase/rsc.ts
@src/lib/onboarding-progress.ts
@src/components/onboarding/gym-surface.tsx
</context>

## Artifacts this phase produces

This plan creates these net-new symbols (convergence source-grounding pass MUST treat these as newly-created, not drift):

- `TourProvider` — `"use client"` context provider component exported from `src/components/tour-provider.tsx` (NEW file).
- `useTour` — context hook exported from the same file; throws when used outside the provider.
- `TourValue` — the context value type `{ currentStepId, isActive, advance, dismiss }` (frozen by UI-SPEC; no other members).
- `src/components/tour-provider.tsx` — NEW file path.
- `getOnboardingProgress`, `deriveCurrentStep` — consumed here, created in plan 03-01.

<tasks>

<task type="auto">
  <name>Task 1: Create TourProvider context provider + useTour() hook</name>
  <files>src/components/tour-provider.tsx</files>
  <read_first>
    - src/components/onboarding/gym-surface.tsx — the `"use client"` + `useState(initialProp)` + best-effort `fetch`-mutation analog (lines 1, 21, 73-77); seed state from the prop, no `useEffect` fetch.
    - src/lib/onboarding-progress.ts — `ProgressRow` type (38-45) and `defaultProgress()` (54-63) for the D-06 blank slate; reuse both, do not hand-roll.
    - src/lib/onboarding/current-step.ts — `deriveCurrentStep` signature (created in plan 03-01); use it to compute currentStepId.
    - src/app/api/onboarding-progress/route.ts — the PATCH contract; advance sends `{ complete_step, last_step_id }`, dismiss sends `{ dismissed: true }`.
    - src/lib/onboarding-progress.ts — `PatchBody.strict()` (17-29): the PATCH body must contain ONLY allowed keys; never send `completed_steps`.
    - .planning/phases/03-minimal-start-tourprovider-wiring/03-UI-SPEC.md — Interaction Contract (lines 151-184): the exactly-4-member surface, the no-flash hydration contract, the silent-no-op-on-failure rule, and the "deliberately deferred" list (do NOT add isComplete/progress/skip/goTo).
    - .planning/phases/03-minimal-start-tourprovider-wiring/03-PATTERNS.md — tour-provider pattern assignment (99-162) for the canonical context shape and the advance/dismiss write pattern.
  </read_first>
  <action>
    Create `src/components/tour-provider.tsx` with `"use client"` at the top. Define `type TourValue = { currentStepId: string | null; isActive: boolean; advance: (stepId: string) => Promise<void>; dismiss: () => Promise<void> }` — EXACTLY these four members (UI-SPEC freezes this; do NOT add isComplete, progress, skip, or goTo — those are deferred to Phase 4+). Create `const TourContext = createContext<TourValue | null>(null)`. Export `function TourProvider({ initialProgress, children }: { initialProgress: ProgressRow | null; children: React.ReactNode })`. Inside: seed `const [progress, setProgress] = useState<ProgressRow>(initialProgress ?? defaultProgress())` (import `ProgressRow` and `defaultProgress` from `@/lib/onboarding-progress`) — this is the D-06 blank-slate path; do NOT add a `useEffect` fetch (it would defeat the no-flash guarantee). Compute `const currentStepId = useMemo(() => deriveCurrentStep(progress.completed_steps, progress.dismissed, { gymCount: 0, restDays: [], completedSteps: progress.completed_steps }), [progress.completed_steps, progress.dismissed])` — Phase 3 passes neutral gym/schedule probe state (gymCount 0, restDays []) per RESEARCH A2; pass `progress.completed_steps` as the shortcut probe input so the shortcut_viewed key is honored. Define `async function advance(stepId: string)`: optimistically `setProgress((p) => ({ ...p, completed_steps: p.completed_steps.includes(stepId) ? p.completed_steps : [...p.completed_steps, stepId], last_step_id: stepId }))`, then `await fetch("/api/onboarding-progress", { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify({ complete_step: stepId, last_step_id: stepId }) }).catch(() => {})` — send ONLY `complete_step` (single key, never the full array; server `mergeProgress` is authoritative and dedupe-appends) and `last_step_id`; the body must satisfy `PatchBody.strict()`. Define `async function dismiss()`: optimistically `setProgress((p) => ({ ...p, dismissed: true }))` (this makes currentStepId null / isActive false immediately), then `await fetch("/api/onboarding-progress", { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify({ dismissed: true }) }).catch(() => {})`. Both writes are best-effort (optimistic state already applied; the walkthrough is an optional surface — D-08). Build `const value: TourValue = { currentStepId, isActive: currentStepId !== null, advance, dismiss }` and `return <TourContext.Provider value={value}>{children}</TourContext.Provider>` — render `{children}` directly with NO wrapper div/element/padding/margin (UI-SPEC Pitfall 5; the `Provider` is not a DOM node so the (tabs) layout flow is unaffected). Export `function useTour()` that reads `useContext(TourContext)` and throws `new Error("useTour must be used within TourProvider")` if null. This file renders no coachmark, overlay, spotlight, tooltip, or skip/X button (that is Phase 4).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/tour-provider.tsx` begins with `"use client"` and contains `createContext`
    - It exports `TourProvider` and `useTour`
    - The context value type has EXACTLY four members: `currentStepId`, `isActive`, `advance`, `dismiss` (no `isComplete`, no `progress`, no `skip`, no `goTo`)
    - `useState` is seeded from `initialProgress ?? defaultProgress()`; there is NO `useEffect` that fetches progress
    - `advance` PATCHes a body containing only `complete_step` and `last_step_id`; `dismiss` PATCHes a body containing only `dismissed`; neither sends `completed_steps`
    - The provider's return renders `{children}` with no wrapping DOM element (returns `<TourContext.Provider ...>{children}</TourContext.Provider>` directly)
    - `useTour` throws when called outside the provider
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>TourProvider + useTour exist with the frozen 4-member contract, no-flash hydration from initialProgress, optimistic best-effort PATCH writes, and no rendered UI.</done>
</task>

<task type="auto">
  <name>Task 2: Wire (tabs)/layout.tsx — async gate + hydration read + TourProvider mount</name>
  <files>src/app/(tabs)/layout.tsx</files>
  <read_first>
    - src/app/(tabs)/layout.tsx — the file being modified; it is currently a SYNCHRONOUS function (line 37) and must become `async`. Note the existing `getNavProfile()` helper (11-15), the two Suspense nav slots, the RefreshOnFocus, and the aria-hidden header spacer (52-55) — all preserved unchanged.
    - src/app/onboarding/username/page.tsx — `isAutoUsername` definition (lines 11-13): `!u || /^user_[a-f0-9]{8}$/.test(u)`. Reuse this regex (one definition); do not invent a new check.
    - src/lib/supabase/rsc.ts — `getViewerProfile` (request-cached; the gate reuses it, zero extra round trip) and `getOnboardingProgress` (added in plan 03-01; the hydration read).
    - src/components/tour-provider.tsx — the `TourProvider` created in Task 1; its prop is `initialProgress: ProgressRow | null`.
    - .planning/phases/03-minimal-start-tourprovider-wiring/03-RESEARCH.md — Pitfalls 1, 2, 5 (direct read NOT self-fetch; gate before Suspense; no wrapper element) and the Code Examples "Narrowed, centralized gate in the async layout" (402-421).
    - .planning/phases/03-minimal-start-tourprovider-wiring/03-CONTEXT.md — D-01, D-02, D-04, D-05, D-06.
  </read_first>
  <action>
    Convert `TabsLayout` to `async`. At the top of the component body, before returning any JSX: `const profile = await getViewerProfile();` then `if (!profile) redirect("/login");` then `if (isAutoUsername(profile.username)) redirect("/onboarding/username");` — this is the ONLY username gate going forward (D-01, ONB-02). Do NOT add any `onboarding_complete` check (D-02 — the wizard bounce is removed). Add the `isAutoUsername` helper to this file by copying the definition from `src/app/onboarding/username/page.tsx:11-13` (`function isAutoUsername(u: string | null) { return !u || /^user_[a-f0-9]{8}$/.test(u); }`) — keep the regex identical (`/^user_[a-f0-9]{8}$/`). After the gate, hydrate: `const initialProgress = await getOnboardingProgress();` — this is a direct request-cached DB read (RESEARCH A1 / Pitfall 1); do NOT make the RSC `fetch("/api/onboarding-progress")` (no precedent; needs absolute URL + cookie forwarding). On failure the reader already returns `null`, which is the D-06 blank slate. Import `getOnboardingProgress` from `@/lib/supabase/rsc` and `TourProvider` from `@/components/tour-provider`. In the returned JSX, replace the bare `{children}` at line 56 with `<TourProvider initialProgress={initialProgress}>{children}</TourProvider>` (D-05) — mounted between the header spacer and the BottomBar Suspense slot. Do NOT wrap it in any extra div. Preserve verbatim: `RefreshOnFocus`, both `<Suspense>` nav slots (`TopBar`/`BottomBar`), the `aria-hidden` header-height spacer (52-55), and the `export const dynamic = "force-dynamic"`. The existing `getNavProfile()` helper and the `TopBar`/`BottomBar` components stay as-is (they call `getViewerProfile()`, which is request-cached, so the gate's read is shared — no extra round trip).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `export default async function TabsLayout` (the function is now async)
    - The layout body calls `await getViewerProfile()` and redirects to `/login` when null and to `/onboarding/username` when `isAutoUsername(profile.username)` is true, before the JSX return
    - The layout contains NO `onboarding_complete` reference and NO `redirect("/onboarding/schedule")`
    - `isAutoUsername` is defined with the exact regex `/^user_[a-f0-9]{8}$/`
    - The layout calls `await getOnboardingProgress()` (a direct reader import) and does NOT call `fetch("/api/onboarding-progress")` anywhere
    - `process.env.NEXT_PUBLIC_SITE_URL` does NOT appear in the layout (no self-fetch absolute URL)
    - The JSX renders `<TourProvider initialProgress={initialProgress}>{children}</TourProvider>` with no wrapping element; `RefreshOnFocus`, both Suspense nav slots, and the aria-hidden spacer are still present
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>(tabs)/layout.tsx is async, gates on username-only, hydrates progress via a direct request-cached read (no self-fetch, no flash), and mounts TourProvider around {children} with no wrapper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (TourProvider) → PATCH /api/onboarding-progress | advance()/dismiss() send progress writes; the route is the server-authoritative boundary. |
| browser navigation → RSC layout gate | The username gate runs server-side before any client render; bypassing it client-side cannot reach a gated route's server output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-FORGE | Tampering / Elevation | client-forged progress via advance()/dismiss() | mitigate | Writes go through the existing Zod-`PatchBody.strict()` route; the handler pins `user_id` to `auth.user.id` and runs server-authoritative `mergeProgress` (Phase 1). The client sends only `complete_step`/`last_step_id`/`dismissed`; `completed_steps` is never client-supplied. Owner-only RLS backs the upsert. |
| T-03-SMUGGLE | Tampering | extra columns smuggled in PATCH body | mitigate | `PatchBody.strict()` rejects unknown fields (Phase 1). Provider bodies contain only allowed keys. |
| T-03-GATE | (functional, not security) | gate divergence leaving a user wizard-trapped | mitigate | Single async layout gate (D-01); no per-page divergence. Username-only redirect; no onboarding_complete bounce (D-02). |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH: zero new packages). |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (strict; catches possibly-null and missing-await).
- `npm test` stays green (no pure-logic regression).
- Grep check: the layout contains no `onboarding_complete`, no `redirect("/onboarding/schedule")`, no `fetch("/api/onboarding-progress")`, no `NEXT_PUBLIC_SITE_URL`.
- Grep check: `src/components/tour-provider.tsx` PATCH bodies contain only `complete_step`/`last_step_id`/`dismissed` (never `completed_steps`).
- UAT (success criteria 1, 3, 4): a username-set, onboarding_complete=false user lands on the dashboard (not /onboarding/schedule); first paint shows no flash; dismiss persists across reload (no re-prompt).
</verification>

<success_criteria>
- A username-only user lands directly in the (tabs) app (ONB-01, criterion 1).
- The layout gate redirects only on missing/auto username (criterion 2 — completed alongside plan 03-03 page cleanup).
- TourProvider mounts, hydrates with no flash, persists advancement, resumes on reload (criterion 3).
- dismiss() persists dismissed:true; reload does not re-prompt (ONB-04, criterion 4).
- useTour() exposes exactly the frozen 4-member contract.
</success_criteria>

<output>
Create `.planning/phases/03-minimal-start-tourprovider-wiring/03-02-SUMMARY.md` when done
</output>
