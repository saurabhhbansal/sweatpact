---
phase: 09-admin-dashboard
plan: 04
type: execute
wave: 2
depends_on:
  - 09-02
files_modified:
  - src/components/admin/financial-overview.tsx
  - src/components/admin/user-overview.tsx
  - src/components/admin/range-control.tsx
  - src/components/admin/checkin-trend-chart.tsx
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
must_haves:
  truths:
    - "FinancialOverview renders active pact count, total stakes (formatCents), total penalties (formatCents), and settlement completion rate as a percentage from props"
    - "UserOverview renders total registered, onboarding-completed, with-active-pact, and checked-in-this-week counts from props"
    - "RangeControl renders three Links (7d/30d/90d) to /admin?range=... and visually marks the current range"
    - "CheckinTrendChart is a client component rendering a recharts multi-series LineChart from WeekBucket[] props (verified, geo-fail, total)"
    - "All four components receive data via serializable props and never fetch (no Supabase/PostHog calls inside components)"
  artifacts:
    - path: "src/components/admin/financial-overview.tsx"
      provides: "DASH-01 KPI card"
      exports: ["FinancialOverview"]
    - path: "src/components/admin/user-overview.tsx"
      provides: "DASH-03 KPI card"
      exports: ["UserOverview"]
    - path: "src/components/admin/range-control.tsx"
      provides: "DASH-02 7d/30d/90d segmented Link control"
      exports: ["RangeControl"]
    - path: "src/components/admin/checkin-trend-chart.tsx"
      provides: "DASH-02 recharts client line chart"
      exports: ["CheckinTrendChart"]
    - path: "package.json"
      provides: "recharts dependency"
      contains: "recharts"
  key_links:
    - from: "src/components/admin/checkin-trend-chart.tsx"
      to: "recharts"
      via: "LineChart/Line/XAxis/YAxis/Tooltip/ResponsiveContainer/CartesianGrid imports in a \"use client\" island"
      pattern: "from [\"']recharts[\"']"
    - from: "src/components/admin/checkin-trend-chart.tsx"
      to: "src/lib/admin-metrics.ts"
      via: "imports the WeekBucket type for its data prop"
      pattern: "WeekBucket"
    - from: "src/components/admin/financial-overview.tsx"
      to: "src/lib/money.ts"
      via: "formatCents for stakes/penalties display"
      pattern: "formatCents"
---

<objective>
Build the four Supabase-backed presentation components for DASH-01/02/03, plus install `recharts`. These are thin, props-only views: `FinancialOverview` (DASH-01 KPIs), `UserOverview` (DASH-03 KPIs), `RangeControl` (DASH-02 date-range Links), and `CheckinTrendChart` (DASH-02 recharts client island). All rendering logic lives here so the page (Plan 06) only fetches data and passes props.

Purpose: Isolating the recharts client island and the KPI cards keeps the RSC page server-only and the chart hydration-correct (recharts cannot render in an RSC).
Output: 4 components in `src/components/admin/` + `recharts` in package.json. Visual correctness is verified at phase UAT; build/typecheck verifies compilation here.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-admin-dashboard/09-UI-SPEC.md
@.planning/phases/09-admin-dashboard/09-PATTERNS.md
@.planning/phases/09-admin-dashboard/09-RESEARCH.md

# Source-of-truth analogs
@src/app/(tabs)/dashboard/page.tsx
@src/components/ui/card.tsx
@src/lib/money.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: FinancialOverview + UserOverview KPI cards (DASH-01, DASH-03)</name>
  <files>src/components/admin/financial-overview.tsx, src/components/admin/user-overview.tsx</files>
  <read_first>
    - src/app/(tabs)/dashboard/page.tsx (KPI/ledger display blocks lines 134-253: glass-card, text-xs eyebrow, font-bold metric, formatCents usage)
    - src/components/ui/card.tsx (Card/CardHeader/CardTitle/CardContent API and rounded-[2rem] glass-card)
    - src/lib/money.ts (formatCents)
    - .planning/phases/09-admin-dashboard/09-UI-SPEC.md (Typography: Display metric text-3xl/700; Color: white accent for headline numbers, success/destructive semantics; Spacing)
  </read_first>
  <action>
    Create `src/components/admin/financial-overview.tsx` as an RSC component (NO `"use client"`; pure props, no hooks). Props interface `FinancialOverviewProps { activePactCount: number; totalStakesCents: number; totalPenaltiesCents: number; settlementRate: number /* 0..1 */ }`. Render a `Card` (`rounded-[2rem] glass-card`, import from `@/components/ui/card`) titled "Financial overview" with four metrics: active pacts (count), total stakes on the line (`formatCents(totalStakesCents)` from `@/lib/money`), total penalties issued (`formatCents(totalPenaltiesCents)`), settlement completion rate (render `settlementRate` as a whole-number percentage, e.g. `Math.round(settlementRate * 100) + "%"`). Use the established type scale from UI-SPEC: metric figures `text-3xl font-bold text-white leading-tight`, labels `text-xs uppercase tracking-[0.08em] text-white/55`. Create `src/components/admin/user-overview.tsx` the same way: props `UserOverviewProps { totalUsers: number; onboardedUsers: number; usersWithActivePact: number; checkedInThisWeek: number }`, a `Card` titled "User overview" with the four labeled counts. Both components are presentational only — no data fetching, no async.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Neither file contains `"use client"` (they are RSC prop components)
    - `financial-overview.tsx` imports `formatCents` from `@/lib/money` and `Card` from `@/components/ui/card`
    - Settlement rate is rendered as a percentage (source contains `* 100`)
    - `npm run typecheck` passes; `npm run lint` reports no errors for the two files
  </acceptance_criteria>
  <done>FinancialOverview and UserOverview render all four DASH-01 / DASH-03 metrics from props using brand tokens; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: RangeControl + CheckinTrendChart + install recharts (DASH-02)</name>
  <files>src/components/admin/range-control.tsx, src/components/admin/checkin-trend-chart.tsx, package.json, package-lock.json</files>
  <read_first>
    - src/lib/admin-metrics.ts (WeekBucket type shape — the chart's data prop)
    - src/app/(tabs)/dashboard/page.tsx (Link navigation pattern, lines 222-251)
    - .planning/phases/09-admin-dashboard/09-RESEARCH.md (Pattern 4 recharts island; "Standard Stack" recharts 3.9.0; Package Legitimacy Audit — recharts approved)
    - .planning/phases/09-admin-dashboard/09-UI-SPEC.md (Chart palette: total=white, verified=success hsl(var(--success)), geo-fail=destructive; tooltip glass-card; date-range pill active = bg-white text-black, >=36px tall)
  </read_first>
  <action>
    Run `npm install recharts` (recharts 3.9.0, audit-approved in RESEARCH — React-18 compatible, `react-is` peer already present). Create `src/components/admin/range-control.tsx` as a server-safe component using `Link` from `next/link`: props `{ current: "7d" | "30d" | "90d" }`; map over `["7d","30d","90d"] as const` rendering a `Link` to `/admin?range=${r}`; the active pill uses `bg-white text-black` and inactive uses `text-white/55 hover:text-white`; each pill is `>= h-9` (36px) per UI-SPEC touch target. No client state — selection is the `?range` searchParam. Create `src/components/admin/checkin-trend-chart.tsx` starting with `"use client";` and importing `{ LineChart, Line, XAxis, YAxis, Tooltip, ResponsiveContainer, CartesianGrid }` from `recharts`. Import the `WeekBucket` type from `@/lib/admin-metrics`. Props `{ data: WeekBucket[] }`. Render a `ResponsiveContainer width="100%" height={260}` wrapping a `LineChart` over `data` with `XAxis dataKey="week"`, `YAxis`, `CartesianGrid`, `Tooltip`, and three `Line` series keyed `total` (stroke white `hsl(0 0% 100%)`), `verified` (stroke `hsl(var(--success))`), and `geoFail` (stroke `hsl(var(--destructive))`), each `dot={false}`. Pass data as serializable props only — never fetch inside this component (Pitfall 6: recharts is client-only and must receive precomputed props).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies include `recharts` (grep `recharts`)
    - `checkin-trend-chart.tsx` line 1 is `"use client";` and it imports from `recharts` and imports `WeekBucket` from `@/lib/admin-metrics`
    - The chart defines three `Line` series with dataKeys `total`, `verified`, `geoFail`
    - `range-control.tsx` renders `Link` elements to `/admin?range=7d`, `/admin?range=30d`, `/admin?range=90d`
    - `npm run build` succeeds (the recharts client island compiles in the App Router build); `npm run typecheck` passes
  </acceptance_criteria>
  <done>recharts is installed; RangeControl (Links) and CheckinTrendChart (client recharts multi-series) compile in the production build and render the three DASH-02 series from WeekBucket props.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | A new dependency (recharts) enters the supply chain |
| RSC props → client island | Only serializable, server-computed data crosses into the chart |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-SC | Tampering | `npm install recharts` | mitigate | recharts is audit-approved in RESEARCH Package Legitimacy Audit (OK verdict, ~9 yrs, multi-million weekly downloads, shadcn-official). Not [ASSUMED]/[SUS]/[SLOP] — no blocking checkpoint required |
| T-09-11 | Information Disclosure | chart props | mitigate | Chart receives only aggregated WeekBucket counts as props; no per-user identifiers serialized to the client island |
</threat_model>

## Artifacts this phase produces

New symbols/files created by this plan:
- File `src/components/admin/financial-overview.tsx` — `FinancialOverview(props)` RSC card (DASH-01)
- File `src/components/admin/user-overview.tsx` — `UserOverview(props)` RSC card (DASH-03)
- File `src/components/admin/range-control.tsx` — `RangeControl({ current })` Link control (DASH-02)
- File `src/components/admin/checkin-trend-chart.tsx` — `CheckinTrendChart({ data })` `"use client"` recharts line chart (DASH-02)
- Dependency: `recharts` added to `package.json` / `package-lock.json`

<verification>
- `npm run build` succeeds with the recharts client island
- `npm run typecheck` and `npm run lint` pass for the four components
- Grep confirms `recharts` in package.json and `"use client"` at top of `checkin-trend-chart.tsx`
- Visual correctness (brand tokens, chart colors, layout) deferred to phase UAT (no RTL harness in repo)
</verification>

<success_criteria>
The four Supabase-backed presentation components exist and compile; the recharts dependency is installed and audit-clean. Plan 06 can import these and feed them server-computed props.
</success_criteria>

<output>
Create `.planning/phases/09-admin-dashboard/09-04-SUMMARY.md` when done.
</output>
