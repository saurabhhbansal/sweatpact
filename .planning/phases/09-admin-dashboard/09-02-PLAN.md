---
phase: 09-admin-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/admin-metrics.ts
  - src/lib/admin-metrics.test.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
must_haves:
  truths:
    - "settlementRate(settled, pending) returns 0 when settled+pending is 0, otherwise settled/(settled+pending)"
    - "activePactCount counts groups that have at least 2 members (a live pact = a group with >=2 group_members)"
    - "totalStakesCents sums each active group's default_penalty_cents across active pacts"
    - "rangeToDays maps '7d'->7, '30d'->30, '90d'->90, and any other value->30 (default)"
    - "bucketCheckinsByWeek groups checkin rows into ISO-week buckets keyed by isoWeekMonday(local_day) with verified/unverified/manual/shortcut/total counts"
    - "mergeGeoFailByWeek attaches a PostHog geo-fail count to the matching week bucket and leaves non-matching weeks at 0"
  artifacts:
    - path: "src/lib/admin-metrics.ts"
      provides: "pure financial + trend + user-overview helpers and the WeekBucket type"
      exports: ["settlementRate", "activePactCount", "totalStakesCents", "usersWithActivePact", "rangeToDays", "rangeStartDay", "bucketCheckinsByWeek", "mergeGeoFailByWeek", "WeekBucket"]
    - path: "src/lib/admin-metrics.test.ts"
      provides: "unit tests for every pure helper, including zero/empty edge cases"
  key_links:
    - from: "src/lib/admin-metrics.ts"
      to: "src/lib/derived-status.ts"
      via: "reuse isoWeekMonday(day) for ISO-week bucketing (no new date math)"
      pattern: "isoWeekMonday"
    - from: "src/lib/admin-metrics.ts"
      to: "src/lib/money.ts"
      via: "re-export formatCents for admin financial display"
      pattern: "formatCents"
---

<objective>
Build the pure, testable computation layer for the three Supabase-backed panels: financial overview (DASH-01), check-in trend buckets (DASH-02), and user overview (DASH-03). All cross-source merging and aggregation math lives here so the RSC page (Plan 06) stays a thin orchestrator and the logic is unit-tested.

Purpose: Financial correctness is server-authoritative — these helpers compute the figures an owner trusts. Extracting them into pure functions is what makes DASH-01/02/03 verifiable without a DB.
Output: `src/lib/admin-metrics.ts` (helpers + `WeekBucket` type) + co-located tests. No DB calls, no React.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-admin-dashboard/09-RESEARCH.md
@.planning/phases/09-admin-dashboard/09-PATTERNS.md

# Source-of-truth analogs
@src/lib/money.ts
@src/lib/derived-status.ts
@src/lib/checkin-reconciliation.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Financial + user-overview helpers (DASH-01, DASH-03)</name>
  <files>src/lib/admin-metrics.ts, src/lib/admin-metrics.test.ts</files>
  <read_first>
    - src/lib/money.ts (formatCents signature to re-export; cents are integers)
    - src/lib/checkin-reconciliation.test.ts (vitest test structure)
    - .planning/phases/09-admin-dashboard/09-RESEARCH.md ("Open Questions" Q1 active-pact/stake; "Code Examples" settlementRate; Assumptions A1/A2/A5)
  </read_first>
  <behavior>
    - settlementRate(0, 0) === 0
    - settlementRate(3, 1) === 0.75
    - settlementRate(2, 0) === 1
    - activePactCount([{group_id:"g1"},{group_id:"g1"},{group_id:"g2"}]) === 1 (only g1 has >=2 members)
    - activePactCount([]) === 0
    - totalStakesCents over active groups sums each active group's default_penalty_cents once (e.g. g1 active @ 5000 -> 5000; g2 with 1 member excluded)
    - usersWithActivePact counts distinct user_ids that belong to a group with >=2 members
  </behavior>
  <action>
    Create `src/lib/admin-metrics.ts` (no `"use client"`; pure module). Re-export `formatCents` from `@/lib/money` for admin display reuse (PATTERNS shared pattern). Implement and export:
    (1) `settlementRate(settled: number, pending: number): number` — `const total = settled + pending; return total === 0 ? 0 : settled / total;` This is penalties settled / penalties owed where owed = settled + pending obligation rows; disputed/voided are excluded from the denominator (document this in a one-line comment, per RESEARCH A5).
    (2) `activePactCount(memberRows: Array<{ group_id: string }>): number` — group member rows by `group_id`, count groups whose membership count is `>= 2`. A live pact is structurally a `groups` row with two `group_members` (RESEARCH Open Q1 / A1; schema `group_members` PK is `(group_id,user_id)` with `unique(user_id)`).
    (3) `totalStakesCents(activeGroups: Array<{ default_penalty_cents: number }>): number` — sum `default_penalty_cents` across the active groups (RESEARCH A2: stake source is the group's configured penalty). Each active group contributes its stake once.
    (4) `usersWithActivePact(memberRows: Array<{ group_id: string; user_id: string }>): number` — count distinct `user_id` across rows whose `group_id` is in the active-group set (>=2 members) (DASH-03).
    Add a `describe` block per helper in `admin-metrics.test.ts` covering the behavior cases plus the zero/empty edges. NOTE: the active-pact definition (group with >=2 members; stake = default_penalty_cents) is the documented planner resolution of RESEARCH Open Q1 — flag it in a code comment so UAT confirms it matches owner intent.
  </action>
  <verify>
    <automated>npx vitest run src/lib/admin-metrics.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `settlementRate`, `activePactCount`, `totalStakesCents`, `usersWithActivePact` are all exported and pure
    - `settlementRate(0,0)` returns `0` (no divide-by-zero) — asserted in tests
    - `activePactCount` excludes single-member groups — asserted
    - `npx vitest run src/lib/admin-metrics.test.ts` exits 0; `npm run typecheck` passes
  </acceptance_criteria>
  <done>DASH-01 financial math and DASH-03 active-pact user count exist as tested pure helpers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Range + ISO-week trend bucketing (DASH-02)</name>
  <files>src/lib/admin-metrics.ts, src/lib/admin-metrics.test.ts</files>
  <read_first>
    - src/lib/admin-metrics.ts (the file from Task 1)
    - src/lib/derived-status.ts (isoWeekMonday signature — reuse it; do NOT write new week math)
    - src/app/(tabs)/dashboard/page.tsx (how checkin_events rows are shaped: local_day, status, source)
    - .planning/phases/09-admin-dashboard/09-RESEARCH.md (Pitfall 1: geo-fail is NOT in Supabase — third series is a PostHog merge)
  </read_first>
  <behavior>
    - rangeToDays("7d") === 7; rangeToDays("90d") === 90; rangeToDays("30d") === 30; rangeToDays("bogus") === 30
    - rangeStartDay(todayIso, 7) returns the YYYY-MM-DD that is 6 days before today inclusive-window (document the convention)
    - bucketCheckinsByWeek([]) === []
    - bucketCheckinsByWeek with two verified rows in the same ISO week yields one WeekBucket with verified:2, total:2
    - bucketCheckinsByWeek splits source into manual vs shortcut counts; unverified counted separately
    - mergeGeoFailByWeek(buckets, [{week, count}]) sets geoFail on the matching week and 0 elsewhere
  </behavior>
  <action>
    Extend `src/lib/admin-metrics.ts`. Export a `WeekBucket` type with fields `week: string` (the isoWeekMonday key, e.g. "2026-06-22"), `verified: number`, `unverified: number`, `manual: number`, `shortcut: number`, `geoFail: number`, `total: number`. Implement:
    (1) `rangeToDays(range: string): 7 | 30 | 90` — `"7d"->7`, `"90d"->90`, default `30` (RESEARCH/CONTEXT default 30d). The `?range` searchParam is Zod-validated at the page boundary; this maps the validated value to a day count.
    (2) `rangeStartDay(today: string, days: number): string` — compute the inclusive window start YYYY-MM-DD `days` back from `today`; reuse date utilities already in the repo where possible and document the inclusive/exclusive convention in a comment.
    (3) `bucketCheckinsByWeek(rows: Array<{ local_day: string; status: "verified" | "unverified"; source: "shortcut" | "manual" | "admin" }>): WeekBucket[]` — bucket each row under `isoWeekMonday(local_day)` (imported from `@/lib/derived-status`), incrementing `verified`/`unverified` by status, `manual`/`shortcut` by source, and `total`; initialize `geoFail` to 0; return buckets sorted ascending by `week`.
    (4) `mergeGeoFailByWeek(buckets: WeekBucket[], geoFailRows: Array<{ week: string; count: number }>): WeekBucket[]` — set each bucket's `geoFail` from the geoFailRows entry whose `week` matches; weeks with no geo-fail row stay at 0. This is the seam that joins the Supabase trend with the PostHog `checkin:geo_failed` series (Pitfall 1 — geo-fail is never written to `checkin_events`, so it is sourced from PostHog by Plan 03 and merged here; this delivers DASH-02's geo-fail series WITHOUT dropping it).
    Add tests for all four helpers in `admin-metrics.test.ts`, including the empty-array and single-week cases from behavior.
  </action>
  <verify>
    <automated>npx vitest run src/lib/admin-metrics.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bucketCheckinsByWeek` imports `isoWeekMonday` from `@/lib/derived-status` (no hand-rolled week math)
    - `WeekBucket` includes a `geoFail` field initialized to 0 by the bucketer
    - `rangeToDays` returns 30 for unknown input (asserted)
    - `npx vitest run src/lib/admin-metrics.test.ts` exits 0; `npm run typecheck` passes
  </acceptance_criteria>
  <done>DASH-02 trend bucketing + range mapping + geo-fail merge seam exist as tested pure helpers reusing isoWeekMonday.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `?range` searchParam → metrics | User-supplied range string must not reach a query as raw input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Tampering | rangeToDays input | mitigate | `?range` is Zod-enum validated at the page boundary (Plan 06); `rangeToDays` maps only to fixed integers 7/30/90 — no interpolation of raw input |
| T-09-06 | Tampering | financial figures | mitigate | All financial math is pure and unit-tested; figures are computed server-side from service-role aggregates, never client-supplied |
</threat_model>

## Artifacts this phase produces

New symbols/files created by this plan:
- File `src/lib/admin-metrics.ts`
  - `settlementRate(settled, pending): number` (DASH-01)
  - `activePactCount(memberRows): number` (DASH-01)
  - `totalStakesCents(activeGroups): number` (DASH-01)
  - `usersWithActivePact(memberRows): number` (DASH-03)
  - `rangeToDays(range): 7|30|90` (DASH-02)
  - `rangeStartDay(today, days): string` (DASH-02)
  - `bucketCheckinsByWeek(rows): WeekBucket[]` (DASH-02)
  - `mergeGeoFailByWeek(buckets, geoFailRows): WeekBucket[]` (DASH-02)
  - `type WeekBucket` (consumed by the chart in Plan 04 and the page in Plan 06)
  - re-export `formatCents` from `@/lib/money`
- File `src/lib/admin-metrics.test.ts` — unit tests for all helpers

<verification>
- `npx vitest run src/lib/admin-metrics.test.ts` exits 0
- `npm run typecheck` passes
- `bucketCheckinsByWeek` reuses `isoWeekMonday` (grep confirms import from `@/lib/derived-status`)
</verification>

<success_criteria>
DASH-01/02/03 computation is fully extracted into tested pure helpers with a shared `WeekBucket` type. The page (Plan 06) and chart (Plan 04) can import these without re-implementing math.
</success_criteria>

<output>
Create `.planning/phases/09-admin-dashboard/09-02-SUMMARY.md` when done.
</output>
