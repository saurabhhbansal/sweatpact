---
phase: 06-skip-on-complete-replay-completion-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/onboarding-progress.ts
  - src/lib/onboarding-progress.test.ts
  - src/app/(tabs)/settings/client.tsx
autonomous: true
requirements: [PROG-03]
must_haves:
  truths:
    - "From Settings, the user can tap a 'Replay app tour' control that reactivates the walkthrough"
    - "Replay sets dismissed=false WITHOUT clearing completed_steps, so auto-skip fast-forwards already-done steps (D-04)"
    - "The existing PATCH /api/onboarding-progress endpoint is reused — no new endpoint, the .strict() Zod schema stays valid for existing callers (D-06)"
  artifacts:
    - path: "src/lib/onboarding-progress.ts"
      provides: "PatchBody extended with replay: z.literal(true).optional(); mergeProgress sets dismissed:false on replay"
      contains: "replay"
    - path: "src/lib/onboarding-progress.test.ts"
      provides: "Vitest coverage: replay parses, replay sets dismissed false, replay preserves completed_steps, existing callers unaffected"
    - path: "src/app/(tabs)/settings/client.tsx"
      provides: "Replay app tour control that PATCHes { replay: true } and router.refresh()es"
      contains: "Replay app tour"
  key_links:
    - from: "src/app/(tabs)/settings/client.tsx"
      to: "src/app/api/onboarding-progress/route.ts"
      via: "fetch PATCH /api/onboarding-progress with body { replay: true }"
      pattern: "onboarding-progress"
    - from: "src/lib/onboarding-progress.ts"
      to: "src/app/api/onboarding-progress/route.ts"
      via: "PatchBody (replay field) + mergeProgress (dismissed:false on replay) consumed by the PATCH handler"
      pattern: "replay"
---

<objective>
Add the "Replay app tour" entry point in Settings (PROG-03). A ghost control in
the existing `SettingsForm` calls the existing `PATCH /api/onboarding-progress`
with a new `{ replay: true }` body flag. The Phase-1 Zod schema (`PatchBody`) is
minimally extended with `replay: z.literal(true).optional()` (keeps `.strict()`,
keeps every existing caller valid), and `mergeProgress` interprets `replay: true`
as "set `dismissed: false`, leave `completed_steps` untouched" (D-04: replay
re-enters the tour from the first not-done step; auto-skip from Plan 01
fast-forwards already-done steps). After the PATCH resolves, `router.refresh()`
re-hydrates the layout RSC's `initialProgress`, which flips the tour back to
active.

Purpose: Satisfy PROG-03 and Phase 6 success criterion 2 (user can replay from
Settings; replay re-derives completion gracefully without resetting progress).
Output: Extended Zod schema + merge logic with Vitest; a brand-correct ghost
control in SettingsForm wired to the existing endpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-skip-on-complete-replay-completion-hardening/06-CONTEXT.md
@.planning/phases/06-skip-on-complete-replay-completion-hardening/06-PATTERNS.md
@.planning/phases/06-skip-on-complete-replay-completion-hardening/06-UI-SPEC.md

# Files this plan modifies — read current state before editing
@src/lib/onboarding-progress.ts
@src/lib/onboarding-progress.test.ts
@src/app/(tabs)/settings/client.tsx
@src/app/api/onboarding-progress/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend PatchBody + mergeProgress for the replay signal, with Vitest coverage</name>
  <files>src/lib/onboarding-progress.ts, src/lib/onboarding-progress.test.ts</files>
  <read_first>
    - src/lib/onboarding-progress.ts (the file being modified — note PatchBody is a z.object({...}).strict(); mergeProgress is a pure function returning the row to upsert; dismissed is currently passed through only when patch.dismissed is defined)
    - src/lib/onboarding-progress.test.ts (the file being modified — note existing PatchBody and mergeProgress describe blocks; follow the blankRow() helper and assertion style)
    - src/app/api/onboarding-progress/route.ts (the PATCH handler calls mergeProgress(existing, parsed.data) then upserts — confirming mergeProgress is the single seam where replay logic belongs)
    - .planning/phases/06-skip-on-complete-replay-completion-hardening/06-CONTEXT.md (D-04: replay keeps completed_steps intact, auto-skip applies; D-06: reuse the PATCH endpoint, keep the Zod schema valid)
  </read_first>
  <behavior>
    - Test: PatchBody.safeParse({ replay: true }).success === true
    - Test: PatchBody.safeParse({ replay: false }).success === false (z.literal(true) rejects false — replay is one-way)
    - Test: PatchBody.safeParse({ replay: true, complete_step: "gym" }).success === true (replay composes with existing fields)
    - Test: existing callers unaffected — PatchBody.safeParse({ complete_step: "gym" }).success === true still holds
    - Test: mergeProgress(blankRow({ dismissed: true, completed_steps: ["schedule","gym"] }), { replay: true }) returns dismissed === false AND completed_steps still equals ["schedule","gym"] (no reset, D-04)
    - Test: mergeProgress(blankRow({ dismissed: true }), {}) leaves dismissed === true (replay is opt-in; absence does not reactivate)
  </behavior>
  <action>
    In src/lib/onboarding-progress.ts, add one field to the PatchBody object literal: `replay: z.literal(true).optional(),`. Keep the trailing `.strict()` chained call exactly as-is. The `PatchInput` type infers automatically. In `mergeProgress`, after computing `completed_steps` and before/within the returned object, derive the dismissed value so that when `patch.replay === true` the merged `dismissed` is `false` (replay reactivates the tour), otherwise fall back to the existing rule (`patch.dismissed !== undefined ? patch.dismissed : existing.dismissed`). Do NOT touch `completed_steps` on replay (D-04 — auto-skip from Plan 01 handles fast-forward; resetting would re-teach done steps). Then append new test cases to src/lib/onboarding-progress.test.ts covering every assertion in <behavior>, using the existing blankRow() helper. Do not remove or alter existing assertions.
  </action>
  <verify>
    <automated>cd "D:/Code Projects/Gym Tracker" && npx vitest run src/lib/onboarding-progress.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/onboarding-progress.ts PatchBody contains `replay: z.literal(true).optional()` and still ends with `.strict()`
    - mergeProgress returns `dismissed: false` when `patch.replay === true`, and preserves `completed_steps` unchanged on replay
    - `npx vitest run src/lib/onboarding-progress.test.ts` passes all existing + new cases
    - New test asserts `mergeProgress(blankRow({ dismissed: true, completed_steps: ["schedule","gym"] }), { replay: true }).completed_steps` equals `["schedule","gym"]`
    - New test asserts `PatchBody.safeParse({ replay: false }).success` is `false`
  </acceptance_criteria>
  <done>PatchBody accepts replay:true (one-way), mergeProgress reactivates the tour without resetting completed_steps, and Vitest proves both plus existing-caller compatibility.</done>
</task>

<task type="auto">
  <name>Task 2: Add the "Replay app tour" ghost control to SettingsForm wired to the PATCH endpoint</name>
  <files>src/app/(tabs)/settings/client.tsx</files>
  <read_first>
    - src/app/(tabs)/settings/client.tsx (the file being modified — note SettingsForm structure: Profile row, "Gyms" SectionHeader + GymsSection, "iOS Shortcuts" SectionHeader + Link row, "Notifications", "Account", delete. The NotifyToggle.toggle() pattern shows fetch+busy+router.refresh(); ChangePasswordButton shows the glass-card row button + error-text pattern. Imports already include useState, useTransition, useRouter)
    - src/app/api/onboarding-progress/route.ts (PATCH returns { ok: true, progress } on success; 400 on validation_failed)
    - .planning/phases/06-skip-on-complete-replay-completion-hardening/06-UI-SPEC.md (§Components → "Replay app tour button": Button variant ghost or secondary — NOT white accent fill; placement under "iOS Shortcuts" section or near gym/schedule; label exactly "Replay app tour"; optional sublabel "Walk through the app again"; h-11 min tap target; error copy "Couldn't restart the tour. Try again." in text-sm text-destructive; disabled at opacity-50 during in-flight PATCH; no spinner)
  </read_first>
  <action>
    In src/app/(tabs)/settings/client.tsx, add a new inner client component (e.g. `ReplayTourButton`) defined in the same file alongside ChangePasswordButton, and render it inside SettingsForm in the onboarding-adjacent region — directly after the "iOS Shortcuts" Link row (the `/shortcut` row) so it sits in the setup region per the UI-SPEC. Use the settings-row visual pattern: a real `<button type="button">` styled as a `glass-card` row (`rounded-[1.4rem] glass-card px-4 py-3.5`) matching the ChangePasswordButton trigger, with a label "Replay app tour" and a sublabel "Walk through the app again" (`text-xs text-white/50`); optionally a leading lucide `RotateCcw` icon (`h-[18px] text-white/60`) — import it from lucide-react alongside the existing icons. The control must be a quiet ghost/row, NOT the white-filled Button default variant (UI-SPEC: replay is deliberately not the accent). On click, run an async handler mirroring NotifyToggle.toggle(): guard against double-fire with a `busy` state, set busy true, `fetch("/api/onboarding-progress", { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify({ replay: true }) })`, set busy false, and on `!res.ok` show the inline error "Couldn't restart the tour. Try again." in `text-sm text-destructive` (clear it on a fresh attempt). On success, `startTransition(() => router.refresh())` so the layout RSC re-reads initialProgress and TourProvider reactivates. Disable the button while busy (`disabled={busy}` → `opacity-50` via disabled styling). No new top-level SectionHeader and no page restructure.
  </action>
  <verify>
    <automated>cd "D:/Code Projects/Gym Tracker" && grep -q "Replay app tour" src/app/(tabs)/settings/client.tsx && grep -q "replay: true" src/app/(tabs)/settings/client.tsx && npx tsc --noEmit 2>&1 | grep -i "settings/client" || echo "settings client typechecks, replay control present"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(tabs)/settings/client.tsx contains the exact label string `Replay app tour`
    - The replay handler fetches `/api/onboarding-progress` with method PATCH and body containing `replay: true`
    - On PATCH failure the file renders the exact error string `Couldn't restart the tour. Try again.` with class `text-sm text-destructive`
    - The control is NOT rendered as `<Button>` default variant (it is a ghost/glass-card row, not the white accent fill)
    - On success the handler calls `router.refresh()` inside `startTransition`
    - `npx tsc --noEmit` reports no new type errors in settings/client.tsx
  </acceptance_criteria>
  <done>A quiet ghost "Replay app tour" row sits in the Settings setup region, PATCHes { replay: true }, shows an inline error on failure, and refreshes on success to reactivate the tour.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PATCH /api/onboarding-progress | The replay flag crosses from the Settings client into the existing route handler, validated by the .strict() Zod PatchBody |
| route handler → Supabase | The upsert is owner-pinned to auth.user.id by the existing handler; the client cannot target another user's row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | replay field on PatchBody | mitigate | `replay: z.literal(true).optional()` rejects any value other than literal true (false, strings, numbers all 400); `.strict()` still rejects unknown fields — the schema surface widens by exactly one boolean-true field |
| T-06-04 | Elevation of Privilege | mergeProgress replay → dismissed:false | accept | Replay only flips the caller's own tour state; the handler already pins user_id to auth.user.id (existing Phase-1 auth gate), so a user can only replay their own tour. No privilege boundary crossed |
| T-06-05 | Tampering | completed_steps integrity on replay | mitigate | mergeProgress explicitly leaves completed_steps untouched on replay (D-04); the client still cannot send a full completed_steps array (PatchBody has no such field), so server-authoritative progress is preserved |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan — RotateCcw is from already-installed lucide-react; no legitimacy gate needed |
</threat_model>

<verification>
- `npx vitest run src/lib/onboarding-progress.test.ts` is fully green (existing + new cases)
- `npx tsc --noEmit` passes with no new errors in onboarding-progress.ts or settings/client.tsx
- grep confirms `Replay app tour` and `replay: true` appear in settings/client.tsx
- PatchBody still ends with `.strict()` and rejects `{ replay: false }`
</verification>

<success_criteria>
- The user can replay the walkthrough from Settings via the existing endpoint (PROG-03)
- Replay reactivates the tour (dismissed:false) without resetting completed_steps (D-04); auto-skip from Plan 01 fast-forwards done steps
- The Zod schema extension keeps every existing caller valid (D-06)
</success_criteria>

<artifacts_this_phase_produces>
- Modified: `src/lib/onboarding-progress.ts` — `PatchBody` gains `replay: z.literal(true).optional()`; `mergeProgress` reactivates the tour (`dismissed:false`) on replay while preserving `completed_steps`
- Modified: `src/lib/onboarding-progress.test.ts` — new PatchBody + mergeProgress replay cases
- Modified: `src/app/(tabs)/settings/client.tsx` — new `ReplayTourButton` inner component + render in SettingsForm setup region; new `RotateCcw` lucide import
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-skip-on-complete-replay-completion-hardening/06-02-SUMMARY.md` when done
</output>
