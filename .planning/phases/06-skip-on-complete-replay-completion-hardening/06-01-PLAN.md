---
phase: 06-skip-on-complete-replay-completion-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(tabs)/layout.tsx
  - src/components/tour-provider.tsx
  - src/lib/onboarding/current-step.test.ts
autonomous: true
requirements: [PROG-03]
must_haves:
  truths:
    - "On a fresh tour start, a step whose work is already done (gym set, weekly goal set, Shortcut viewed) never paints its coachmark — the tour opens on the first not-done step"
    - "deriveCurrentStep() receives real gymCount and restDays values, not the hardcoded { gymCount: 0, restDays: [] } neutrals"
    - "No new client-side fetch is added — probe data flows from the existing layout RSC reads"
  artifacts:
    - path: "src/app/(tabs)/layout.tsx"
      provides: "Real gymCount + restDays fetched server-side and passed into TourProvider"
      contains: "gymCount"
    - path: "src/components/tour-provider.tsx"
      provides: "TourProvider accepts gymCount + restDays props and forwards them to deriveCurrentStep"
      contains: "gymCount"
    - path: "src/lib/onboarding/current-step.test.ts"
      provides: "Vitest coverage proving real-probe auto-skip resolves the first not-done step"
  key_links:
    - from: "src/app/(tabs)/layout.tsx"
      to: "src/components/tour-provider.tsx"
      via: "gymCount + restDays props on <TourProvider>"
      pattern: "TourProvider[\\s\\S]*gymCount"
    - from: "src/components/tour-provider.tsx"
      to: "src/lib/onboarding/current-step.ts"
      via: "deriveCurrentStep(progress.completed_steps, progress.dismissed, { gymCount, restDays })"
      pattern: "deriveCurrentStep\\("
---

<objective>
Wire real skip-on-complete probe data through the tour so already-done setup
steps are auto-skipped at tour start, with zero flash. Phase 5 left
`TourProvider` passing hardcoded neutrals `{ gymCount: 0, restDays: [] }`; Phase 6
flows the real `gymCount` and `restDays` from the `(tabs)` layout RSC into
`deriveCurrentStep()`. This hardens PROG-02 end-to-end and is the precondition
for PROG-03 replay re-deriving completion correctly (per D-04: replay keeps
`completed_steps` intact and relies on auto-skip to fast-forward).

Purpose: Satisfy Phase 6 success criterion 1 (steps whose work is already done
are auto-skipped end-to-end, derived live from real app state) and enable
replay (Plan 02) to fast-forward done steps.
Output: Layout RSC fetches real probe values; TourProvider forwards them;
deriveCurrentStep resolves the first not-done step before the first coachmark
paints. New Vitest cases lock the real-probe behavior.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-skip-on-complete-replay-completion-hardening/06-CONTEXT.md
@.planning/phases/06-skip-on-complete-replay-completion-hardening/06-PATTERNS.md

# Files this plan modifies — read current state before editing
@src/app/(tabs)/layout.tsx
@src/components/tour-provider.tsx
@src/lib/onboarding/current-step.ts
@src/lib/onboarding/current-step.test.ts
@src/lib/onboarding/completion.ts
@src/lib/supabase/rsc.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch real gym count in the (tabs) layout RSC and pass probe props to TourProvider</name>
  <files>src/app/(tabs)/layout.tsx</files>
  <read_first>
    - src/app/(tabs)/layout.tsx (the file being modified — note it already calls getViewerProfile() and getOnboardingProgress(), and renders <TourProvider initialProgress={initialProgress}>)
    - src/lib/supabase/rsc.ts (getViewerProfile already returns rest_days; getSupabaseRSC() is the request-cached client; getOnboardingProgress() is already called here)
    - src/app/(tabs)/settings/page.tsx (analog for the user_gyms select: supabase.from("user_gyms").select("id, ...").eq("user_id", profile.id) — copy the table name, filter, and id-only select shape)
    - src/components/tour-provider.tsx (current TourProvider prop signature: { initialProgress, children })
    - .planning/phases/06-skip-on-complete-replay-completion-hardening/06-PATTERNS.md (Coupling Inventory rows for layout RSC data fetch + TourProvider prop signature)
  </read_first>
  <action>
    In src/app/(tabs)/layout.tsx, after the existing `const initialProgress = await getOnboardingProgress();` line, fetch the viewer's gym count and read rest days from the already-fetched profile. Reuse the request-cached client: `const supa = getSupabaseRSC();` (import getSupabaseRSC from "@/lib/supabase/rsc" alongside the existing imports) and `const profile = await getViewerProfile();` is already in scope from the gate above — reuse that `profile` rather than re-fetching. Query `supa.from("user_gyms").select("id").eq("user_id", profile.id)` and compute `const gymCount = gyms?.length ?? 0;`. Read `const restDays = (profile.rest_days as number[] | null) ?? [];` (getViewerProfile already selects rest_days). Pass both as new props on the existing element: `<TourProvider initialProgress={initialProgress} gymCount={gymCount} restDays={restDays}>`. Do NOT add a self-fetch of /api/onboarding-progress and do NOT call any browser fetch — this is server-side only (D-07: no new client-side fetch). Keep the existing username gate and Suspense nav structure untouched.
  </action>
  <verify>
    <automated>cd "D:/Code Projects/Gym Tracker" && npx tsc --noEmit 2>&1 | grep -v "^$" | grep -i "layout\|tour-provider" || echo "no layout/tour-provider type errors"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(tabs)/layout.tsx contains `from("user_gyms")` and `.eq("user_id", profile.id)` for the gym-count query
    - src/app/(tabs)/layout.tsx contains `gymCount={gymCount}` and `restDays={restDays}` on the `<TourProvider>` element
    - src/app/(tabs)/layout.tsx does NOT contain any `fetch(` call (grep `fetch(` returns no match in this file)
    - The existing `if (isAutoUsername(profile.username)) redirect("/onboarding/username");` gate line is unchanged
    - `npx tsc --noEmit` reports no new type errors referencing layout.tsx or tour-provider.tsx
  </acceptance_criteria>
  <done>Layout RSC fetches real gymCount (user_gyms count) and restDays (profile.rest_days), passing both as props to TourProvider with no client-side fetch.</done>
</task>

<task type="auto">
  <name>Task 2: Expand TourProvider to accept gymCount + restDays and forward them to deriveCurrentStep</name>
  <files>src/components/tour-provider.tsx</files>
  <read_first>
    - src/components/tour-provider.tsx (the file being modified — note the current useMemo passes hardcoded { gymCount: 0, restDays: [] } and the deps array is [progress.completed_steps, progress.dismissed]; the TourValue type is frozen and must NOT be extended per D-08)
    - src/lib/onboarding/current-step.ts (deriveCurrentStep signature: (completedSteps: string[], dismissed: boolean, probe: { gymCount: number; restDays: number[] }) => string | null)
    - .planning/phases/06-skip-on-complete-replay-completion-hardening/06-CONTEXT.md (D-08 in canonical_refs: TourValue type is frozen — do NOT extend it; only the component props change, not the context value)
  </read_first>
  <action>
    In src/components/tour-provider.tsx, widen the TourProvider function's props object to add `gymCount: number;` and `restDays: number[];` alongside the existing `initialProgress` and `children`. Replace the hardcoded probe object in the `currentStepId` useMemo — change `{ gymCount: 0, restDays: [] }` to `{ gymCount, restDays }`. Add `gymCount` and `restDays` to the useMemo dependency array so it recomputes if the layout re-renders with new values. Remove or update the stale comment block (lines referencing "Phase 3 passes neutral gym/schedule probe state" and "full auto-skip UX is Phase 6") to reflect that real probe data now flows in. Do NOT change the exported `TourValue` type (frozen per D-08) — `currentStepId`, `isActive`, `advance`, `dismiss` remain the only 4 context members. Do NOT add any new useTour() member. The `advance` and `dismiss` functions are unchanged.
  </action>
  <verify>
    <automated>cd "D:/Code Projects/Gym Tracker" && grep -c "gymCount: 0" src/components/tour-provider.tsx | grep -q "^0$" && echo "neutral stub removed" && npx vitest run src/lib/onboarding/current-step.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/tour-provider.tsx no longer contains the literal `gymCount: 0` (grep returns 0 matches)
    - src/components/tour-provider.tsx passes `{ gymCount, restDays }` into the `deriveCurrentStep(` call
    - The useMemo dependency array for `currentStepId` includes `gymCount` and `restDays`
    - The `TourValue` type still declares exactly four members: `currentStepId`, `isActive`, `advance`, `dismiss` (no additions)
    - `npx tsc --noEmit` reports no new type errors in tour-provider.tsx
  </acceptance_criteria>
  <done>TourProvider accepts gymCount + restDays props and forwards them to deriveCurrentStep; the frozen TourValue type is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Vitest cases proving real-probe auto-skip resolves the first not-done step with no intermediate done step</name>
  <files>src/lib/onboarding/current-step.test.ts</files>
  <read_first>
    - src/lib/onboarding/current-step.test.ts (the file being modified — note existing describe blocks already cover dismissed, fresh slate, resume, and single-probe auto-skip; this task adds the "all setup done" combined-probe cases)
    - src/lib/onboarding/current-step.ts (deriveCurrentStep iterates STEPS in order, skips completed and probe-satisfied steps)
    - src/lib/onboarding/steps.ts (STEPS order: schedule, gym, challenge, money, shortcut_viewed; TEACHING_KEYS = gym, challenge, money, shortcut_viewed)
    - src/lib/onboarding/completion.ts (isGymDone(gymCount>0), isScheduleDone(restDays non-empty), isShortcutDone(completedSteps includes "shortcut_viewed"))
  </read_first>
  <behavior>
    - Test: with completedSteps=[], probe { gymCount: 1, restDays: [0,6] } → schedule and gym both auto-skip → returns "challenge" (first not-done teaching step)
    - Test: with completedSteps=["challenge","money"], probe { gymCount: 1, restDays: [0,6] } → schedule, gym auto-skip, challenge+money done → returns "shortcut_viewed"
    - Test: with completedSteps=["challenge","money","shortcut_viewed"], probe { gymCount: 1, restDays: [0,6] } → every step auto-skipped or done → returns null (tour complete, replay would land nowhere to teach)
    - Test (no-flash contract): the FIRST returned id when gym+schedule are both probe-done and no teaching key is complete is "challenge" — never "schedule" or "gym" — proving an already-done step's id is never returned as the opening step
  </behavior>
  <action>
    Append a new describe block to src/lib/onboarding/current-step.test.ts named for the combined real-probe skip-on-complete behavior. Add the four cases described in <behavior> above using the existing `expect(deriveCurrentStep(...)).toBe(...)` / `.toBeNull()` style. Define a local `const allSetupDoneProbe = { gymCount: 1, restDays: [0, 6] as number[] };` for reuse. Do not modify existing tests. These cases lock the D-07 "feels instant, no flash" contract at the pure-logic boundary: the opening step is always the first not-done step, never a completed/auto-skipped one.
  </action>
  <verify>
    <automated>cd "D:/Code Projects/Gym Tracker" && npx vitest run src/lib/onboarding/current-step.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/onboarding/current-step.test.ts` passes with all existing AND new cases green
    - The new describe block asserts `deriveCurrentStep([], false, allSetupDoneProbe)` returns `"challenge"` (proving schedule+gym auto-skip and the opening step is the first not-done teaching step)
    - The new describe block asserts a fully-done combination returns `null`
    - No existing test case in the file was altered
  </acceptance_criteria>
  <done>New Vitest cases prove combined real-probe auto-skip opens on the first not-done step and returns null when complete; full file passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → layout RSC | The probe values (gymCount, restDays) are computed server-side in the RSC and rendered into the client TourProvider as props; the client never supplies them |
| RSC → Supabase | The user_gyms query is owner-scoped via `.eq("user_id", profile.id)` where profile.id comes from the authenticated session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | user_gyms count read in layout RSC | mitigate | Query is strictly `.eq("user_id", profile.id)` where profile.id = authenticated viewer; reuses the request-cached getSupabaseRSC() client (RLS-bound server client), never the admin client — no cross-user gym leakage |
| T-06-02 | Tampering | probe values feeding deriveCurrentStep | accept | Probe values only affect which coachmark renders (cosmetic tour state); they never touch stakes, penalties, or financial data. deriveCurrentStep is a pure read-only function. Low value target |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan — only existing app code is modified; no legitimacy gate needed |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with no new errors referencing layout.tsx or tour-provider.tsx
- `npx vitest run src/lib/onboarding/current-step.test.ts` is fully green
- grep confirms `gymCount: 0` no longer appears in tour-provider.tsx
- grep confirms no `fetch(` was added to layout.tsx
</verification>

<success_criteria>
- Real gymCount + restDays flow from the (tabs) layout RSC into deriveCurrentStep with no client-side fetch
- TourProvider's frozen TourValue type is unchanged (D-08 honored)
- Auto-skip opens the tour on the first not-done step; Vitest proves the no-flash contract at the logic boundary
</success_criteria>

<artifacts_this_phase_produces>
- Modified: `src/app/(tabs)/layout.tsx` — `gymCount` (number) + `restDays` (number[]) computed server-side; new `getSupabaseRSC` import; new props on `<TourProvider>`
- Modified: `src/components/tour-provider.tsx` — `TourProvider` props extended with `gymCount: number` + `restDays: number[]`; `deriveCurrentStep` call now receives real probe; useMemo deps extended
- Modified: `src/lib/onboarding/current-step.test.ts` — new describe block + `allSetupDoneProbe` fixture covering combined real-probe auto-skip
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-skip-on-complete-replay-completion-hardening/06-01-SUMMARY.md` when done
</output>
