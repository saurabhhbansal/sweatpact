---
phase: 06-skip-on-complete-replay-completion-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/onboarding/username/client.tsx
  - src/app/onboarding/username/page.tsx
files_deleted:
  - src/app/onboarding/gym/page.tsx
  - src/app/onboarding/gym/client.tsx
  - src/app/onboarding/schedule/page.tsx
  - src/app/onboarding/schedule/client.tsx
  - src/app/onboarding/shortcut/page.tsx
  - src/app/onboarding/shortcut/client.tsx
  - src/app/onboarding/step-indicator.tsx
autonomous: true
requirements: [PROG-03]
must_haves:
  truths:
    - "The legacy /onboarding/gym, /onboarding/schedule, /onboarding/shortcut wizard pages no longer exist"
    - "step-indicator.tsx is deleted and nothing imports it"
    - "The kept /onboarding/username route still works and, after username save, redirects to /dashboard (not the deleted /onboarding/schedule)"
    - "No code path re-forces the old multi-screen wizard; the build/typecheck passes after deletion"
  artifacts:
    - path: "src/app/onboarding/username/client.tsx"
      provides: "Username save redirects to /dashboard"
      contains: "/dashboard"
    - path: "src/app/onboarding/username/page.tsx"
      provides: "Username page no longer imports or renders the deleted StepIndicator"
  key_links:
    - from: "src/app/onboarding/username/client.tsx"
      to: "/dashboard"
      via: "router.push('/dashboard') after username PATCH succeeds"
      pattern: "router\\.push\\(\"/dashboard\"\\)"
---

<objective>
Delete the dead front-loaded onboarding wizard (D-08/D-09) and repair the two
remaining couplings in the kept `/onboarding/username` route (D-10). The shared
setup surfaces (`GymSurface`, `ScheduleSurface`, `ShortcutSurface` in
`src/components/onboarding/`) replaced these wizard pages in Phase 2 and are NOT
deleted — only the wizard page wrappers and the orphaned `step-indicator.tsx`
go. The username route stays (ONB-01 mandatory start) but currently (a) its
client redirects to the now-deleted `/onboarding/schedule` after save, and (b)
its page imports and renders `StepIndicator` (the deleted 4-step wizard progress
bar). Both must be fixed so the build passes and no path re-forces the wizard.

Purpose: Satisfy Phase 6 success criterion 4 (the legacy /onboarding/* redirect
chain is cleaned up so no path re-forces the old wizard).
Output: 7 deleted files; 2 edited files in the kept username route.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-skip-on-complete-replay-completion-hardening/06-CONTEXT.md
@.planning/phases/06-skip-on-complete-replay-completion-hardening/06-PATTERNS.md

# Files this plan touches
@src/app/onboarding/username/client.tsx
@src/app/onboarding/username/page.tsx
@src/app/onboarding/step-indicator.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit references, then delete the wizard pages and step-indicator</name>
  <files>src/app/onboarding/gym/page.tsx, src/app/onboarding/gym/client.tsx, src/app/onboarding/schedule/page.tsx, src/app/onboarding/schedule/client.tsx, src/app/onboarding/shortcut/page.tsx, src/app/onboarding/shortcut/client.tsx, src/app/onboarding/step-indicator.tsx</files>
  <read_first>
    - src/app/onboarding/username/client.tsx (kept file — line 72 redirects to /onboarding/schedule; this is fixed in Task 2, audit it here)
    - src/app/onboarding/username/page.tsx (kept file — line 5 imports StepIndicator from "../step-indicator" and line ~43 renders it; this is fixed in Task 2, audit it here)
    - .planning/phases/06-skip-on-complete-replay-completion-hardening/06-CONTEXT.md (D-08 delete gym/schedule/shortcut wizard pages; D-09 delete step-indicator.tsx; D-10 keep username; "Audit for other orphaned references" — grep before deleting; the renderer imports GymSurface/ScheduleSurface from src/components/onboarding/ which are NOT deleted)
    - .planning/phases/06-skip-on-complete-replay-completion-hardening/06-PATTERNS.md ("Deleted files — coupling points": only the wizard page clients and username/client.tsx reference the deleted paths; coachmark-renderer imports the surfaces from src/components/onboarding/ — those are safe)
  </read_first>
  <action>
    First, run a reference audit to confirm the only remaining importers/linkers of the deleted paths are the kept username route files (which Task 2 fixes). Search the whole src/ tree for the strings onboarding/gym, onboarding/schedule, onboarding/shortcut, and step-indicator. Expected surviving hits after this phase: only src/app/onboarding/username/client.tsx (the schedule redirect, fixed in Task 2) and src/app/onboarding/username/page.tsx (the StepIndicator import/use, fixed in Task 2). The src/components/tour/coachmark-renderer.tsx imports of GymSurface/ScheduleSurface resolve to src/components/onboarding/* (NOT the deleted wizard client.tsx files) — those must NOT change. If the audit surfaces any OTHER importer of the deleted paths, stop and report it rather than deleting blindly. Then delete these 7 files: src/app/onboarding/gym/page.tsx, src/app/onboarding/gym/client.tsx, src/app/onboarding/schedule/page.tsx, src/app/onboarding/schedule/client.tsx, src/app/onboarding/shortcut/page.tsx, src/app/onboarding/shortcut/client.tsx, src/app/onboarding/step-indicator.tsx. Remove the now-empty gym/, schedule/, shortcut/ directories if the runtime leaves them behind. Do NOT delete anything under src/components/onboarding/ (the shared surfaces) and do NOT touch src/app/onboarding/username/.
  </action>
  <verify>
    <automated>cd "D:/Code Projects/Gym Tracker" && test ! -e src/app/onboarding/gym && test ! -e src/app/onboarding/schedule && test ! -e src/app/onboarding/shortcut && test ! -e src/app/onboarding/step-indicator.tsx && echo "DELETED_OK" && test -e src/components/onboarding && test -e src/app/onboarding/username/page.tsx && echo "KEPT_OK"</automated>
  </verify>
  <acceptance_criteria>
    - The 4 paths src/app/onboarding/gym, /schedule, /shortcut, and src/app/onboarding/step-indicator.tsx no longer exist on disk (the verify command prints DELETED_OK)
    - src/components/onboarding/ still exists (shared surfaces intact) and src/app/onboarding/username/page.tsx still exists (the verify command prints KEPT_OK)
    - A repo-wide search for the deleted import paths finds NO importer other than the two kept username route files (which Task 2 repairs)
    - src/components/tour/coachmark-renderer.tsx is unchanged (its surface imports point at src/components/onboarding/, not the deleted wizard clients)
  </acceptance_criteria>
  <done>The 6 wizard page files and step-indicator.tsx are deleted; the shared surfaces and the username route remain; no orphaned importer survives except the two username files Task 2 fixes.</done>
</task>

<task type="auto">
  <name>Task 2: Repair the kept username route — fix the post-save redirect and remove the deleted StepIndicator</name>
  <files>src/app/onboarding/username/client.tsx, src/app/onboarding/username/page.tsx</files>
  <read_first>
    - src/app/onboarding/username/client.tsx (the file being modified — line ~71-74 the onSubmit success path runs `startTransition(() => { router.push("/onboarding/schedule"); router.refresh(); })`)
    - src/app/onboarding/username/page.tsx (the file being modified — line 5 `import { StepIndicator } from "../step-indicator";` and line ~42-44 renders `<StepIndicator current={0} total={4} />` inside an animate-fade-up-item wrapper)
    - .planning/phases/06-skip-on-complete-replay-completion-hardening/06-CONTEXT.md (D-10 keep username; the username route is the mandatory-start entry point ONB-01 — after username is claimed the user lands directly in the real app)
    - src/app/(tabs)/layout.tsx (the tabs layout gate redirects to /onboarding/username only when the username is auto-generated; once a real username is set, /dashboard renders — confirming /dashboard is the correct post-save destination)
  </read_first>
  <action>
    In src/app/onboarding/username/client.tsx, change the post-save redirect target from "/onboarding/schedule" to "/dashboard" — the success branch must call router.push("/dashboard") (keep the surrounding startTransition + router.refresh() so the tabs layout re-evaluates the gate and renders the dashboard). This lands the new user directly in the real app per ONB-01/D-01 (no wizard chain). In src/app/onboarding/username/page.tsx, remove the `import { StepIndicator } from "../step-indicator";` line (the file is deleted in Task 1) and remove the JSX block that renders `<StepIndicator current={0} total={4} />` (the surrounding animate-fade-up-item wrapper div that exists solely to hold the indicator can be removed too). Do not change anything else on the page — the SweatPactSeal header, the Card, and the UsernamePicker render stay. Do not alter the page's own isAutoUsername gate or its redirect to /dashboard for already-claimed users.
  </action>
  <verify>
    <automated>cd "D:/Code Projects/Gym Tracker" && grep -q 'router.push("/dashboard")' src/app/onboarding/username/client.tsx && ! grep -q "onboarding/schedule" src/app/onboarding/username/client.tsx && ! grep -q "step-indicator\|StepIndicator" src/app/onboarding/username/page.tsx && echo "USERNAME_FIXED" && npx tsc --noEmit 2>&1 | grep -i "onboarding" || echo "no onboarding type errors"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/onboarding/username/client.tsx contains `router.push("/dashboard")` and does NOT contain the string `onboarding/schedule`
    - src/app/onboarding/username/page.tsx contains NO reference to `step-indicator` or `StepIndicator` (no import, no JSX usage)
    - The username page still imports and renders `UsernamePicker` and `SweatPactSeal` (header + form unchanged)
    - `npx tsc --noEmit` reports no errors referencing the onboarding directory
  </acceptance_criteria>
  <done>The kept username route redirects to /dashboard after save and no longer imports or renders the deleted StepIndicator; the build typechecks.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the full build typechecks and the test suite passes after deletion</name>
  <files>src/app/onboarding/username/page.tsx</files>
  <read_first>
    - package.json (confirm the test/build scripts; `npx tsc --noEmit` is the typecheck and `npx vitest run` runs the suite)
    - .planning/phases/06-skip-on-complete-replay-completion-hardening/06-PATTERNS.md (confirms no other importer of the deleted paths — this task is the proof gate)
  </read_first>
  <action>
    Run the full TypeScript typecheck (`npx tsc --noEmit`) and the Vitest suite (`npx vitest run`) to prove the deletions left no dangling import, broken route, or failing test. The typecheck must show no NEW errors introduced by this phase's deletions (pre-existing unrelated errors, if any, are out of scope — compare against the known-clean baseline; the onboarding directory specifically must be error-free). If the typecheck reports a dangling import to any deleted path, that is a regression this task must surface and fix (e.g. a missed importer the Task 1 audit did not catch). Do not add new code beyond removing any dangling reference the typecheck reveals.
  </action>
  <verify>
    <automated>cd "D:/Code Projects/Gym Tracker" && npx tsc --noEmit 2>&1 | grep -iE "onboarding/(gym|schedule|shortcut)|step-indicator" && echo "DANGLING_IMPORT_FOUND" || echo "NO_DANGLING_IMPORTS"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` produces NO error referencing onboarding/gym, onboarding/schedule, onboarding/shortcut, or step-indicator (verify command prints NO_DANGLING_IMPORTS)
    - `npx vitest run` completes with all tests passing (no test imports a deleted path)
    - The onboarding directory contains only the `username/` route plus no `gym/`, `schedule/`, `shortcut/`, or `step-indicator.tsx`
  </acceptance_criteria>
  <done>The typecheck and test suite both pass with no dangling reference to any deleted wizard path; the cleanup is proven complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time | This plan only deletes dead route files and edits one client redirect + one page import; no runtime request boundary is added or changed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Denial of Service | deleting an in-use route | mitigate | A pre-deletion repo-wide reference audit (Task 1) confirms no surviving importer except the two kept username files (repaired in Task 2); Task 3 proves it with tsc + vitest, so no live route is broken by the deletion |
| T-06-10 | Tampering | username post-save redirect target | accept | Redirect changes from a deleted in-app route to /dashboard (an authenticated tab route already gated by the tabs layout). No new external/untrusted destination; the layout gate still enforces the username requirement |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs — this plan only deletes and edits existing files; no legitimacy gate needed |
</threat_model>

<verification>
- The 6 wizard files + step-indicator.tsx are gone; src/components/onboarding/ and the username route remain
- `npx tsc --noEmit` shows no dangling import to any deleted path (NO_DANGLING_IMPORTS)
- `npx vitest run` passes fully
- username/client.tsx redirects to /dashboard; username/page.tsx no longer references StepIndicator
</verification>

<success_criteria>
- The legacy /onboarding/* wizard chain is deleted and no path re-forces it (Phase 6 success criterion 4)
- The kept username mandatory-start route works and lands the user on /dashboard
- The build typechecks and tests pass after the deletions
</success_criteria>

<artifacts_this_phase_produces>
- Deleted: `src/app/onboarding/gym/page.tsx`, `src/app/onboarding/gym/client.tsx`, `src/app/onboarding/schedule/page.tsx`, `src/app/onboarding/schedule/client.tsx`, `src/app/onboarding/shortcut/page.tsx`, `src/app/onboarding/shortcut/client.tsx`, `src/app/onboarding/step-indicator.tsx`
- Modified: `src/app/onboarding/username/client.tsx` — post-save redirect changed to `/dashboard`
- Modified: `src/app/onboarding/username/page.tsx` — removed StepIndicator import + render
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-skip-on-complete-replay-completion-hardening/06-04-SUMMARY.md` when done
</output>
