interface:
  display_name: "Sweatpact"
  short_description: "Repo-specific patterns and workflows for sweatpact"
  default_prompt: "Use the sweatpact repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true